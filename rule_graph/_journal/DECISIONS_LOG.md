# 결정 기록

이 작업은 사용자 개입 없이 진행된다. 막혀서 스스로 정한 것을 **전부** 여기에 남긴다.

이 기록이 없으면 나중에 추적할 수 없고, 다른 방향으로 수행된 병행 작업과의 비교도 성립하지 않는다. 특히 **「무엇 때문에 막혔는가」가 빠지면 같은 지점에서 다른 선택을 했는지를 알 수 없다.**

## 기록 규칙

- **결정한 그 자리에서 쓴다.** 미루면 왜 그랬는지가 이미 소실된다
- 네 항목을 **빠짐없이** 쓴다. 하나라도 비면 기록이 아니다
- **고르지 않은 선택지도 적는다.** 무엇을 버렸는지가 판단의 절반이다
- 결정을 내린 커밋에는 이 파일의 변경이 포함되어야 한다
- append-only — 기존 항목을 고치지 않는다. 뒤집혔으면 새 항목으로 적고 이전 번호를 가리킨다

## 판단 기준

막혔을 때 정하는 순서다. 위에서부터 적용한다.

1. **설계의 불변식을 깨지 않는 쪽**
2. **되돌리기 쉬운 쪽**
3. **읽기 비용이 낮은 쪽**
4. **단순한 쪽**

`왜` 항목에는 이 중 무엇이 작동했는지를 적는다.

## 기록 형식

```markdown
## D-<n> — <한 줄 제목>

- **시점**: <어느 batch / item 에서>
- **막힌 지점**: <무엇이 정해져 있지 않아서 진행할 수 없었는가>
- **선택지**:
  - (a) <...>
  - (b) <...>
  - (c) <...>
- **선택**: <무엇을 골랐는가>
- **왜**: <판단 기준 중 무엇이 작동했는가. 버린 것을 왜 버렸는가>
- **되돌릴 수 있는가**: <예 / 아니오 / 조건부 — 조건>
```

---

## 기록

## D-1 — 원소 트리의 내부 배치를 종류별 디렉터리로 정한다

- **시점**: B1 / item-01-extract 착수 시점
- **막힌 지점**: `item-01` 이 *"산출 위치는 `rule_graph/` 아래이며, 구체 경로는 정해져 있지 않다"* 라고만 정했다. 원소 정본·합성 선언·INDEX·추출 작업기록 네 종류를 어느 경로에 둘지가 없어서 첫 파일을 만들 수 없었다.
- **선택지**:
  - (a) `rule_graph/` 바로 아래 평면 배치 — 모든 파일이 같은 층
  - (b) 종류별 하위 디렉터리 — `rule_graph/elements/`, `rule_graph/composites/`, `rule_graph/_work/`, INDEX 는 `rule_graph/INDEX.md`
  - (c) 원문 파일별 디렉터리 — `rule_graph/rule-authority/{elements,composites}/`
- **선택**: (b)
- **왜**: 설계가 *"파일이 class 이고 폴더가 계층이다"* 라고 정했고, 원소/합성은 권위 축의 계층 그 자체이므로 폴더가 갈라야 할 것이 바로 그것이다(기준 1). (c)를 버린 이유가 더 크다 — 폴더를 **출처(원문 파일)** 로 가르면 서로 다른 규칙이 같은 원소를 참조하는 경로가 구조적으로 막히고, 같은 의미의 원소가 파일별로 복제되어 **불변식 5(정본은 하나)** 를 깨는 방향으로 밀린다. (a)는 파일럿 한 본에서는 성립하지만 B4 대상(239행)에서 즉시 무너진다(기준 4의 단순함이 규모에서 역전된다).
- **되돌릴 수 있는가**: 예. INDEX 가 key → 경로 매핑을 소유하므로 파일을 옮겨도 key 는 불변이고 INDEX 의 경로 열만 고치면 된다.

## D-2 — 원소와 합성이 하나의 번호 공간을 공유한다

- **시점**: B1 / item-01-extract (item-02 주의 ① 이 「부여하기 전에 정하라」고 요구)
- **막힌 지점**: 설계는 `RE-00001` / `RC-00001` 표기만 고정했고, **RE 와 RC 가 번호를 공유하는지 따로 세는지**를 정하지 않았다. 부여를 시작한 뒤에 바꾸면 key 재작성이 되고 그것은 불변식 2 위반이므로, 첫 번호를 붙이기 전에 정해야 했다.
- **선택지**:
  - (a) 공유 — 단일 카운터. `RE-00001`, `RC-00002`, `RE-00003` …
  - (b) 분리 — 타입별 카운터. `RE-00001`…, `RC-00001`… 이 공존
  - (c) 원문 파일별 블록 할당 — `rule-authority` 는 00001–00999
- **선택**: (a) 공유 단일 번호 공간
- **왜**: 발번 상태가 **하나**여서 충돌 가능성이 구조적으로 없다(기준 4). 그리고 되돌리기(기준 2)에서 갈렸다 — 강등이 일어나 타입 접두사를 바꿔야 하는 상황이 오면(실제로 `F-2` 에서 그 상황이 관측됐다), 번호가 전역 유일해야 접두사만 바꿔도 다른 key 와 충돌하지 않는다. (b)는 그 경우 `RE-00007` 이 이미 있는데 `RC-00007` 을 `RE-00007` 로 바꾸려는 충돌이 생긴다. (c)는 *"숫자에 의미를 부여하지 않는다"* 를 정면으로 깨므로 기준 1 에서 탈락.
- **되돌릴 수 있는가**: **아니오.** 되돌리려면 이미 부여한 key 를 재작성해야 하고 그것이 불변식 2 위반이다. 이 결정은 부여 시점 이후 고정이다.

## D-3 — INDEX 는 좁은 표로 두고 출처 앵커는 원소 정본 옆에 둔다

- **시점**: B1 / item-01-extract
- **막힌 지점**: `item-02` 가 INDEX 에 **출처** 필드를 요구하면서 동시에 *"행 번호는 파일이 바뀌면 stale 되므로 `_convention/ANCHOR.md` 의 문면 기반 앵커를 쓴다"* 고 했다. 그런데 앵커는 `PATH / HASH / QUOTE / MEANS / VERIFY` 다섯 줄 블록이라 **표 한 칸에 들어가지 않는다.** 형식이 결정되지 않아 INDEX 를 만들 수 없었다.
- **선택지**:
  - (a) INDEX 를 표가 아닌 앵커 블록 나열로 만든다 — 한 key 당 블록 하나
  - (b) INDEX 는 좁은 표(key·종류·상태·권위·경로·출처종별·생성)로 두고, 전체 앵커 블록은 **각 원소 정본 안**에 그 원소와 붙여 둔다
  - (c) 앵커만 모은 전용 파일 `rule_graph/_work/anchors.md` 를 따로 둔다
- **선택**: (b)
- **왜**: 기준 3(읽기 비용). INDEX 는 설계에서 *"개정 진입점"* 이고 그 용도는 key → 경로를 얻는 것 하나다. 앵커 블록까지 넣으면 key 하나를 찾기 위해 다섯 배 분량을 읽게 되어 INDEX 를 싸게 유지한다는 V1 의 전제(*"싸다 — INDEX 만 읽음"*)가 깨진다. (c)는 원소와 앵커가 다른 파일에 있어 원소를 고칠 때 앵커를 같이 고칠 이유가 사라지고, 두 파일이 조용히 어긋난다. (a)는 (c)보다 낫지만 INDEX 비용 문제가 그대로다.
- **되돌릴 수 있는가**: 예. 앵커 블록은 이동해도 key 에 영향이 없다.

## D-4 — 원소 파일 하나에 여러 원소를 담는다 (성질 묶음 = class)

- **시점**: B1 / item-01-extract (item-02 「확인되지 않은 것」이 이 item 에서 정하라고 지목)
- **막힌 지점**: 원소 정본을 **1파일 1원소**로 둘지 **1파일 다원소**로 둘지가 미정이었다. 파일 개수와 경로가 여기서 갈리므로 첫 원소를 쓰기 전에 정해야 했다.
- **선택지**:
  - (a) 1파일 1원소 — `rule_graph/elements/RE-00001.md`
  - (b) 성질 묶음 1파일 다원소 — 같은 성질의 원소들이 한 파일. 파일이 class
  - (c) 원문 파일 1개당 원소 파일 1개
- **선택**: (b)
- **왜**: 기준 3. 설계의 4층 구조는 **읽기 비용 분리**가 목적인데 (a)는 파일럿 한 본에서만 25개 파일을 만들고 B4 대상까지 가면 수백 개가 된다 — 원소 하나를 고치려고 역참조를 훑을 때 파일 I/O 가 그대로 비용이 된다. 설계가 *"불필요한 파일 I/O 가 곧 운용 비용이다"* 라고 명시한 것과 정면으로 어긋난다. (c)는 D-1 에서 배제한 출처 기준 결합을 파일 층에서 다시 들여온다. 설계의 *"파일이 class"* 에 실제로 대응하는 것은 성질 묶음이다.
- **되돌릴 수 있는가**: 예. 쪼개도 key 는 불변이고 INDEX 의 경로 열만 바뀐다.

## D-5 — 파일럿 단계의 원소 class 는 원문의 H2 절이 아니라 관측된 성질로 가른다

- **시점**: B1 / item-01-extract
- **막힌 지점**: D-4 로 「성질 묶음이 파일」이 정해지자 **무엇을 성질로 볼 것인가**가 비었다. `HANDOFF.md` 의 열린 항목 3(*"원소 트리의 폴더 구조 — 성질·특성을 무엇으로 가르나"*)이 그대로 걸렸다. 파일럿은 원문 한 본뿐이라 비교 대상이 없다.
- **선택지**:
  - (a) 원문의 H2 절(`자격` / `권위 등급` / `충돌과 처분`)을 그대로 class 로 쓴다
  - (b) 추출된 원소의 성질(정의 / 사실 / 범위·적재 / 절차)로 가른다
  - (c) 파일럿에서는 가르지 않고 단일 파일에 전부 담고, B2 이후 두 번째 원문이 생겼을 때 가른다
- **선택**: (b) — 다만 파일럿에서 실제로 나온 class 는 네 개이며, 다섯째가 필요해지면 그때 추가한다
- **왜**: (a)를 버린 것이 이 결정의 실질이다. H2 절은 **원문 파일의 편집 구조**이지 원소의 성질이 아니다. H2 를 class 로 쓰면 D-1 에서 배제한 「출처가 구조를 결정한다」가 폴더 층을 우회해 파일 이름 층으로 되돌아온다 — 다른 규칙 파일에서 같은 성질의 원소가 나와도 합류할 자리가 없다. (c)는 되돌리기(기준 2)로는 제일 싸지만, 그러면 이 item 이 B2 에 넘겨야 할 산출(*"원소 추출의 실제 입도"*, *"INDEX 의 형식"*)에 class 기준이 빠진 채 넘어가고 같은 결정을 B2 에서 다시 만난다.
- **되돌릴 수 있는가**: 예. class 재편은 파일 이동이고 key 는 불변이다.

## D-6 — `authority` 를 타입 접두사가 아니라 INDEX 의 독립 필드가 소유한다

- **시점**: B1 / item-01-extract. `F-2` 를 만난 자리
- **막힌 지점**: 설계가 세 조항을 동시에 요구하는데 강등 시나리오에서 서로를 부정한다 — ① 타입 접두사로 차단 가능 여부를 알 수 있어야 한다 ② 강등해도 key 는 유지된다 ③ key 는 불변이다. `RC-` 로 발번된 합성이 강등되면 접두사가 거짓말을 하게 되고, 접두사를 고치면 ②③이 깨진다. 어느 쪽을 포기할지가 설계에 없다.
- **선택지**:
  - (a) 강등 시 `RC-` key 를 반납하고 `RE-` key 를 새로 발번한다
  - (b) key 는 접두사째 유지하고, `authority` 를 INDEX 의 독립 필드로 두어 강등을 그 필드 변경으로 표현한다
  - (c) 세 번째 접두사(예: `RD-` = demoted)를 도입한다
  - (d) 강등을 아예 쓰지 않는다 — 강등 대신 반납만 쓴다
- **선택**: (b)
- **왜**: 기준 1 이 갈랐다. 불변식 6개 목록에 「접두사로 권위를 안다」는 **들어 있지 않다** — 그것은 `DESIGN.md` 「key 표기」 절의 **근거 설명**이지 불변식이 아니다. 반면 (a)와 (c)는 불변식 2(key 불변)를 직접 깨고, (a)는 *"강등은 삭제가 아니다"* 도 함께 깬다. (d)는 불변식은 안 깨지만 `F-4` 가 보여주듯 강등 대상이 실제로 10건 나오는 상황에서 그 10건을 전부 삭제하게 되어 원문 규범을 잃는다.
- **되돌릴 수 있는가**: 조건부 — INDEX 필드를 없애는 것은 언제든 가능하지만, 그 시점까지 강등된 key 가 있으면 그 강등 상태를 표현할 곳이 사라진다.
- **대가**: *"key 만 보고 차단 가능 여부를 알 수 있어야 한다"* 가 성립하지 않게 된다. `FINDINGS.md` `F-2` 에 기록했다. 이것은 설계 수정이 필요한 지점이며 이 결정으로 닫히지 않는다.

## D-7 — `|S| ≥ 2` 를 차단 방향 `정` 에만 적용한다 (파일럿 범위)

- **시점**: B1 / item-01-extract. `F-3` 을 만난 자리
- **막힌 지점**: 합성 후보 19개 중 8개가 참조 슬롯 1개뿐이라 `|S| ≥ 2` 를 못 채운다. 설계대로면 별칭이므로 원소로 강등되고 차단력을 잃는다. 그런데 **그중 5개는 작업이 아니라 차단 주장을 막는 조각**이다(방향 `역`). 강등하면 원문의 무효화 권한이 사라진다. 설계에 이 경우의 처리가 없다.
- **선택지**:
  - (a) 설계대로 8개 전부 원소로 강등한다
  - (b) 방향 `역` 조각을 다른 원소와 묶어 억지로 `|S| ≥ 2` 를 채운다
  - (c) `|S| ≥ 2` 를 방향 `정` 에만 적용하고, 방향 `역` 은 슬롯 1개도 합성으로 인정한다
  - (d) `|S| ≥ 2` 규칙 자체를 버린다
- **선택**: (c)
- **왜**: `|S| ≥ 2` 의 **근거를 보고 정했다.** 설계가 든 이유는 *"별칭에 권위를 주면 원소가 우회 경로로 차단력을 얻는다"* 다. 이 우려는 방향 `정` 에서만 성립한다 — 방향 `역` 인 단일 조각은 원소에 차단력을 **주는** 것이 아니라 **빼앗는** 것이므로 우회 경로가 생기지 않는다. 즉 (c)는 규칙을 약화시키는 것이 아니라 **규칙의 근거가 미치는 범위로 규칙을 좁히는 것**이다(기준 1). (b)는 실제로 시도했고 실패했다 — 묶을 원소를 저작해야 하고 그 조합이 원문에 없는 주장을 만든다(`F-3` 참조). (a)는 원문 규범 5건을 잃는다. (d)는 별칭 문제가 방향 `정` 에서 그대로 살아난다.
- **되돌릴 수 있는가**: 예. 방향은 INDEX 필드이므로 조건을 다시 넓히면 된다.
- **범위**: **파일럿 한정.** 이것은 설계 수정 제안이며, B2 이후에 같은 판단을 반복하지 말고 설계 자체를 고칠지가 별도로 결정돼야 한다.

## D-8 — 강등 판정 기준을 「자립하는가」에서 「복원 후에도 규범 내용이 남는가」로 좁힌다

- **시점**: B1 / item-01-extract. `F-4` 를 만난 자리
- **막힌 지점**: 설계의 강등 기준은 *"분해했을 때 각 조각이 의미를 갖지 못하면 권위 없는 원소다"* 인데, 실측하면 「의미를 갖는가」가 아니라 **「주어를 생략했는가」** 를 재고 있다. 강등 대상 10건 **전부**가 주어 생략(7) 또는 조응(3) 때문이고, 규범 내용이 없어서 강등된 것은 0건이다. 이 기준을 그대로 쓰면 저자가 주어를 채워 쓰는 것만으로 차단력을 만들 수 있다.
- **선택지**:
  - (a) 설계대로 「절단 직후 자립하는가」로 판정한다
  - (b) **「주어·조응을 복원한 뒤에도 규범 내용이 남는가」** 로 판정한다
  - (c) 판정 전에 원문을 자립적으로 다시 쓴 뒤 분해한다 (전처리)
  - (d) 강등 판정을 사람에게 남기고 기계 기준을 두지 않는다
- **선택**: (b)
- **왜**: 기준 1. (a)는 「권위 = 의미」라는 설계의 명시 원칙을 실제로는 「권위 = 문장 작성 방식」으로 바꿔 놓으므로, 설계를 따르는 것처럼 보이면서 설계의 근거를 깬다. (c)는 **원문 개정이 추출의 선행 조건**이 되어 `item-01` 의 *"원문을 고치지 마라"* 를 정면으로 위반하고, 무엇보다 상류역행(원문을 고치러 올라감)을 이 트랙 안에서 재현한다 — 트랙이 끊으려는 바로 그 기전이다. (d)는 `F-4` 를 기록만 하고 판정을 미루는 것이라 파일럿이 닫히지 않는다.
- **되돌릴 수 있는가**: 예. 판정 기준이지 산출물 구조가 아니다.
- **남는 것**: 「복원」의 범위(어디까지가 복원이고 어디부터가 저작인가)는 여전히 사람 판정이다. 이 파일럿에서는 **원문 안에 선행사가 존재하는 경우만 복원**으로 인정했다 — `RA-08` 의 "해당 primitive" 는 `RA-07A` 가 선행사이므로 복원, 선행사가 없으면 저작.

## D-9 — 원소 class 는 실제로 4개가 나왔고 「절차」는 나오지 않았다

- **시점**: B1 / item-02-key-index. `D-5` 를 실제 원소 25개에 적용하면서
- **막힌 지점**: `D-5` 는 성질로 가른다고 정하면서 예상 class 를 `(정의 / 사실 / 범위·적재 / 절차)` 로 괄호 안에 적었다. 25개를 실제로 분류하니 **「절차」에 해당하는 원소가 하나도 없고**, 대신 예상에 없던 **「요건」이 12개(48%)** 나왔다. 예상한 이름을 억지로 쓸지, 나온 대로 바꿀지가 정해져 있지 않았다.
- **선택지**:
  - (a) `D-5` 의 괄호대로 「절차」 파일을 만들고 비워 둔다
  - (b) 나온 대로 `범위·적재 / 정의 / 요건 / 사실` 네 class 로 확정한다
  - (c) 「요건」을 「정의」에 흡수해 3 class 로 줄인다
- **선택**: (b)
- **왜**: 「절차」 원소가 없는 것은 우연이 아니라 **구조적 결과**다 — 절차를 서술하는 조각은 전부 차단 성질을 가져서 합성으로 갔다. 빈 파일을 만들면 그 사실이 가려진다(기준 1: 설계는 원소를 비차단으로 정의한다). (c)를 버린 이유는 「정의」와 「요건」의 출처 분포가 정반대이기 때문이다 — 정의 4개는 전부 `추출`, 요건 12개는 10개가 `저작`이다. 섞으면 **어느 원소가 원문에 있었고 어느 것을 우리가 썼는지**가 파일 층에서 안 보이게 된다.
- **되돌릴 수 있는가**: 예. class 재편은 파일 이동이고 key 는 불변이다.
- **`D-5` 와의 관계**: `D-5` 를 뒤집는 것이 아니라 그 괄호 안의 예시를 실측으로 대체한다. `D-5` 는 append-only 규칙에 따라 고치지 않았다.

## D-10 — 합성 key 를 정본 생성 전에 발번하고 `발번됨(정본 미생성)` 상태를 둔다

- **시점**: B1 / item-02-key-index
- **막힌 지점**: 원소 정본을 쓰다 보니 원소가 **형제 조각이 어느 합성으로 갔는지**를 가리켜야 했다(예: `RE-00003` 이 분리해 낸 부정구가 `RC-00028` 로 갔다는 것). 그러려면 합성 key 가 이미 있어야 하는데, 합성 정본은 `item-03` 에서 만든다. INDEX 에 없는 key 를 참조하면 불변식 3(참조 무결성)이 깨진 상태로 커밋된다. 설계에 이 중간 상태의 표현이 없다.
- **선택지**:
  - (a) 원소 정본에서 합성 참조를 빼고 `item-03` 에서 다시 넣는다
  - (b) `item-02` 와 `item-03` 을 한 커밋으로 합친다
  - (c) INDEX 가 발번의 단일 소유자이므로 합성 key 를 **먼저 발번**하고, 정본이 아직 없는 상태를 `발번됨(정본 미생성)` 으로 표기한다
  - (d) 합성 정본 파일을 빈 stub 으로 미리 만든다
- **선택**: (c)
- **왜**: 기준 1. 설계가 *"INDEX 가 key 의 단일 소유자"* 라고 못박았으므로, 발번은 정본 생성과 분리될 수 있고 오히려 그것이 단일 소유의 뜻이다. 중간 상태를 **INDEX 에 드러내는 쪽**이 감춘 채로 가는 것보다 검사 가능하다 — `상태` 열만 보면 아직 안 닫힌 key 가 몇 개인지 즉시 나온다. (a)는 참조를 두 번 쓰는 일이고 중간에 빠뜨리면 조용히 사라진다. (b)는 커밋 규율의 *"의존이 닫힌 단위"* 를 만족시키지만 되돌릴 단위가 커진다(기준 2). (d)는 빈 파일이 정본인 척하게 되어 `V2`(정본·사본 일치)가 빈 정본과 일치하는 사본을 통과시킨다.
- **되돌릴 수 있는가**: 예 — 상태 어휘를 없애면 되지만, 그러면 이 중간 상태를 표현할 곳이 사라진다.
- **대가**: `상태` 어휘가 2값에서 3값이 됐다. 설계에는 `살아있음 / 반납됨` 만 있었다.

## D-11 — 출처 어휘를 `추출` / `복원` / `저작` 3값으로 둔다

- **시점**: B1 / item-02-key-index
- **막힌 지점**: `item-03` 이 *"원문에 없는 원소를 만들 때 그것이 추출이 아니라 저작임을 명시하라"* 고 요구하는데, 실제로는 **둘 사이에 걸린 것**이 있다. `RE-00008` 은 원문 "결정 가능한 predicate가 … 연결되어 있고"에서 주어 `Hard gate` 를 같은 줄의 label 로 채운 것이다. 어휘를 한 글자도 새로 쓰지 않았으므로 저작이라 하기 어렵고, 문면 그대로도 아니므로 추출도 아니다. 2값으로는 이 5건을 어디에도 넣을 수 없었다.
- **선택지**:
  - (a) 2값 유지 — 주어 복원도 저작으로 센다
  - (b) 2값 유지 — 주어 복원은 추출로 센다
  - (c) 3값 — `추출` / `복원` / `저작`
- **선택**: (c)
- **왜**: 이 구분이 **`F-4` 의 측정 그 자체**다. `F-4` 가 관측한 것은 「주어를 채우면 권위가 생긴다」인데, (a)나 (b)를 쓰면 주어를 채운 건수가 다른 범주에 섞여 사라진다. 즉 2값을 유지하면 이 파일럿의 핵심 발견을 스스로 못 세게 된다(기준 1 — 설계 검증이라는 목적을 깨뜨리지 않는 쪽). (a)는 저작 비율을 부풀려 `F-1-a` 를 과대 보고하고, (b)는 축소 보고한다.
- **되돌릴 수 있는가**: 예. INDEX 의 한 열이며 병합은 언제든 가능하다.
- **판정 기준**: `복원` 은 **원문 안에 선행사가 존재하는 경우만**이다(`D-8` 과 같은 기준). 선행사가 없으면 `저작`.

## D-12 — 합성 정본은 그것이 서비스하는 규칙 표면별로 묶는다

- **시점**: B1 / item-03-compose
- **막힌 지점**: `D-1` 이 `rule_graph/composites/` 디렉터리를 정했지만 **그 안에서 파일을 무엇으로 가르는지**는 정하지 않았다. 원소는 `D-5` 로 성질별이 정해졌는데 합성에는 대응하는 결정이 없었다.
- **선택지**:
  - (a) 원소와 같이 성질별 (`차단 유형별`, `방향별` 등)
  - (b) **그것이 서비스하는 규칙 표면별** — `composites/rule-authority.md`
  - (c) 합성 하나당 파일 하나
- **선택**: (b)
- **왜**: `D-1` 이 원소에 대해 출처별 배치를 배제한 근거가 **재사용**이었다 — 여러 규칙이 같은 원소를 참조해야 하므로 출처로 가르면 안 된다. 합성에는 그 근거가 적용되지 않는다. 합성은 *"작업을 막을 수 있는 규범"* 이고, 무엇을 막는지는 그것이 속한 규칙 표면이 정한다. 즉 **합성은 재사용 대상이 아니라 소속 대상**이다. 그리고 쿡북이 자기 합성을 찾아갈 때 파일 하나만 열면 되므로 읽기 비용이 낮다(기준 3). (c)는 19개 파일이 되어 `D-4` 에서 배제한 이유가 그대로 재현된다.
- **되돌릴 수 있는가**: 예. INDEX 경로 열 수정.
- **주의**: 합성이 다른 규칙의 합성을 참조하는 경우(`RC-00035` → `RC-00031` 같은 형태가 규칙 표면을 넘을 때)는 이 파일럿에서 발생하지 않았다. B2 이후에 나오면 이 결정이 다시 걸린다.

## D-13 — 저작된 용어 정의를 별도 class 로 분리한다

- **시점**: B1 / item-03-compose
- **막힌 지점**: 합성 슬롯을 채우려니 원문에 정의가 없는 용어 12개를 저작해야 했다. 전부 「정의」 성질이므로 `D-5` 대로면 `elements/definition.md` 에 들어간다. 그런데 기존 `definition.md` 4건은 전부 `출처 = 추출` 이고 새 12건은 전부 `저작` 이다. 섞을지 가를지가 정해져 있지 않았다.
- **선택지**:
  - (a) `definition.md` 에 합쳐 넣고 각 원소의 `출처` 표기로만 구분한다
  - (b) `elements/authored-term.md` 를 새 class 로 만든다
  - (c) `저작` 원소 전부(요건 10건 포함)를 한 파일로 모은다
- **선택**: (b)
- **왜**: `D-9` 에서 「정의」와 「요건」을 가른 것과 **같은 근거**다 — 출처 분포가 정반대인 것을 한 파일에 두면 「원문에 있던 것」과 「우리가 만든 것」이 파일 층에서 안 보인다. `item-03` 이 *"이 구분이 없으면 나중에 원문에 있던 것과 우리가 만든 것이 섞여 되돌릴 수 없다"* 고 명시적으로 요구했다(기준 1·2). (c)는 성질이 다른 것(요건과 용어 정의)을 출처만으로 묶어 `D-5` 의 class 기준을 깬다. (a)는 요구를 INDEX 열 하나에만 의존시킨다.
- **되돌릴 수 있는가**: 예.
- **부수 효과**: 이 class 가 존재한다는 사실 자체가 측정값이 됐다 — 파일 하나를 보면 **원문이 정의하지 않고 쓴 용어가 12개**라는 것이 즉시 나온다. `FINDINGS.md` `F-7`

## D-14 — 쿡북과 합성 선언은 물리적으로 분리한다

- **시점**: B1 / item-03-compose. `HANDOFF.md` 열린 항목 2 이자 `item-03` 기대 결과 5
- **막힌 지점**: *"기존 파일이 쿡북이 되는데, 합성규칙 선언을 같은 파일에 둘지 분리할지"* 가 설계에서 열려 있었다.
- **선택지**:
  - (a) 같은 파일 — `rules/rule-authority.md` 안에 쿡북과 합성 선언을 모두 둔다
  - (b) 분리 — 쿡북은 `rules/rule-authority.md`, 합성은 `rule_graph/composites/rule-authority.md`
  - (c) 쿡북을 없애고 합성 선언 파일만 둔다
- **선택**: (b)
- **왜**: 기준 3 이 결정적이었고, **실측이 그것을 뒷받침한다.** 합성 선언 파일은 284행이다. (a)를 택하면 일상 작업이 여는 파일이 349행이 되어 4층 구조의 목적(*"일상 작업은 쿡북만 읽는다"*)이 정의상 불가능해진다. 분리하면 쿡북은 65행에서 멈춘다. (c)는 `rules/rule-authority.md` 가 사라지는데, 이 repo 의 `scripts/docs-working-model-check.ps1` 이 `rule_docs/rule-authority/` 폴더에서 계산한 경로의 **존재**를 검사하므로 두 개의 FAIL 이 발생한다(`RULE_DOCS-CANDIDATE-BACKLOG` · `RULE_DOCS-ORPHAN`). 그 검사는 파일 **내용은 읽지 않으므로**, 같은 경로에 쿡북을 남기는 (b)는 통과한다.
- **되돌릴 수 있는가**: 예. 두 파일을 합치는 것은 언제든 가능하다.
- **확인한 사실**: 위 검사 동작은 이 브랜치에서 서브에이전트가 `scripts/docs-working-model-check.ps1:215-219, 223-224, 244-245` 를 읽어 확인했다. `tests/**` 에는 이 파일에 대한 단언이 **없다**.

## D-15 — 쿡북의 제목행만 바이트 그대로 유지한다

- **시점**: B1 / item-03-compose
- **막힌 지점**: 쿡북 전환은 원문 문면을 들어내는 것이므로 원소 37개의 출처 앵커 `QUOTE` 가 working tree 에서 사라진다. 앵커를 어떻게 다룰지가 정해져 있지 않았다.
- **선택지**:
  - (a) 앵커를 쿡북의 새 문면으로 다시 잡는다
  - (b) 앵커를 `HASH` 로 고정된 이력 조회 전용으로 격하하고, 제목행 하나만 바이트 그대로 남긴다
  - (c) 원문 문면을 쿡북 안에 주석으로 보존해 앵커를 살린다
- **선택**: (b)
- **왜**: (a)는 **출처를 가짜로 만든다** — 추출은 원문에서 일어났는데 앵커가 쿡북을 가리키면 그 원소가 쿡북에서 나온 것처럼 보인다. (c)는 원문 전문이 쿡북 안에 남아 `F-10` 의 팽창을 더 키우고, 무엇보다 **정본이 둘이 된다**(불변식 5 위반). (b)는 앵커의 성격을 「현재 위치」에서 「이력」으로 바꾸지만, 추출 출처로서는 그쪽이 정확하다 — 이후 원문이 더 바뀌어도 출처가 흔들리지 않는다(기준 1·2).
- **되돌릴 수 있는가**: 예. `HASH` 는 남아 있으므로 언제든 그 시점 문면을 복원할 수 있다.
- **대가**: `_convention/ANCHOR.md` 가 **핵심**이라 부른 조회(현재 위치 재탐색)가 이 앵커들에 대해 성립하지 않는다. `FINDINGS.md` `F-9`

## D-16 — B2 대상으로 `rule-conflict-and-revision-routing.md` 를 고른다

- **시점**: B2 착수
- **막힌 지점**: 로드맵과 설계가 *"`snippets/rules/` 중 1본"* 이라고만 정했다. 5개 후보 중 무엇인지가 없다.
- **선택지**:
  - (a) `repository-change-safety.md` — 22행 2335B, 가장 작다
  - (b) `rule-conflict-and-revision-routing.md` — 42행 3474B, H2 8개
  - (c) `global-file-mutation-boundary.md` — 42행 5366B
  - (d) `no-background-or-hidden-state.md` — 39행 5856B
- **선택**: (b)
- **왜**: B2 의 목적은 *"원소 트리 없이 배포본만으로 자립하는지"* 를 **실패하는 쪽으로 시험**하는 것이다. 그렇다면 자립이 깨질 여지가 가장 큰 대상을 골라야 한다. 실측으로 확인한 것 — 5개 중 다른 파일을 참조하는 문면이 있는 것은 `repository-change-safety.md`(1건)와 `global-file-mutation-boundary.md`(4건)이고, (b)는 **0건**이다. 즉 (b)는 **원문 상태에서 이미 완전히 자립**한다. 분해 후에도 자립하는지를 재면, 자립이 깨졌을 때 그 원인이 **분해 때문**임이 확정된다 — 원래 안 되던 것과 섞이지 않는다(기준 1: 시험의 목적을 깨뜨리지 않는 쪽).

  (b)를 고른 두 번째 이유는 **주제**다. 이 파일은 규칙 충돌과 개정 라우팅을 다루며, 트랙의 결과(*"거슬러 올라가는 것을 최대한 막는다"*)가 겨냥하는 바로 그 대상이다. B1 이 `rule-authority.md` 로 자기 적용 실증을 했듯 B2 도 같은 성격을 갖는다.

  (a)를 버린 이유 — 가장 작다는 것은 B1 에서 이미 `F-10` 을 만들었다. 작은 대상을 또 고르면 같은 조건에서 같은 결과를 다시 얻을 뿐이고, 대상 크기가 원인인지를 가려내지 못한다.
- **되돌릴 수 있는가**: 예. B2 를 다른 파일로 다시 하는 것은 언제든 가능하다.

## D-17 — 배포 tier 에서는 쿡북과 합성 선언을 한 파일에 둔다 (`D-14` 가 성립하지 않는다)

- **시점**: B2
- **막힌 지점**: `D-14` 는 쿡북(`rules/`)과 합성 선언(`rule_graph/composites/`)을 물리적으로 분리하기로 정했다. 그런데 배포 tier 에 같은 배치를 적용하면 **adopter 가 합성 선언을 받지 못한다.** `rule_graph/` 가 배포 payload 가 아니기 때문이다. 어느 쪽을 따를지가 정해져 있지 않았다.
- **확인한 사실**: 배포 payload root 는 상수 `@('config', 'scripts', 'snippets', 'templates')` 이며(`scripts/lib/install-pipeline-core.ps1:68`), manifest·marker 검증이 이 상수와의 일치를 요구한다(`:946-955`, `:1012-1023`). **`rule_graph/` 는 이 목록에 없으므로 adopter 에게 가지 않는다.** 이것은 설계의 전제(*"adopter 는 배포 payload 만 받고 원소 트리는 받지 않는다"*)를 실측으로 확인한 것이기도 하다.
- **선택지**:
  - (a) `D-14` 를 따라 분리 — 합성은 `rule_graph/composites/` 에 두고 배포본에는 key 참조만 남긴다
  - (b) **배포 tier 한정으로 합병** — 쿡북 · 합성 선언 · 사본을 전부 `snippets/rules/<rule>.md` 안에 둔다
  - (c) `rule_graph/composites/` 를 배포 payload root 에 추가한다
  - (d) `D-14` 를 뒤집어 repo-only tier 도 합병한다
- **선택**: (b)
- **왜**: (a)는 설계가 명시적으로 금지한 상태를 만든다 — *"사본이 없으면 배포본이 깨진 참조 덩어리가 된다."* adopter 는 `[RC-XXXXX]` 를 보고도 그것이 무엇인지 알 방법이 없다(기준 1). *(이 자리에 처음에는 예시로 `RC-00057` 이라 썼는데, 그것이 key 형태라 V1 이 실제 참조로 집어내 dangling 을 냈다. 자릿수를 `XXXXX` 로 바꿨다 — 의미를 바꾸지 않는 표기 교정이며, 이 사건 자체는 `FINDINGS.md` `F-20` 에 등재했다.)* (c)는 payload root 상수를 바꾸는 일이고, 그러면 manifest·marker·테스트를 함께 고쳐야 하며 **원소 트리 전체가 adopter 에게 배포된다** — 설계가 배포하지 않기로 한 것을 배포하게 되어 복제(사본 병기)라는 장치의 존재 이유가 사라진다. (d)는 B1 실측(합성 선언 284행)이 배제한다 — 합치면 쿡북이 349행이 되어 *"일상 작업은 쿡북만 읽는다"* 가 정의상 불가능해진다.
- **되돌릴 수 있는가**: 예. 두 tier 의 배치는 서로 독립이다.
- **결과로 확정되는 것**: **쿡북·합성의 물리적 관계는 tier 마다 다르다.** repo-only tier 는 분리, 배포 tier 는 합병. `HANDOFF.md` 의 열린 항목 2 가 「하나의 답」이 아니라 「tier 별로 다른 답」으로 닫힌다. `FINDINGS.md` `F-11`

## D-18 — 배포 대상 원소의 정본은 영어로 쓴다

- **시점**: B2
- **막힌 지점**: `rule_graph/` 는 repo 내부 표면이라 human-facing prose 가 한국어가 기본이다. `snippets/rules/**` 는 *"불특정 adopter 가 직접 소비"* 하므로 영어를 유지한다. 배포 대상 원소는 **정본이 `rule_graph/` 에 있고 사본이 `snippets/rules/` 에 간다** — 두 규율이 같은 문장의 두 위치에 서로 다르게 걸린다.
- **선택지**:
  - (a) 정본 한국어 + 사본 영어 (각 위치의 언어 규율을 따른다)
  - (b) **정본·사본 모두 영어**
  - (c) 정본·사본 모두 한국어 (배포본의 영어 규율을 깬다)
  - (d) 정본에 한국어·영어를 병기한다
- **선택**: (b)
- **왜**: (a)가 **`V2` 를 원천적으로 불가능하게 만든다.** 설계는 *"복제를 허용하는 대가로 기계 검증이 필수가 된다. 검증이 없으면 정본과 사본은 반드시 어긋난다"* 고 했고, `V2` 는 *"합성에 기재된 사본이 원소 정본과 같은가"* 를 검사한다. 정본이 한국어이고 사본이 영어이면 **「같다」를 판정할 기계적 수단이 없다** — 번역 동치는 기계가 못 센다. 즉 (a)는 설계의 구성요소 하나를 제거한다(기준 1). (c)는 adopter 표면의 언어 규율을 깨고, (d)는 정본이 둘이 되어 불변식 5 를 흔든다.
- **되돌릴 수 있는가**: 예. 다만 되돌리면 `V2` 를 포기하는 것과 같다.
- **대가**: `rule_graph/elements/` 안에 **언어가 섞인 파일**이 생긴다 — 정본 문장은 영어, 그 주변의 출처·비고·class 설명은 한국어. 이것은 repo 의 자연어 저작 규율이 예상한 형태가 아니다. `FINDINGS.md` `F-12`

## D-19 — 배포본에는 key·조합 선언·사본만 넣고 추적 정보는 전부 뺀다

- **시점**: B2. 배포본에 무엇을 담을지 정하는 자리
- **막힌 지점**: `D-17` 로 합성 선언과 사본이 배포본 안에 들어가는 것이 정해졌다. 그런데 B1 의 합성 선언 형식에는 **출처 조각 id**(`RA-03` 등)와 **앵커**와 **실측 표**가 함께 있다. 이것들을 배포본에 그대로 옮길지가 정해져 있지 않았다.
- **확인한 제약**: 배포 tier 자신의 admission 조건이 이것을 직접 다룬다.
  - `snippets/rules/README.md:28` — *"(d) carries measurement or pilot traces — those stay in the rule's source repository, never in a distributed rule."*
  - `snippets/rules/README.md:34` — *"A distributed rule never depends on its source repository's surfaces (`rules/`, `rule_docs/`, `docs/`, the repo glossary) as a meaning source … its terms are defined in its own text or in surfaces shipped in the same distribution."*
  - `snippets/rules/README.md:23` — 1차 시험: *"could an adopter who knows nothing about the rule's source repository read and follow the rule?"*
- **선택지**:
  - (a) B1 의 합성 선언 형식을 그대로 배포본에 옮긴다
  - (b) **key · 조합 선언 · 원소 내용 사본만 넣고, 출처 조각 id · 앵커 · 실측 · `rule_graph/` 경로는 전부 뺀다**
  - (c) key 까지 빼고 사본만 넣는다 — 배포본을 완전히 자족적인 산문으로 만든다
  - (d) 배포본에 `rule_graph/` 로 가는 경로를 병기해 adopter 가 정본을 찾아갈 수 있게 한다
- **선택**: (b)
- **왜**: (a)와 (d)는 위 제약을 직접 위반한다 — (a)는 (d) 조항의 pilot trace 금지에, (d)는 L34 의 source-repo 표면 의존 금지에 걸린다. **`rule_graph/` 는 배포되지 않으므로 그 경로는 adopter 에게 해석 불가능한 포인터다.**

  (c)를 버린 것이 이 결정의 실질이다. key 를 빼면 **설계의 핵심 장치 하나가 배포 tier 에서 사라진다** — *"막힌 지점에 이름을 준다. 무엇이 막았는지가 식별자로 나온다."* adopter 가 배포 규칙에 막혔을 때 상류에 보고할 수 있는 것이 바로 그 key 다. key 는 adopter 에게 **의미 원천이 아니라 식별자**이고, 의미는 같은 파일 안의 사본이 준다. 따라서 L34 의 「meaning source 의존」에 걸리지 않는다(기준 1).
- **되돌릴 수 있는가**: 예.
- **남는 긴장**: key 는 adopter 에게 **해석할 수 없지만 인용할 수는 있는 토큰**이다. 이것이 L23 의 1차 시험(*"소스 repo 를 전혀 모르는 adopter 가 읽고 따를 수 있는가"*)을 통과하는지는 「따를 수 있는가」를 무엇으로 보느냐에 달렸다. 규범을 따르는 데는 지장이 없고, 정본을 확인하는 데는 지장이 있다. **이 판단은 이 작업이 단독으로 닫을 수 없다.** `FINDINGS.md` `F-13`

## D-20 — 방향 `정` / `역` 의 판정 기준을 명문화하고 `RA-15` 를 정정한다

- **시점**: B2 조각 판정 직후
- **막힌 지점**: B1 이 방향 축(`정` / `역`)을 도입하면서 **판정 기준을 문서화하지 않았다.** 그 결과 같은 형태의 두 조각에 반대 방향이 붙었다 — `RA-15`("필수 registry·scanner·load·의례를 만들지 않으며")는 `정`, `RA-31`("영구 rubric·registry·scanner·checklist 또는 작업 간 필수 load 를 만들지 않는다")은 `역`. B2 에서 이 불일치가 드러났고, 기준 없이는 B2 의 26개 조각을 판정할 수 없었다.
- **선택지**:
  - (a) 기준을 문서화하지 않고 사례별로 판정한다 (현상 유지)
  - (b) **막는 대상이 1차 작업이면 `정`, 지위·권위에 관한 2차 주장이면 `역`**
  - (c) 문면 형태로 가른다 — 부정문(`~하지 않는다`)이면 `역`, 명령문이면 `정`
  - (d) 방향 축을 버리고 `F-3` 의 우회를 철회한다
- **선택**: (b)
- **왜**: (b)가 기존 판정을 가장 많이 설명한다 — `RA-04` `RA-08` `RA-14` `RA-18` `RA-19` `RA-31` 여섯 건을 전부 설명하고, `RA-15` 하나만 설명하지 못한다. 설명되지 않는 하나가 **오판정**이라고 보는 것이 여섯을 재분류하는 것보다 싸다(기준 4). (c)는 `F-4` 가 이미 배격한 것과 같은 오류다 — **문면 형태로 판정하면 규범 내용이 아니라 어투를 재게 된다.** (d)는 `F-3` 이 관측한 것(차단을 막는 조각이 권위를 잃는다)이 되살아난다.
- **정정 내용**: `RA-15` → `역`. 따라서 `RC-00034` 의 방향도 `역`. B1 의 합성 역 건수가 6 → **7** 이 된다. `_work/rule-authority.extract.md` · `composites/rule-authority.md` · `INDEX.md` 세 곳을 고쳤고, 고쳤다는 사실을 각 자리에 남겼다.
- **근거**: 필수 registry·scanner·load 는 **그 자체가 차단 기제**다. 그것의 신설을 막는 것은 「차단할 수 있는 것을 만들지 못하게」 하는 것이므로 `역` 이다.
- **되돌릴 수 있는가**: 예. 방향은 INDEX 의 한 열이고 key 는 건드리지 않았다.
- **남는 것**: (b)도 전부를 가르지는 못한다. B2 의 `RCR-38`(owner 의 schema 복사 금지)은 **복사된 schema 가 게이트로 기능하는지**에 따라 방향이 갈리는데 문면이 그것을 말하지 않는다. 기준이 있어도 **문면이 답을 주지 않는 조각이 남는다.**

## D-21 — B2 는 한 절을 표본으로 완전 처리하고 배포 파일은 수정하지 않는다

- **시점**: B2. 배포본 전환을 실행하는 자리
- **막힌 지점**: 조각 판정 결과 이 파일은 합성 후보 26건 · 참조 슬롯 95개이고 **그중 64개(67%)가 문면에 대응 문장이 없어 저작이 필요**하다. 파일 전체를 설계대로 처리하면 **원소 64개를 새로 쓰는 일**이 되는데, 그것은 「기존 규칙을 분해한다」가 아니라 「새 규칙을 저작한다」다. 전체를 할지 말지가 정해져 있지 않았다.
- **선택지**:
  - (a) 파일 전체를 처리한다 — 원소 약 64개를 저작한다
  - (b) **한 절을 완전히 처리해 표본으로 삼고, 배포 파일은 수정하지 않는다**
  - (c) 표 계산만으로 배율을 추정하고 실제 전환은 하지 않는다
  - (d) B2 를 보류로 두고 B3 로 간다
- **선택**: (b). 절은 `Dependency containment` 를 골랐다
- **왜**: (c)를 버린 것이 핵심이다. **이 트랙의 선행 조사가 실패한 방식이 정확히 그것이었다** — 주장 36건 중 11건이 무효였고 원인이 「확인하지 않은 것을 확인된 것처럼 다룬 데」 있었다. 표 계산으로는 「사본이 원문보다 길어지는가」를 말할 수 없다. 실제로 써 봐야 중복이 몇 자인지 나온다(실제로 나왔다 — 사본의 38%가 중복이었고, 이것은 계산으로 예측되지 않았다).

  (a)를 버린 이유는 **저작 64건이 분해의 산출이 아니기 때문**이다. `item-03` 이 실패 조건으로 *"신규 저작 비율이 너무 높아 분해가 아니라 재작성이 된다"* 를 명시했고, 67%는 그 조건에 해당한다. 전체를 저작하면 그 실패를 실패가 아닌 것처럼 만든다.

  (d)는 아무 실측도 남기지 않는다. B2 의 물음(*"사본을 넣어도 배포본이 자립하는가"*)은 표본 하나로 답할 수 있다.

  절 선정 — `Dependency containment` 는 이 규칙의 **주 차단 트리거**를 담고, 이 파일에서 **정의된 용어 2개가 모두 있는 유일한 절**이다. 즉 **가장 유리한 조건**의 절이다. 여기서 배율이 2.71× 라면 다른 절은 그보다 높다.
- **배포 파일을 수정하지 않은 이유**: 8개 절 중 1개만 전환하면 파일이 반쪽 상태가 된다. 그 상태를 커밋하면 배포 payload 에 **일관성 없는 규칙**이 들어간다. 표본은 `_work/` 에 두고, 합성 4건의 INDEX 상태를 `표본(미반영)` 으로 표기했다.
- **되돌릴 수 있는가**: 예. 표본은 `_work/` 에 있고 배포 파일은 손대지 않았다.
- **범위 명시**: 전체 파일 배율 외삽(약 9300자)은 **표본 1개에서 나온 것이며 확인되지 않았다.** 그렇게 기록했다.

