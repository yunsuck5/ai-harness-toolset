# skills — system status

Current status for the deployed instruction-surface architecture: the always-loaded snippet payload (`snippets/CLAUDE_SNIPPET.md` / `snippets/AGENTS_SNIPPET.md`) plus on-demand function-level skills (`snippets/claude-skills/**`). The **design source** is `docs/systems/skills/FUNCTION_LEVEL_SKILL_ARCHITECTURE_PLAN.md` (design-stage plan; it approves no batch by itself). Routing: `docs/current/REPO_READING_GUIDE.md` (Q10). Review artifact/verdict contract: `docs/contracts/review/REVIEW_RESULT_CONTRACT.md`.

This subsystem owns the snippet↔skill responsibility split (what stays always-loaded vs what becomes an on-demand skill). It does **not** own install / update / uninstall (LTS — `docs/systems/install-update/**`, routes via Q1), or Brief (Q4).

## Current state

- **Two source function-level skills**, each discovered by its own `description` (the snippet carries no skill index or skill-routing pointer): `snippets/claude-skills/ai-harness-review/SKILL.md` (review-only; owns the full review lifecycle) and `snippets/claude-skills/ai-harness-brief/SKILL.md` (owns the manual Brief save / checkpoint / user-requested restore / update procedure). Review/Brief *procedure* is not restated in the snippet — it lives in the skills + the review contract, reached by pointer.
- **Snippet = a 2-H2 always-loaded bootstrap** (current shape — SK-06): `## Safety floor` (managed-block-only / no whole-file overwrite, explicit approval gates incl. verdict-approves-nothing, adoption destinations) + `## Operating rules and topology` (a rule trigger gate routing action-classes to rule *files* under `<ToolRoot>/snippets/rules/`, plus ToolRoot channel order + `log/` partitioning).
- **Two-tier rules architecture** backs the bootstrap: global-distribution `snippets/rules/*.md` (installed under the `snippets/` payload root; one rule group per file) + repo-only `<repo-root>/rules/*.md` (this repo's dev rules). The whole distribution (snippet, `snippets/rules/`, both skills, both templates) is **`docs/`-free** — `docs/` is not installed.
- **Snippets stay symmetric** except the intended vendor-specific loci (H1, intro tool-name, adoption destinations).
- **Maintenance posture:** the implemented batches are in the Completed ledger; only **Batch 4** (Not yet done) remains, as a separate scoped goal. The SK-06 hard-minimization model **supersedes** the earlier "always-loaded payload responsibility" enumeration, the GSF "complete with intentional residuals" objective-completion framing, and GSF-B3's "create no rules surface" decision (GSF-B3's loading model retained; no `.claude/rules/` / `.codex/rules/` / `@import`).

## Completed ledger

| ID | Item | Closed at | Current meaning | Detail |
|---|---|---|---|---|
| SK-00 | Function-level skill architecture plan | `936969f` | design-stage plan source; approves no batch | `docs/systems/skills/FUNCTION_LEVEL_SKILL_ARCHITECTURE_PLAN.md` |
| SK-01 | Batch 1 — snippet review-procedure de-duplication | `56de8d3` | snippet `## Review flow` reduced + `## Execution discipline` section removed; the review procedure is owned by the `ai-harness-review` skill + the review contract; the verdict quick reference stays as the single in-snippet home; no hard boundary / routing invariant dropped | plan §8 Batch 1; `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` |
| SK-02 | Batch 2 — restore-offer removal + manual brief-skill extraction + snippet minimization | `a75ffa4` (Batch 2 impl complete); 2D doc-closeout = this row's commit | unsolicited session-start restore-offer **removed** (discarded, not relocated) + BR-D-02 retired; the manual Brief save / checkpoint / restore / update procedure extracted to the `ai-harness-brief` skill; snippet BF-protocol section removed and skill-routing pointers dropped (discovery via each skill's `description`). The Batch 2 **implementation arc** also changed install / activation / uninstall + reviewer-guard scripts/tests (notably 2C-0 `832377e`, the §8A activation-surface lifecycle prerequisite) — so "no runtime/script change" applies only to the 2D doc-closeout, not to Batch 2 as a whole | plan §8 Batch 2; commit arc `4b7b58f` (2A/2B) → `a75ffa4`; key landings `52f2e1b` / `6a4bef5` / `832377e` / `68b177f` / `0d40f2d`; lifecycle prerequisite `docs/systems/install-update/GLOBAL_INSTALL_UPDATE_MODEL.md` §8A |
| SK-03 | Track D — instruction-surface global-snippet cleanup | this Track D commit | the two PowerShell-authoring rules (`.ps1` = UTF-8 BOM+CRLF; native-exe stdout/stderr/exit separation) **moved out** of the snippet to the repo-local root `CLAUDE.md` / `AGENTS.md` (Track C→D handoff; kept in snippet: commit/push approval + temp-file cleanup); `## Review flow` **renamed** `## Review record` (no-sidecar review-record invariant preserved); `## Role neutrality` reviewer/auditor + "any verdict" wording **aligned** byte-identical across both snippets. Declined (documented): the `.claude\AGENTS.md` Adoption↔Forbidden dedup and the verdict-meaning trim. Batch 3 ran separately (SK-05) | instruction-surface plan §14 Track D; `docs/architecture/instruction-surface/GLOBAL_SNIPPET_RELOCATION_AUDIT.md` §3.3 / §3.5 / §3.11 / §5; Track C→D handoff `docs/architecture/instruction-surface/REPO_LOCAL_INSTRUCTION_SURFACE_PLAN.md` §9 |
| SK-04 | GSF-B1 — Global Snippet First snippet minimization + direct owner-surface absorption | this GSF-B1 commit | `## Review record` **folded** into `## Project layout` (no-sidecar two-file record invariant + `REVIEW_RESULT_CONTRACT.md` pointer preserved), snippet **11 → 10 H2**, review-record-path duplication removed; all other non-deferred sections **proven keep** under keep-by-proof; six-point snippet-invariant check passed. Deferred to Batch 3 (since removed — SK-05): `## Brief` BF-lv3 non-claim and the no-auto-mirror Forbidden bullet | GSF-B1 spec `docs/architecture/instruction-surface/GLOBAL_SNIPPET_FIRST_MIGRATION_SPEC_GSF_B1.md`; classification record `docs/architecture/instruction-surface/GLOBAL_SNIPPET_FIRST_GSF_B1_CLASSIFICATION.md`; design/plan in the same folder |
| SK-05 | Batch 3 (= instruction-surface Track F) — non-current snippet-section removal | this Batch 3 commit | deleted the snippet `## Brief` (BF-lv3 non-claim) and other non-current sections plus the no-auto-mirror `## Forbidden` bullet; dropped the now-ungrounded `BRIEF semantics` item from the `## Role neutrality` reviewer-binding list (reviewer-mode Brief exclusion + operator-protocol scoping retained); snippet **10 → 8 H2**, both snippets symmetric. Kept (scope discipline): the `## Project layout` runtime-log separation line and **no** canonical-Brief path added to the snippet (restore-source pointer owned by `ai-harness-brief` / the brief spec — 현 `docs/brief/brief_spec.md`). Removed facts keep their non-deployed owners | skill plan §8 Batch 3 + §11.6 review criterion 6; instruction-surface §14 Track F; binding Track B audit `docs/architecture/instruction-surface/GLOBAL_SNIPPET_RELOCATION_AUDIT.md` §3.8–§3.10 / §5; owners(당시): 구 BRIEF_CONTRACT·구 brief DEFERRED — 현 `docs/brief/brief_spec.md`·`docs/brief/brief_backlog.md` |
| SK-06 | Global-snippet hard-minimization corrective — 2-H2 bootstrap + two-tier rules architecture + docs-free distribution | this corrective commit | snippet reduced **8 → 2 H2** (`## Safety floor` + `## Operating rules and topology`); critical safety floor inline, the rest pointer-routed to the distributed rules tier. **Two rules tiers created** (user-directed): global-distribution `snippets/rules/*.md` (installed under the `snippets/` payload root; `global-file-mutation-boundary` / `no-background-or-hidden-state` / `repository-change-safety`, one group/file) + repo-only `<repo-root>/rules/*.md` (`powershell-and-file-encoding`, migrated from the root files). Whole distribution made **`docs/`-free**; review/Brief procedures stay in the deployed skills. **Follow-up corrective:** `## Operating rules and topology` was later strengthened into a **rule trigger gate** (action-class → rule-file routing + non-substitution + stop-and-ask fail-safe) — routing only, no rule body copied, still 2 H2, both snippets mirror-edited. **Supersedes** the GSF "complete" framing and GSF-B3's "create no rules surface" decision (loading model retained; no `.claude/rules/` / `.codex/rules/` / `@import`) | per-item disposition `docs/architecture/instruction-surface/GLOBAL_SNIPPET_HARD_MINIMIZATION_CORRECTIVE.md` (§6 for the trigger-gate follow-up); superseded framing in `GLOBAL_SNIPPET_FIRST_MIGRATION_PLAN.md` §2 / `..._GSF_B3_RULES_LOADING_DECISION.md` §1 / `REPO_READING_GUIDE.md` Q10/Q11 |

## Not yet done

Each is a separate scoped goal + Codex review gate + explicit user approval (plan §8); none is approved by the plan or by this status file.

- **Batch 4** — review-polishing selective-capture vehicle decision (instruction vs skill; non-hook).

## Accepted residual risks

none. The Batch 1 `## Execution discipline` facts were resolved in-batch, not carried as residuals: the foreground→background auto-conversion reporting rule was **removed**; review-scope-integrity was **relocated** to `snippets/claude-skills/ai-harness-review/SKILL.md` (step 2); temporary-file cleanup was generalized and now lives in the rules tier `snippets/rules/repository-change-safety.md`. (Provenance: SK-01 + git history. Managed-block H2 count is not a frozen invariant — a function-specific section is removed once a function-level skill owns its procedure.)
