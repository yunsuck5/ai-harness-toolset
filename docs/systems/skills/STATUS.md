# skills — system status

Current status for the deployed instruction-surface architecture: the always-loaded snippet payload (`snippets/CLAUDE_SNIPPET.md` / `snippets/AGENTS_SNIPPET.md`) plus on-demand function-level skills (`snippets/claude-skills/**`). The **design source** is `docs/systems/skills/FUNCTION_LEVEL_SKILL_ARCHITECTURE_PLAN.md` (design-stage plan; it approves no batch by itself). Routing: `docs/current/REPO_READING_GUIDE.md` (Q10). Review artifact/verdict contract: `docs/contracts/review/REVIEW_RESULT_CONTRACT.md`.

This subsystem owns the snippet↔skill responsibility split (what stays always-loaded vs what becomes an on-demand skill). It does **not** own install / update / uninstall (LTS — `docs/systems/install-update/**`, routes via Q1), Brief (Q4), or Chatlog.

## Current state

- **Two source function-level skills:** `snippets/claude-skills/ai-harness-review/SKILL.md` (review-only; owns the full review lifecycle) and `snippets/claude-skills/ai-harness-brief/SKILL.md` (owns the manual Brief **save / checkpoint / user-requested restore / update** procedure, as of **Batch 2C-2**). Batch 2C-2 moved the procedure out of the snippet (the `## BF save / checkpoint protocol` section was **removed**); the **routing-pointer-removal batch** then dropped the skill-routing pointers entirely — `## Brief` carried only the BF-lv3 non-claim until **Batch 3** deleted it (along with `## Chatlog` and the `no Brief↔Chatlog mirror` Forbidden bullet — SK-05 below); the review-record hard boundary (no-sidecar two-file record) now lives in `## Project layout` after **GSF-B1** folded the former `## Review record` section there (SK-04 below), with **no** skill-routing pointer in the snippet. Each skill's `description` owns its own discovery.
- **Always-loaded payload responsibility (per the plan):** for current implemented capabilities only — hard boundaries / forbidden rules, adoption discipline, path/topology invariants, role neutrality + reviewer-mode exclusion, the verdict quick reference, and cross-task execution invariants. The snippet carries **no** skill-routing pointers, skill index, or *skill* trigger/routing table — skill discovery is owned by each skill's `description`. (The `## Operating rules and topology` **rule trigger gate** added by the SK-06 follow-up routes action classes to rule *files* under `<ToolRoot>/snippets/rules/`, not to skills.) Review *procedure* is not restated in the payload; it lives in the `ai-harness-review` skill + the review contract, referenced by pointer.
- **Batch 1 landed** (commit `56de8d3`) as the first implementation batch. **Batch 2 is fully landed and closed out** — 2A/2B (unsolicited session-start restore-offer removal + BR-D-02 retirement) + 2C-0 (generic mirror) + 2C-1 (skill skeleton) + 2C-2 (Brief/BF procedure extraction + snippet minimization) + the routing-pointer-removal follow-up (snippet skill-routing pointers dropped) all landed by `a75ffa4`, and the 2D closeout (this STATUS done-flip + SK-02 ledger + `REPO_READING_GUIDE.md` Q10 reconciliation) is recorded below. **Batch 3 (= Track F) has since landed** (SK-05 below); Batch 4 (plan §8) is not implemented. The plan approves no batch by itself; each is a separate scoped goal + review gate + explicit user approval.
- **Track D (instruction-surface global-snippet cleanup) landed** (SK-03 below): the two PowerShell-authoring rules left the snippet `## Other rules` for the repo-local root `CLAUDE.md` / `AGENTS.md` (the Track C→D handoff — they remain present in the root files; `docs/architecture/instruction-surface/REPO_LOCAL_INSTRUCTION_SURFACE_PLAN.md` §9); `## Review flow` was renamed `## Review record` (resolving the SK-02 title/content-mismatch follow-up, no-sidecar review-record invariant preserved); and the `## Role neutrality` reviewer/auditor wording was aligned byte-identical across both snippets. The snippets remain symmetric except the three intended tool-specific loci (intro tool-name, `## Adoption destination`, `## Adoption rules` "either"/"any of those"). **Batch 3 (Chatlog / BF-lv3 removal) was NOT part of Track D** — it ran separately, leaving `## Brief` + `## Chatlog` + the `no Brief↔Chatlog mirror` Forbidden bullet untouched by Track D (since removed by Batch 3 — SK-05 below).
- **GSF-B1 (Global Snippet First — global snippet minimization) landed** (SK-04 below): applying the GSF-B1 spec keep-by-proof, the `## Review record` section was **folded into `## Project layout`** (the no-sidecar two-file record invariant + the review-contract pointer preserved), reducing the snippet from **11 → 10 H2 sections** and removing the review-record-path duplication (single-home-plus-pointers). All other non-deferred sections were **proven keep**; `## Brief` (BF-lv3 non-claim), `## Chatlog`, and the `no Brief↔Chatlog mirror` Forbidden bullet were **deferred to Batch 3 / Track F** (classified `defer`, not re-owned, not edited) — **since removed by Batch 3 (SK-05 below)**. Binding per-item classification record: `docs/architecture/instruction-surface/GLOBAL_SNIPPET_FIRST_GSF_B1_CLASSIFICATION.md`.
- **Batch 3 (= instruction-surface Track F — non-current Brief/Chatlog snippet removal) landed** (SK-05 below): the `## Brief` (BF-lv3 non-claim) and `## Chatlog` sections were **deleted** and the `## Forbidden` `no Brief↔Chatlog mirror` bullet **removed**, taking the snippet **10 → 8 H2**; the `## Role neutrality` reviewer-binding list dropped the now-ungrounded `BRIEF semantics` item (the reviewer-mode Brief exclusion + operator-protocol scoping kept; `## Project layout` `log/chatlog/` separation line kept as global-keep topology). The removed facts keep their non-deployed owners: BF lv3 non-currency → `docs/contracts/brief/BRIEF_CONTRACT.md` BF Level table + `docs/systems/brief/DEFERRED.md`; Chatlog ≠ Brief / restore-source / reconstruction-evidence → `docs/contracts/chatlog/CHATLOG_CONTRACT.md` (+ `BRIEF_CONTRACT.md`); the no-auto-mirror boundary → the snippet's general no-daemon / no-auto-mutation Forbidden bullets + both contracts' non-goals.
- **Hard-minimization corrective landed** (SK-06 below): the global snippet was reduced from the **8-H2 compact policy bundle** to a **2-H2 always-loaded bootstrap** — `## Safety floor` (managed-block-only / no whole-file overwrite, explicit approval gates incl. verdict-approves-nothing, destinations incl. forbidden `%USERPROFILE%\.claude\AGENTS.md`) + `## Operating rules and topology` (pointer to the rules tier + ToolRoot channel order + `log/` partitioning). A **two-tier rules architecture** was introduced (user-directed): the **global-distribution rules tier `snippets/rules/*.md`** (ships under the `snippets/` payload root → installed to `<ToolRoot>/snippets/rules/`; one rule group per file: `global-file-mutation-boundary.md`, `no-background-or-hidden-state.md`, `repository-change-safety.md`) holds the reusable always-on rules not absorbed into a skill/template/script, and the **repo-only rules tier `<repo-root>/rules/*.md`** holds this repo's dev rules (`powershell-and-file-encoding.md`, migrated out of the root files). The review / Brief procedures + verdict consumption + operator/reviewer stance + role binding + ToolRoot-resolution detail stay in the deployed `ai-harness-review` / `ai-harness-brief` skills (discovered by description; the bootstrap does **not** name or route to them). The whole distribution (snippet, `snippets/rules/`, skills, templates) was made **`docs/`-free** — `docs/` is not installed. This **supersedes** the prior "Always-loaded payload responsibility" enumeration above, the GSF "complete with intentional residuals" objective-completion framing, **and GSF-B3's "create no rules surface" decision** (the user directed the rules tiers; GSF-B3's loading model is retained; no `.claude/rules/` / `.codex/rules/` / `@import`). Per-item disposition record: `docs/architecture/instruction-surface/GLOBAL_SNIPPET_HARD_MINIMIZATION_CORRECTIVE.md`.

## Completed ledger

| ID | Item | Closed at | Current meaning | Detail |
|---|---|---|---|---|
| SK-00 | Function-level skill architecture plan | `936969f` | design-stage plan source; approves no batch | `docs/systems/skills/FUNCTION_LEVEL_SKILL_ARCHITECTURE_PLAN.md` |
| SK-01 | Batch 1 — snippet review-procedure de-duplication | `56de8d3` | `snippets/CLAUDE_SNIPPET.md` / `AGENTS_SNIPPET.md` `## Review flow` reduced (Batch 1 left a routing pointer to the `ai-harness-review` skill + the review contract; the later routing-pointer-removal batch removed that pointer, leaving only the review-record hard boundary), and the `## Execution discipline` section **removed entirely** (launch discipline owned by the skill step 5, review-scope-integrity relocated to the skill step 2, temporary-file cleanup generalized to `## Other rules`); the verdict quick reference (`## Result verdict vocabulary`) is retained as the single in-snippet home; no hard boundary / routing invariant dropped (review still fully specified via skill + contract) | plan §8 Batch 1; `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` |
| SK-02 | Batch 2 — restore-offer removal + manual brief-skill extraction + snippet minimization | `a75ffa4` (Batch 2 impl complete); 2D doc-closeout = this row's commit | unsolicited session-start restore-offer **removed** (discarded, not relocated) and the BF-lv3 restore-offer component (BR-D-02) retired; the manual Brief **save / checkpoint / user-requested restore / update** procedure extracted to the `ai-harness-brief` skill; snippet `## BF save / checkpoint protocol` section removed, `## Brief` reduced to the BF-lv3 non-claim (later removed entirely by Batch 3 — SK-05), and skill-routing pointers dropped (discovery via each skill's `description`); remaining BF lv3 components stay deferred. **Behavior-change scope:** this 2D closeout round is doc-only, but the Batch 2 *implementation arc* did change install / activation / uninstall + reviewer-guard scripts and tests (notably 2C-0 `832377e` generic activation-surface enumeration, the §8A lifecycle prerequisite; also `52f2e1b`, `68b177f`) — so "no runtime/script change" is scoped to the 2D closeout, not to Batch 2 as a whole. Out of scope (separate user-directed follow-up): post-batch snippet cleanup, incl. the `## Review flow` title/content mismatch | plan §8 Batch 2; commit arc `4b7b58f` (2A/2B) → `a75ffa4` — key landings: `4b7b58f` 2A/2B + BR-D-02 retire, `52f2e1b` reviewer-mode restore guard, `6a4bef5` §8A activation-surface policy, `832377e` 2C-0, `68b177f` 2C-1, `0d40f2d` 2C-2, `a75ffa4` routing-pointer-removal; lifecycle prerequisite `docs/systems/install-update/GLOBAL_INSTALL_UPDATE_MODEL.md` §8A |
| SK-03 | Track D — instruction-surface global-snippet cleanup | this Track D commit | enabled by Track C (root `CLAUDE.md`/`AGENTS.md` now carry the PowerShell rules): the two PowerShell-authoring rules (`.ps1` = UTF-8 BOM+CRLF; native-exe stdout/stderr/exit separation) **removed** from snippet `## Other rules` (kept: commit/push approval + temp-file cleanup); `## Review flow` **renamed** `## Review record` (resolving the SK-02 title/content-mismatch follow-up; no-sidecar review-record hard boundary preserved); `## Role neutrality` reviewer/auditor + "any verdict" wording **aligned** byte-identical across both snippets. Snippets remain symmetric except the three intended tool-specific loci (intro tool-name, `## Adoption destination`, `## Adoption rules` "either"/"any of those"). **Declined (documented):** the `.claude\AGENTS.md` Adoption-destination↔Forbidden dedup (would weaken a hard boundary) and the verdict-meaning trim (`yes with risk` ≠ `yes` is a reviewer-critical always-on boundary). **Batch 3 (Brief/Chatlog + the `no Brief↔Chatlog mirror` bullet) NOT absorbed by Track D** — it ran separately and has since landed (SK-05). | instruction-surface plan §14 Track D; `docs/architecture/instruction-surface/GLOBAL_SNIPPET_RELOCATION_AUDIT.md` §3.3 / §3.5 / §3.11 / §5; Track C→D handoff `docs/architecture/instruction-surface/REPO_LOCAL_INSTRUCTION_SURFACE_PLAN.md` §9 |
| SK-04 | GSF-B1 — Global Snippet First snippet minimization + direct owner-surface absorption | this GSF-B1 commit | applying the committed GSF-B1 spec: `## Review record` **folded** into `## Project layout` (no-sidecar two-file record invariant + `REVIEW_RESULT_CONTRACT.md` pointer preserved), snippet **11 → 10 H2**, review-record-path duplication removed; all other non-deferred sections **proven keep** under keep-by-proof (procedures already extracted by Batch 1/2/Track D; remaining are adopter-universal invariants with no better deployed owner — deployment boundary + reviewer-mode reachability); the six-point snippet-invariant check passed (no hard boundary / adoption / topology / role / verdict invariant dropped; CLAUDE/AGENTS symmetry preserved). **Deferred by GSF-B1 (not re-owned / not renumbered), and since removed by Batch 3 (SK-05):** `## Brief` BF-lv3 non-claim, `## Chatlog`, the `no Brief↔Chatlog mirror` Forbidden bullet → **skill-plan Batch 3 / Track F**. No `rules/` surface / hook / docs deletion (stop/report boundaries: none hit). | GSF-B1 spec `docs/architecture/instruction-surface/GLOBAL_SNIPPET_FIRST_MIGRATION_SPEC_GSF_B1.md`; classification record `docs/architecture/instruction-surface/GLOBAL_SNIPPET_FIRST_GSF_B1_CLASSIFICATION.md`; design/plan in the same folder |
| SK-06 | Global-snippet hard-minimization corrective — 2-H2 bootstrap + two-tier rules architecture + docs-free distribution | this corrective commit | snippet reduced **8 → 2 H2** (`## Safety floor` + `## Operating rules and topology`); critical safety floor inline, the rest pointer-routed to the distributed rules tier. **Two rules tiers created** (user-directed): global-distribution `snippets/rules/*.md` (under the `snippets/` payload root → installed; `README` + `global-file-mutation-boundary` + `no-background-or-hidden-state` + `repository-change-safety`, one group/file) and repo-only `<repo-root>/rules/*.md` (`README` + `powershell-and-file-encoding`, migrated from the root files). Review / Brief procedures stay in the deployed skills (discovered by description; bootstrap does not name/route to them). **Whole distribution made `docs/`-free** (snippet, `snippets/rules/`, both SKILL.md, both review templates). Snippets symmetric except the intended vendor-specific loci (H1, intro tool-name, Destinations bullet). **Supersedes** the GSF "complete with intentional residuals" objective-completion framing **and GSF-B3's "create no rules surface" decision** (loading model retained; no `.claude/rules/` / `.codex/rules/` / `@import`). No runtime / install / update / uninstall / skill-procedure / ToolRoot / hook / memory / global-file change; **no payload-root change** (`snippets/rules/` works because `snippets/` is already a payload root); no docs deletion; stop/report boundaries: none hit. **Follow-up corrective:** `## Operating rules and topology` was later strengthened from a passive pointer into a **rule trigger gate** (action-class → rule-file routing map + non-substitution clauses + stop-and-ask fail-safe) to fix a trigger-compliance failure — routing only, **no rule body copied**, still 2 H2, no hook (future candidate); both snippets mirror-edited (trigger section byte-identical). Detail: `GLOBAL_SNIPPET_HARD_MINIMIZATION_CORRECTIVE.md` §6. | per-item disposition `docs/architecture/instruction-surface/GLOBAL_SNIPPET_HARD_MINIMIZATION_CORRECTIVE.md`; superseded framing in `GLOBAL_SNIPPET_FIRST_MIGRATION_PLAN.md` §2 / `..._GSF_B3_RULES_LOADING_DECISION.md` §1 / `REPO_READING_GUIDE.md` Q10/Q11 |
| SK-05 | Batch 3 (= instruction-surface Track F) — non-current Brief/Chatlog snippet removal | this Batch 3 commit | deleted snippet `## Brief` (BF-lv3 non-claim) + `## Chatlog` sections and the `## Forbidden` `no Brief↔Chatlog mirror` bullet; dropped the now-ungrounded `BRIEF semantics` item from the `## Role neutrality` reviewer-binding list (reviewer-mode Brief exclusion + operator-protocol scoping retained); snippet **10 → 8 H2**, both snippets symmetric (shared body byte-identical, only the three intended tool-specific loci differ). **Kept (scope discipline):** the `## Project layout` `log/chatlog/` separation line (global-keep topology, parallels `log/evidence/`; plan §8 acceptance = no Chatlog *section*) and **no** canonical-Brief-path added to the snippet (binding audit §5 — the restore-source pointer is owned by `ai-harness-brief` / `BRIEF_CONTRACT.md`; snippet-keeping optional). **Mirror bullet dropped, not generically replaced** — the general no-daemon / no-auto-mutation Forbidden bullets + `BRIEF_CONTRACT.md` / `CHATLOG_CONTRACT.md` non-goals already own the no-auto-mirror boundary (audit §5 'drop if covered'). No runtime / install / skill-procedure / ToolRoot / hook / memory / global-file change; no docs deletion; stop/report boundaries: none hit. | skill plan §8 Batch 3 + §11.6 review criterion 6; instruction-surface §14 Track F; binding Track B audit `docs/architecture/instruction-surface/GLOBAL_SNIPPET_RELOCATION_AUDIT.md` §3.8–§3.10 / §5; owners `docs/contracts/brief/BRIEF_CONTRACT.md`, `docs/systems/brief/DEFERRED.md`, `docs/contracts/chatlog/CHATLOG_CONTRACT.md` |

## Not yet done

Each is a separate scoped goal + Codex review gate + explicit user approval (plan §8); none is approved by the plan or by this status file.

- **Batch 4** — review-polishing selective-capture vehicle decision (instruction vs skill; non-hook).

## Accepted residual risks

none — the Batch 1 `## Execution discipline` facts were resolved in-batch, not carried as a residual:

- The foreground→background **auto-conversion** reporting rule was **removed** (not relocated).
- The **review-scope-integrity** rule ("do not shrink a review's scope to avoid a long-running run") was **relocated** into `snippets/claude-skills/ai-harness-review/SKILL.md` (step 2, review scope integrity discipline).
- **Temporary-file cleanup** is a general operator-hygiene rule; it is not review-specific. (Originally generalized into the snippet's `## Other rules`; the hard-minimization corrective (SK-06) moved it to the global-distribution rules tier `snippets/rules/repository-change-safety.md` when `## Other rules` was removed.)

The snippet's `## Execution discipline` section was therefore **removed entirely** (the review-run launch discipline is owned by `ai-harness-review` SKILL step 5). Managed-block section structure / H2 count is not a frozen invariant — a function-specific procedure section is removed once its procedure is owned by a function-level skill.
