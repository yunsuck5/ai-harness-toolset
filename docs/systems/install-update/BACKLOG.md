# install-update — backlog (open candidates)

Open, not-yet-started install / update / operational candidates. **This file is the open-work entrypoint for the install-update subsystem** — each row (ID + candidate + direction note) is the triage-level entry, sufficient to scope a future goal. The original full analysis is preserved as historical provenance in `docs/archive/backlog/operations.md` (not a current dependency). None is approved for implementation; each needs a separate scoped goal + review. The parallel "operations backlog track" relationship to `docs/decisions/POST_MVP_PLAN.md` §11 is noted in `docs/roadmap/CURRENT_MILESTONES.md`.

> A row marked **[RETIRED]** is kept for provenance but is **no longer an active/open post-MVP candidate** and is not handled in the LTS version — reopening it requires a **separate explicit reopen decision** (new scoped `/goal` + Codex review gate + explicit user decision).

| ID | Open candidate | Direction (triage) | Historical provenance |
|---|---|---|---|
| IU-B-01 | Smoke evidence preservation | scope-defined; runbook-only / helper-script / archive-manifest choice deferred to a scoped goal | `docs/archive/backlog/operations.md` |
| IU-B-02 | Project-local copy model docs vs global stable runtime ToolRoot model | docs-debt candidate (legacy channel-5 vs current channel-3 wording reconciliation) | `docs/archive/backlog/operations.md` |
| IU-B-03 | Path normalization edge-case hardening (cross-cutting) | `scripts/lib/path.ps1` edge-case hardening; used by install + review | `docs/archive/backlog/operations.md` |
| IU-B-04 | Install validation report evidence hygiene | strongly-preserved polishing backlog; separate PASS verdict from anomalous wrapper signal in closeout reports | `docs/archive/backlog/operations.md` |
| IU-B-05 | Snapshot auxiliary evidence exactness wording | wording-accuracy polishing for snapshot / evidence reports | `docs/archive/backlog/operations.md` |
| IU-B-06 | Long-lived docs commit hash hygiene (cross-cutting) | low-priority doc hygiene; keep literal commit hashes out of long-lived docs; spans roadmap + install docs | `docs/archive/backlog/operations.md` |
| IU-B-07 **[RETIRED]** | One-shot natural-language update completion / safe activation auto-apply | **RETIRED — removed from the post-MVP work list (2026-05-31); not handled in the LTS version.** No longer an active/open post-MVP candidate and no longer a "post-LTS future candidate". Background (unchanged, historical): Phase 4a completed only the activation-apply substrate / coverage alignment (3-surface shared resolver + apply orchestration); the original Phase 4 objective ("자연어 업데이트 지시 한 번으로 추가 yes/no 없이 activation apply 까지 완결") was **never implemented**. **Current intended behavior is retained** — update runs via `update-global.ps1` → `install-update.ps1 -Mode update-source`, and activation apply stays a **separate explicit approval step** (a natural-language update does NOT auto-perform any global/user activation mutation). This is a **retirement, not a completion** (Phase 4b is NOT implemented), and it makes **no** claim that activation auto-apply is feasible or safe. **Reopen requires a separate explicit reopen decision** (new scoped `/goal` + Codex review gate + explicit user decision); until then it is out of scope. idea-only detail: `docs/systems/install-update/IDEAS.md` 항목 1 (also marked retired). | `docs/systems/install-update/IDEAS.md` 항목 1 |
| IU-B-08 | Uninstall / teardown lifecycle | **Implementation CLOSED (batches 1–3) + isolated-machine dogfood CLEARED + docs closeout DONE.** `Remove-ManagedBlock` primitive (`2fe5328`), read-only dry-run target resolver (`d43f784`), and destructive `-Apply` + temp finalizer (`fc1c6a7`) are all implemented + tested + Codex-review-closed; decisions O1–O7 are resolved (O6 = project-scope teardown deliberately NOT adopted). As-built reference: `docs/systems/install-update/UNINSTALL_LIFECYCLE_DESIGN.md` (§13 records the dogfood); status detail in `STATUS.md`. **Done since last triage:** (a) **real-environment dogfood CLEARED** — an isolated non-primary machine `-Apply` (`uninstall_preview` blocked=0/warn=0/wouldRemove=9 → `-Apply` exit 0 → finalizer `status=uninstalled`) + footprint-zero verification + clean reinstall (`verify_pass`); the primary work machine was **not** mutated and a real primary-machine `-Apply` remains a separate explicit boundary not performed; the dogfood surfaced a documented boundary (post-uninstall reinstall is a first-time managed-block INSERTION, a separate explicit approval — see `UNINSTALL_LIFECYCLE_DESIGN.md` §13.2). **That first-time-insertion tooling boundary is now CLOSED by IU-B-09** (deterministic `apply-managed-block.ps1 -Insert` / `install-global.ps1`). (b) **uninstall docs closeout DONE** (this sync: STATUS / design §13 / INSTALL.md §11(b) / this row). (c) **LTS readiness — CLOSED for the install/update lifecycle subsystem** (HEAD `69ef433`; confirmatory full Pester 440/440): the subsystem entered LTS maintenance — see `STATUS.md` "install/update lifecycle — LTS readiness (subsystem closeout)". This is the install/update subsystem Step 6 / post-MVP closeout decision only — NOT a whole-project LTS, NOT Phase 4b completion, NOT a primary-/main-work-machine apply claim. **Remaining for IU-B-08:** none. | `docs/systems/install-update/UNINSTALL_LIFECYCLE_DESIGN.md` |
| IU-B-09 | Fresh-install regression closeout / lifecycle entrypoint split | **Implementation CLOSED + docs closeout DONE (this batch).** Restores the deterministic fresh-install bootstrap path that drifted during update polishing (it is **not** a new installer framework). Adds **`scripts/install-global.ps1`** (fresh global install: canonical pipeline materialize + activation bootstrap via first-time managed-block insertion + skill mirror create, closed by `install-update.ps1 -Mode verify` = `verify_pass`; existing-install fail-fast, no overwrite option), **`scripts/update-global.ps1`** (operator-facing existing-install update thin wrapper → delegates to `install-update.ps1 -Mode update-source`; missing/invalid area → fail-fast to `install-global.ps1`), and a **first-time managed-block insertion primitive** (`Add-ManagedBlock` + `apply-managed-block.ps1 -Insert`; absent→create / 0-pair→append preserving outside content / 1-pair→fail-fast; replace-only default mode unchanged). `install-update.ps1` behavior unchanged (smoke helper extracted to `scripts/lib/operational-smoke.ps1` for reuse). Closes the IU-B-08 discovered boundary (post-uninstall 0-pair reinstall first-time insertion now deterministic). No real `%USERPROFILE%` mutation — all destructive paths exercised against TestDrive/temp fixtures. Tests: `managed-block` / `apply-managed-block` / `install-global` / `update-global` `.Tests.ps1` (full Pester 440/440, incl. two corrective-review regression tests). Codified in `INSTALL.md` §6.1 / §7.1.1 / §10 / §11 (b); detail in `STATUS.md`. **Follow-up IU-B-09.1 (docs/template only):** Session-A natural-language update succeeded but routed through `install-update.ps1` (not `update-global.ps1`) because the installed root README + `INSTALL.md` §7.1 quickstart still exposed `install-update.ps1` as primary; IU-B-09.1 makes `update-global.ps1` the operator-facing primary in `templates/install-root/AI_HARNESS_TOOLSET_ROOT_README.md` + `INSTALL.md` (§7.1/§7.2/§7.3), demoting `install-update.ps1 -Mode update-source` to underlying/compat (no code/logic change; snippets untouched). **Notebook dogfood (natural-language, isolated non-primary machine):** update + uninstall + clean-install natural-language **routing** are all dogfood-cleared (Sessions A–C + Session A-retest; see `STATUS.md`). The earlier `update-global.ps1` routing PENDING (Session-A updates `4b4e9dc`→`dc1bf7e`→`f8a935c` ran via `install-update.ps1` because the payload predated `update-global.ps1` / the IU-B-09.1 polish) is now **CLEARED** by the Session A-retest: a natural-language update from the `f8a935c0` payload to latest HEAD `66eee572` was routed through the primary `scripts/update-global.ps1` (→ delegated to underlying `install-update.ps1 -Mode update-source`), `complete` + `verify_pass`, 3 surfaces byte-identical. **Remaining:** none for the routing dogfood. The install/update lifecycle subsystem **LTS readiness is now CLOSED** (IU-B-08 (c) above / `STATUS.md` LTS closeout, HEAD `69ef433`, Pester 440/440) — the subsystem is in LTS maintenance. Phase 4b / **IU-B-07 is RETIRED** (removed from the post-MVP work list; not implemented; reopen only by explicit decision). | `docs/systems/install-update/STATUS.md` (IU-B-09 bullet) |

| IU-B-10 | Uninstall package-discovery docs hardening | **Implementation CLOSED (docs/template/test only; no code/behavior change; LTS-maintenance scoped goal).** A **primary-work-machine** uninstall incident ran as a **manual partial uninstall** (operator saw only the install-root top level, did not descend into `current\scripts\` where `uninstall-global.ps1` lives, did not read installed `README.md`/`INSTALL.md`, hand-cleaned, and missed the Codex `%USERPROFILE%\.codex\AGENTS.md` managed block — a stale marker span later removed by a corrective cleanup). **Root cause = installed-package-hierarchy discovery failure — NOT a snippets/global-instruction deficiency and NOT a code bug** (the official `scripts/uninstall-global.ps1` already removes both managed-block surfaces incl. the Codex effective surface; §13 isolated-machine dogfood cleared). Explicitly a **manual partial uninstall incident, NOT an official uninstall dogfood.** Fix: installed root README template (`templates/install-root/AI_HARNESS_TOOLSET_ROOT_README.md`) gains an "Uninstalling this install" section (discovery of `current\scripts\uninstall-global.ps1`, dry-run→`-Apply`, footprint-zero, Codex target set incl. `%CODEX_HOME%\AGENTS.md` + `AGENTS.override.md` precedence, block-only `AGENTS.md`→0-byte normal, manual-cleanup-is-fallback-not-dogfood); `INSTALL.md` §7.3 records README uninstall discovery; design doc §14 records the incident + classification; tests lock README/contract wording + snippets-untouched guard. **Snippets intentionally NOT modified.** No code/behavior change; no real install/update/uninstall executed. **Remaining:** none. | `docs/systems/install-update/UNINSTALL_LIFECYCLE_DESIGN.md` (§14) |

| IU-B-12 | Install bootstrap clone cleanup enforcement | **Implementation CLOSED (docs/template/test only; no code/behavior change; LTS-maintenance scoped goal).** A **primary-work-machine clean-install retest** (`...git 설치해줘`) succeeded via `install-global.ps1 -RepoUrl` (`installed` / `verify_pass` / `smoke=pass`, **script-internal run-scoped work-area cleanup OK**) but the **operator-created bootstrap/source clone** (`%USERPROFILE%` 아래 처음 만든 clone) was left behind and the operator asked "정리할까요?" on the success path; an honest follow-up confirmed the guide says it should auto-remove and that not deleting was **habitual over-cautiousness, not guide-based**. **Root cause = operator-created bootstrap clone cleanup *discipline* failure on the fresh-install path — NOT a script run-scoped work-area cleanup failure and NOT a code bug** (the script self-cleans its own work area and has no knowledge of the externally-created bootstrap clone). Gap: the "Operator bootstrap clone cleanup 규칙" existed only in `INSTALL.md` §7.1 (update flow); §6.1 (fresh install) had no equivalent. Fix: `INSTALL.md` §6.1 gains a parallel **"Operator bootstrap clone cleanup 규칙 (fresh install)"** (auto-delete on `installed`/`verify_pass`/smoke-pass success, **don't ask** — "정리할까요?" on success = contract violation; three exceptions = cleanup-failure→exact leftover path+reason / investigation-needed / evidence-preserve-needed; cleanup-failure = **success with cleanup leftover, not full lifecycle closeout**; operator-workflow rule, **not mechanized**); §3.2 + §2A phase 5 cross-reference it; installed root README "Bootstrap clone cleanup" note **generalized to cover install**; tests lock the §6.1 + README wording. **Snippets intentionally NOT modified. No code change** to `install-global.ps1` (operator-workflow discipline the contract deliberately does not mechanize). No real install/update/uninstall executed. **Remaining:** none. | `docs/systems/install-update/STATUS.md` (IU-B-12 bullet) |

Closed install/update/operational items are recorded in `docs/systems/install-update/STATUS.md` — the main milestones in the **Completed ledger** (IU-01..IU-12, incl. Aggregate digest reproducibility = IU-11) and the operations-backlog closeouts in the **Operational closeout ledger** (IU-OPS-01 PowerShell smoke invocation quoting hardening (W, `c183c6b`), IU-OPS-02 Managed block marker detection, IU-OPS-03 Global instruction file path semantics, IU-OPS-04 Channel 3 smoke validation closeout, IU-OPS-05 Activation managed-block apply tooling hardening). "Brief / Chatlog location reconciliation" is a brief-system closeout (`docs/systems/brief/STATUS.md`). All closed-item full detail is preserved in `docs/archive/backlog/operations.md` as historical record. Deferred items with reopen conditions: `docs/systems/install-update/DEFERRED.md`.
