# review — system status

Current status for the Codex review subsystem. The **canonical artifact contract** is `docs/contracts/review/REVIEW_RESULT_CONTRACT.md`; reviewer **config** is `docs/policies/REVIEWER_CONFIG_POLICY.md`; effort/cost operating guidance is `docs/policies/REVIEW_EFFORT_GUIDE.md`. Routing: `docs/current/SOURCE_OF_TRUTH.md` (Q2, Q3).

## Current state

- **Maintenance mode.** No new feature / reviewer adapter / multi-reviewer orchestration / review-history DB / cross-run aggregation / automatic retention without a separate scoped approval. Bug fix and contract clarification are in-scope.
- **Review System LTS — closed.** LTS Phase 1 (3 batches; `e6027ca`), Phase 2 Batch 1A (Counter-argument H2 Option B; `76033f4`), and IDEAS.md governance (devil's advocate + multi-reviewer idea-only durability mitigation; `37a895c`) all closed. Subsequent work proceeds under maintenance mode only — no broad redesign without separate scoped approval.
- **Canonical task/perspective/pass topology** is the current shape (strict C1): one Codex attempt per `pass-NN`, under one **required** `<perspective>` directory, under one `<review-task-id>` directory — `<ProjectRoot>/log/review/<review-task-id>/<perspective>/pass-NN/input.md` (AI-authored) + `result.md` (dual-authored: reviewer-adapter verdict/disclosure body + a runner-appended `## Reviewer run provenance` block — RV-B-06 P3; `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3). `-Perspective` is **required** (the scripts fail fast without it; no two-level fallback); legacy two-level `<review-task-id>/pass-NN/` records from before strict C1 are manual-readable runtime artifacts only (not tool-supported, not migrated, not deleted). See the S6 strict-C1 bullet under Open / historical (Completed-ledger RV-B-08). No sidecar JSON, no hash-binding files, no external staging, no flat run-id layout.
- **Operator entrypoint:** `scripts/review-prepare.ps1` → `scripts/review-run.ps1` (one Codex execution per call), with `scripts/review-verify.ps1` as the post-hoc canonical-artifact check and `scripts/review-input-verify.ps1` as the input gate. Natural-language UX: `docs/user_guide/OPERATOR_GUIDE_KR.md` §7 + `snippets/claude-skills/ai-harness-review/SKILL.md`.
- Verdict (`yes` / `no` / `yes with risk`) never auto-approves commit / push / publish / merge / release.

## Completed ledger

| ID | Item | Closed at | Current meaning | Detail |
|---|---|---|---|---|
| RV-01 | Codex review subsystem operational; entered maintenance mode | — | review flow stable; maintenance scope only | `docs/decisions/POST_MVP_PLAN.md` §2 |
| RV-02 | Canonical review task/pass topology — contract alignment | `a5d94a5` | source-of-truth wording aligned to task/pass shape | `docs/decisions/POST_MVP_PLAN.md` §10 |
| RV-03 | Canonical review task/pass topology — implementation | `c81fe45` | script/template/I-O shape aligned to RV-02; removed-legacy sidecar artifacts dropped from normal path | `docs/decisions/POST_MVP_PLAN.md` §10 |
| RV-04 | Review effort/cost operating guide added | — | advisory only; introduces no automatic gate | `docs/policies/REVIEW_EFFORT_GUIDE.md` |
| RV-05 | Review System LTS Phase 1 (Batch 0/I/II) closed | `e6027ca` | V2 mirror cleanup + verdict→next-action mapping (`§6a`) + §5a operator stance + light P3 wording in H1 preamble; all source mutation + propagation 4 surfaces (channel 3 / CLAUDE.md / Claude skill / AGENTS.md) resolved | `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §5a / §6a + `scripts/review-run.ps1` H1 preamble; commit `e6027ca` |
| RV-06 | Phase 2 Batch 1A — Counter-argument H2 Option B codification | `76033f4` | strongly-recommended (non-parser) `## Counter-argument` H2 added to result.md convention (`REVIEW_RESULT_CONTRACT.md §3c`); 4 source surfaces (contract + result template + input template + SKILL); parser surface unchanged; H1 preamble runtime instruction is aligned with §3c by Stage 4-R1 (strongest-case-against directs to `## Counter-argument`); Batch 1B mirror surfaces deliberately separate (see RV-B-05) | `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3c; commit `76033f4` |
| RV-07 | IDEAS.md governance — idea-only durability mitigation | `37a895c` | `docs/systems/review/IDEAS.md` codifies devil's advocate pre-pass and multi-reviewer/calibration loop as idea-only items (not implementation backlog, not deferred implementation scope); reopen criteria documented in source | IDEAS.md (`docs/systems/review/IDEAS.md`) |
| RV-B-06 | Reviewer runtime provenance in the result artifact (P1–P4) | `cfac1ed` | `result.md` is self-describing via a runner-appended `## Reviewer run provenance` block: P2 adapter kind/version H1 stdout run-facts → P3 result.md provenance persistence + contract dual-authorship → P4 operator surfacing + single-author mirror-label reconciliation. Adapter-neutral; no parser-gate change; no sidecar; no version pin. Reviewed P2 `review-result-provenance-p2-2026-06-03` / P3 `review-result-provenance-p3-2026-06-03` / P4 `review-result-provenance-p4-2026-06-03`. Activation/global update not yet performed (separate pending action). | `docs/systems/review/REVIEW_RESULT_PROVENANCE_SPEC.md`; commits `77691c2`(P1)/`fdde410`(P2)/`fbd295e`(P3)/`cfac1ed`(P4) |
| RV-B-07 | U9 config-backed category-effort policy | — | config-backed `category → {model, reasoningEffort}` lookup built (closes the U9 traceability gap); `config/reviewer.json` 12 generic categories at the safe floor + `review-run.ps1 -EffortCategory` resolver + AC-RR29–39; mechanism only, no gate change; per-category value tuning deferred to operational data; global activation a separate pending action | `docs/systems/review/REVIEW_POLISHING_DECISION_RECORD.md` U9 + the U9 bullet under Open / historical; git history |
| RV-B-08 | Review artifact perspective layout — strict C1 canonical | `460ee3e` | three-level `log/review/<review-task-id>/<perspective>/pass-NN/`, `-Perspective` required (no two-level fallback); global channel-3 + managed-block + deployed-skill adoption complete | `docs/systems/review/REVIEW_ARTIFACT_PERSPECTIVE_LAYOUT_PLAN.md` + the S6 strict-C1 bullet under Open / historical; git history |

## Open / historical

- Open review-subsystem backlog candidates are consolidated in `docs/systems/review/BACKLOG.md` (open: RV-B-01..RV-B-05; closed/tombstoned: RV-B-06, RV-B-07, RV-B-08 — see the Completed ledger plus the polishing-cycle / S6-C1 closeout bullets below and their BACKLOG tombstones); their full historical text and the removed-legacy items are preserved in git history (classification index: `docs/backlog/INDEX.md`). BACKLOG.md is the source-of-truth for the open set and supersedes this range when they diverge. Removed-legacy review-cycle / `meta.json` / `result.json` / `target-files.list` / `<run-id>` identifiers are historical reason only, never operator paths.
- Idea-only items (devil's advocate pre-pass orchestration, multi-reviewer consensus / post-merge calibration loop) are codified in `docs/systems/review/IDEAS.md`. They are NOT backlog rows and NOT deferred implementation scope — reopen requires the criteria documented in that file plus a separate scoped goal. Vertical-vs-horizontal review mode is a separate "later candidate" class — not in IDEAS.md, not in BACKLOG, not in the implementation queue.
- **Polishing cycle — adopted decision + implementation plan.** `docs/systems/review/REVIEW_POLISHING_DECISION_RECORD.md` (agent-blind / role-neutral decision: `dual-perspective coverage`, U1–U10, packet enforcement, effort/model strategy, reviewer read-only / no-silent-fix invariants, the U10 hard gate, the final-report 10-field schema) + `docs/systems/review/REVIEW_POLISHING_IMPLEMENTATION_PLAN.md` (plan/spec entry; batch split A→B→C→D, evidence plan, approval boundary, caller-side 2-pass closeout method). **Layered readiness (unchanged):** policy adoption `yes`; implementation `no — plan/spec required` — each implementation batch needs a separate scoped goal + review gate. Policy-level records; no source/script/test change in the records themselves.
- **Batches A–C implemented (effort override + reviewer-safe invocation).** Batch A spec `docs/systems/review/REVIEW_POLISHING_BATCH_A_SPEC.md` (Gate 1 / Gate 2 investigation; no source change). Batch B: `scripts/review-run.ps1` resolves per-invocation reasoning effort (explicit `-Effort` > `config/reviewer.json` `reasoningEffort` > safe default `xhigh`; enum-validated, no silent fallback; `applied-effort` run-fact). Batch C: `--ignore-user-config` + `--sandbox read-only` + `--ask-for-approval never` make the reviewer-safe posture structural, with negative test `scripts/review-safety-negtest.ps1`; documented in `docs/policies/REVIEWER_CONFIG_POLICY.md`. Model-fallback removal (corrective): reviewer `model` resolves explicit `-Model` > config `model` > **fail-fast** (no hardcoded version; `fallbackModel` dead/legacy). Batch D spec `docs/systems/review/REVIEW_POLISHING_BATCH_D_SPEC.md` (report-schema / run-fact wiring). **Non-claim:** reviewer-safe invocation is verified for the tested vectors only (create / modify-tracked / modify-existing), **not a blanket guarantee** (caveat home `docs/policies/REVIEWER_CONFIG_POLICY.md`); "U9 operational" here = the per-invocation effort override + reviewer-safety axes for the scalar safe-default / explicit-override path — the named config-backed `category → {model, effort}` vehicle is the separate U9 batch below. Per-batch commit / Pester-AC detail: git history.
- **Batch D mainline complete (D1→D2→D3; no D4).** Spec `docs/systems/review/REVIEW_POLISHING_BATCH_D_SPEC.md`. D1: final-report 10-field schema operative home `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §6b (+ SKILL mirror; invocation-count / artifact-pass-count / corrective-loop-count axes kept distinct). D2: `scripts/review-run.ps1` emits resolved `model` + `model-source`, `reviewer-safe-posture`, and engine identity as H1 run-facts (`tests/review-run.Tests.ps1` AC-RR21–24, 24/24). D3: §6b.2 surfaces those run-facts into the human final report's reviewer-guard-status field. Committed (spec `843b913`; D1 `441886d` / D2 `065cbea` / D3 `b3a9d86`), pushed, globally activated. Docs/runner only — parser surface unchanged; no concrete model version in durable docs (`config/reviewer.json` source-of-truth). Non-mainline deferred/optional follow-up (not a Batch D blocker): optional mirror propagation to `OPERATOR_GUIDE_KR.md` / `README.md` / `templates/`. Detail: the spec + `REVIEW_RESULT_CONTRACT.md` §6b + git history.
- **RV-B-06 reviewer runtime provenance — closed.** P1–P4 (`## Reviewer run provenance` block + contract dual-authorship) implemented, reviewed, committed, pushed (`77691c2`/`fdde410`/`fbd295e`/`cfac1ed`). Authoritative compact record: Completed ledger RV-B-06 (BACKLOG tombstone); design `docs/systems/review/REVIEW_RESULT_PROVENANCE_SPEC.md`; full narrative in git history. Adapter-neutral; no parser-gate change; no sidecar; no version pin. **Activation/global update is a separate pending action (not performed).**
- **Review-input / result governance increments — implemented (wording-only; no parser/verifier/runtime change).** Under the RV-B-04 / RV-B-05 umbrellas: `## Known concerns` hypothesis-form convention — confirmed-disclosure vs hypothesis-to-verify, disclosure duty preserved (impl `88f6788`; design home `docs/systems/review/REVIEW_INPUT_KNOWN_CONCERNS_HYPOTHESIS_FORM_PLAN.md`, plan-doc `929f534`); runner stdout exact-line-anchor test discipline in `tests/README.md` (impl `2a15255`; design home `docs/systems/review/REVIEW_RUNNER_STDOUT_ANCHOR_TEST_PLAN.md`, plan-doc `c0136a6`); reviewer-reproduction opt-in `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3d — broad reproduction opt-in only, sandbox non-reproduction is a limitation not automatic target risk, §3a/§3b preserved (resolves the Batch-D-deferred "validation reproducibility in the reviewer's read-only sandbox" item — deferred→done) (impl `fdb9fe1`; design home `docs/systems/review/REVIEW_VALIDATION_EVIDENCE_REPRODUCTION_POLICY_PLAN.md`, plan-doc `9d77986`); local-validation closeout scope `REVIEW_RESULT_CONTRACT.md` §6c — full-suite-green by change class, suite terms single-homed in `tests/README.md` (impl `94f375c`; design home `docs/systems/review/REVIEW_LOCAL_VALIDATION_CLOSEOUT_POLICY_PLAN.md`, plan-doc `028f8dc`). RV-B-06 not reopened; full detail in the named plan docs + git history.
- **U9 config-backed category-effort policy — implemented (closes the U9 traceability gap; config/schema/runtime/tests/docs).** `config/reviewer.schema.json` `categoryPolicy` (strict portable `$defs/categoryEntry`) + `config/reviewer.json` **12 generic** categories all at the safe floor (configured safe-default model + `xhigh`; generic-only for global-install portability — project-specific categories deliberately not shipped) + `scripts/review-run.ps1 -EffortCategory` resolver (per-axis precedence; `effort-policy-match` by key presence — genuine miss soft-falls-back, matched-but-malformed entry fails fast unconditionally; `effort-category` / `effort-policy-match` run-facts) + `tests/review-run.Tests.ps1` AC-RR29–39. **Safety:** review effort is NOT lowered — every shipped category equals the current safe default; per-category VALUE tuning below the floor stays deferred to first-cycle operational data (recorded in the decision record). No parser/verifier gate change; no concrete model version in durable docs (`config/reviewer.json` source-of-truth). BACKLOG: RV-B-07 opened + closed in this batch. **Global activation after commit is a separate pending action.** Authoritative compact record: Completed ledger RV-B-07; detail: decision record U9 + git history.
- **Repo-wide `.md` EOL normalization — audited no-op (closed).** Audit found all tracked `.md` already LF/no-BOM matching `.gitattributes` `*.md text eol=lf` (0 committed CRLF), so normalization is a no-op; `.md` LF enforcement is via `.gitattributes`, not `verify-ps1.ps1` (which scans only `.ps1`). The `verify-ps1.Tests.ps1` autocrlf test-harness flakiness is the separate accepted residual in `docs/systems/install-update/STATUS.md` "Accepted residual risks", not a `.md` EOL issue. Detail in git history.
- **S6 strict-C1 review-artifact perspective layout — done.** Canonical layout = three-level `log/review/<review-task-id>/<perspective>/pass-NN/` (work-id / perspective / corrective-attempt as separate path segments), `-Perspective` required on `review-prepare/run/verify` (missing/empty fails fast; **no two-level fallback**); pre-strict-C1 two-level artifacts are legacy manual-readable runtime records (not tool-supported, not migrated, not deleted). Committed `5da6664` (initial C1, `-Perspective` optional) → `460ee3e` (strict C1 follow-up, two-level fallback removed), pushed; global channel-3 engine + Claude/Codex managed-block + deployed skill all adopted (verify_pass). No parser/verifier gate change; verdict vocabulary unchanged. Authoritative compact record: Completed ledger RV-B-08. Design / detail home `docs/systems/review/REVIEW_ARTIFACT_PERSPECTIVE_LAYOUT_PLAN.md` (Status: implemented — strict C1, done); BACKLOG tombstone RV-B-08; full surface list + Codex review tasks in git history.

## Accepted residual risks (LTS maintenance)

The following residual risks were surfaced during the LTS closeout and are explicitly accepted as part of LTS maintenance posture. Reopen requires concrete new evidence beyond the current pattern.

- **H1 preamble / Counter-argument surface alignment** (closed by Stage 4-R1) — the prior dual-surface tension (H1 preamble strongest-case-against pointing to `## Notes` vs `REVIEW_RESULT_CONTRACT.md §3c` making `## Counter-argument` the dedicated surface) was disclosed and user-accepted at Batch 1A entry. Stage 4-R1 (this batch) aligned the H1 reviewer-mode preamble in `scripts/review-run.ps1` to direct strongest-case-against substance to `## Counter-argument`; `## Counter-argument` remains optional / strongly-recommended / non-parser (no Option A parser-required escalation). Option A (parser-required `## Counter-argument`) is not introduced; `REVIEW_RESULT_CONTRACT.md` §3c.5 records it as a §3c non-goal, and no active source-managed escalation threshold exists.
- **Operator narrow-scope over-confidence pattern** (recurrence; dual nature — operator failure mode + review-system effectiveness evidence) — Batch 1A pass-02 within-file enumeration, IDEAS governance citation provenance, and v2/v3 polishing-inventory count drift are documented recurrences. In each case Codex independent review mechanically caught the operator scope-narrowing before it could land as final accepted state (via `Get-ChildItem` for filesystem count, `rg -n` for citation provenance, and grep for within-file enumeration); the recurrence is operator-side failure mode while the catch is review-system effectiveness evidence — both framings hold simultaneously in LTS maintenance. Residual: operator can still create wrong inputs before Codex review runs. Mitigation: Codex independent review (primary) + operator-side discipline codified in `snippets/claude-skills/ai-harness-review/SKILL.md` step 4 (citation provenance verify before quoting + mechanical count check + grep-based exhaustiveness) + `REVIEW_RESULT_CONTRACT.md §5a.4` retraction protocol when discovered ex-post. No script-side enforcement (governance plan D12 not-implementable-yet).
- **Bash chain final-exit-code false-fail** (operator reporting convention) — when a `;`-chained shell line ends with a command whose non-zero exit is informational (e.g., absence-check `ls` of a directory expected to be missing), the chain's final exit code can mask the success of the preceding `review-verify` call. Harness-level mitigation is out of toolset scope (the toolset does not own the shell-chain reporter). Operator convention: when reporting a `review-verify` result, do not chain it before an informational command whose non-zero exit would override the success exit.
