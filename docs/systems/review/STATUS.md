# review — system status

Current status for the Codex review subsystem. The **canonical artifact contract** is `docs/contracts/review/REVIEW_RESULT_CONTRACT.md`; reviewer **config** is `docs/policies/REVIEWER_CONFIG_POLICY.md`; effort/cost operating guidance is `docs/policies/REVIEW_EFFORT_GUIDE.md`. Routing: `docs/current/SOURCE_OF_TRUTH.md` (Q2, Q3).

## Current state

- **Maintenance mode.** No new feature / reviewer adapter / multi-reviewer orchestration / review-history DB / cross-run aggregation / automatic retention without a separate scoped approval. Bug fix and contract clarification are in-scope.
- **Review System LTS — closed.** LTS Phase 1 (3 batches; `e6027ca`), Phase 2 Batch 1A (Counter-argument H2 Option B; `76033f4`), and IDEAS.md governance (devil's advocate + multi-reviewer idea-only durability mitigation; `37a895c`) all closed. Subsequent work proceeds under maintenance mode only — no broad redesign without separate scoped approval.
- **Canonical task/pass topology** is the current shape: one Codex attempt per `pass-NN`, grouped under one `<review-task-id>` directory; record = `<ProjectRoot>/log/review/<review-task-id>/pass-NN/input.md` (AI-authored) + `result.md` (dual-authored: reviewer-adapter verdict/disclosure body + a runner-appended `## Reviewer run provenance` block — RV-B-06 P3; `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3). An **optional `<perspective>` segment** gives a three-level `<review-task-id>/<perspective>/pass-NN/` when `-Perspective <viewpoint>` is supplied (two-level remains the default; S6 C1, implemented complete-candidate / pre-commit — see the S6 C1 ledger bullet under Open / historical). No sidecar JSON, no hash-binding files, no external staging, no flat run-id layout.
- **Operator entrypoint:** `scripts/review-prepare.ps1` → `scripts/review-run.ps1` (one Codex execution per call), with `scripts/review-verify.ps1` as the post-hoc canonical-artifact check and `scripts/review-input-verify.ps1` as the input gate. Natural-language UX: `docs/user_guide/OPERATOR_GUIDE_KR.md` §7 + `snippets/claude-skills/ai-harness-review/SKILL.md`.
- Verdict (`yes` / `no` / `yes with risk`) never auto-approves commit / push / publish / merge / release.

## Completed ledger

| ID | Item | Closed at | Current meaning | Detail |
|---|---|---|---|---|
| RV-01 | Codex review subsystem operational; entered maintenance mode | — | review flow stable; maintenance scope only | `docs/decisions/POST_MVP_PLAN.md` §2 |
| RV-02 | Canonical review task/pass topology — contract alignment | `a5d94a5` | source-of-truth wording aligned to task/pass shape | `docs/decisions/POST_MVP_PLAN.md` §10 |
| RV-03 | Canonical review task/pass topology — implementation | `c81fe45` | script/template/I-O shape aligned to RV-02; removed-legacy sidecar artifacts dropped from normal path | `docs/decisions/POST_MVP_PLAN.md` §10 |
| RV-04 | Review effort/cost operating guide added | — | advisory only; introduces no automatic gate | `docs/policies/REVIEW_EFFORT_GUIDE.md` |
| RV-05 | Review System LTS Phase 1 (Batch 0/I/II) closed | `e6027ca` | V2 mirror cleanup + verdict→next-action mapping (`§6a`) + §5a operator stance + light P3 wording in H1 preamble; all source mutation + propagation 4 surfaces (channel 3 / CLAUDE.md / Claude skill / AGENTS.md) resolved | `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §5a / §6a + `scripts/review-run.ps1` H1 preamble; commit `e6027ca` |
| RV-06 | Phase 2 Batch 1A — Counter-argument H2 Option B codification | `76033f4` | strongly-recommended (non-parser) `## Counter-argument` H2 added to result.md convention (`REVIEW_RESULT_CONTRACT.md §3c`); 4 source surfaces (contract + result template + input template + SKILL); parser surface unchanged; H1 preamble runtime instruction is aligned with §3c by Stage 4-R1 (strongest-case-against directs to `## Counter-argument`); Batch 1B mirror surfaces deliberately separate (see RV-B-05) | `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3c; commit `76033f4` |
| RV-07 | IDEAS.md governance — idea-only durability mitigation | `37a895c` | `docs/systems/review/IDEAS.md` codifies devil's advocate pre-pass and multi-reviewer/calibration loop as idea-only items (not implementation backlog, not deferred implementation scope); reopen criteria documented in source | IDEAS.md (`docs/systems/review/IDEAS.md`) |
| RV-B-06 | Reviewer runtime provenance in the result artifact (P1–P4) | `cfac1ed` | `result.md` is self-describing via a runner-appended `## Reviewer run provenance` block: P2 adapter kind/version H1 stdout run-facts → P3 result.md provenance persistence + contract dual-authorship → P4 operator surfacing + single-author mirror-label reconciliation. Adapter-neutral; no parser-gate change; no sidecar; no version pin. Reviewed P2 `review-result-provenance-p2-2026-06-03` / P3 `review-result-provenance-p3-2026-06-03` / P4 `review-result-provenance-p4-2026-06-03`. Activation/global update not yet performed (separate pending action). | `docs/systems/review/REVIEW_RESULT_PROVENANCE_SPEC.md`; commits `77691c2`(P1)/`fdde410`(P2)/`fbd295e`(P3)/`cfac1ed`(P4) |

## Open / historical

- Open review-subsystem backlog candidates are consolidated in `docs/systems/review/BACKLOG.md` (RV-B-01..RV-B-05; RV-B-06 closed — see Completed ledger RV-B-06 + its BACKLOG tombstone); their full historical text and the removed-legacy items are preserved in git history (classification index: `docs/backlog/INDEX.md`). The explicit range above mirrors the current open row set in BACKLOG.md — that file is the source-of-truth for open candidates and supersedes this range when they diverge. Removed-legacy review-cycle / `meta.json` / `result.json` / `target-files.list` / `<run-id>` identifiers are historical reason only, never operator paths.
- Idea-only items (devil's advocate pre-pass orchestration, multi-reviewer consensus / post-merge calibration loop) are codified in `docs/systems/review/IDEAS.md`. They are NOT backlog rows and NOT deferred implementation scope — reopen requires the criteria documented in that file plus a separate scoped goal. Vertical-vs-horizontal review mode is a separate "later candidate" class — not in IDEAS.md, not in BACKLOG, not in the implementation queue.
- **Adopted decision record (review-system polishing cycle):** `docs/systems/review/REVIEW_POLISHING_DECISION_RECORD.md` — agent-blind / role-neutral decision record covering `dual-perspective coverage` (the canonical successor to the legacy vertical/horizontal review-mode framing noted above — coverage policy, not reviewer invocation count), the U1–U10 decisions, packet enforcement, effort/model strategy, reviewer read-only / no-silent-fix and reviewer-invocation-safety invariants, the U10 hard gate for U9 operationalization, and the final-report 10-field schema. Layered readiness: final decision adoption `yes`; implementation `no — plan/spec required`; U9 operational and reviewer-safe invocation claims blocked until the combined first hard gate (U10 per-invocation effort override + reviewer-safety override) is verified. It is a policy-level adopted decision, not an implementation — no source/script/test change accompanies it, and it stays outside the implementation queue until a separate scoped plan/spec.
- **Implementation planning document (plan/spec entry):** `docs/systems/review/REVIEW_POLISHING_IMPLEMENTATION_PLAN.md` — plan/spec entry document derived from the adopted decision record. Defines the first hard gate (U10 per-invocation effort override verification + reviewer-safe invocation override verification), the evidence plan, the batch split (A investigation/spec → B effort override → C reviewer-safety → D report wiring), the approval boundary, and the caller-side 2-pass review closeout method usable at the current HEAD. It is a plan entry document, not an implementation directive and not an implementation approval; each batch requires a separate scoped goal + review gate. Layered readiness is unchanged from the decision record (adoption `yes`; implementation `no — plan/spec required`; U9 operational and reviewer-safe invocation claims blocked until the combined first hard gate (U10 per-invocation effort override + reviewer-safety override) is verified).
- **Batch B implemented (per-invocation effort override wiring):** `scripts/review-run.ps1` now resolves a reasoning effort (explicit `-Effort` > `config/reviewer.json` `reasoningEffort` > built-in safe default `xhigh`), validates it against `{none,minimal,low,medium,high,xhigh}` (invalid fails fast, no silent fallback), passes it to the Codex CLI as `-c model_reasoning_effort=<value>`, and captures the applied effort as a run-fact from the Codex stderr header (`applied-effort:` line; `not-observed` when absent). Config schema (`config/reviewer.schema.json`) and the effort/config policies (`docs/policies/REVIEWER_CONFIG_POLICY.md`, `docs/policies/REVIEW_EFFORT_GUIDE.md`) were reconciled to the adopted default-`xhigh`-with-downgrade policy. **Batch B alone does not establish `U9 operational`** — that still requires the Batch C reviewer-safety verification (the combined first hard gate). Reviewer-safe invocation precedence remains verified for one tested write vector only (Batch A), not a blanket guarantee.
- **Batch C implemented (reviewer-safe invocation hardening + negative test):** `scripts/review-run.ps1` now also passes `--ignore-user-config` so the reviewer-safe posture is structural (the reviewer tool's permissive `$CODEX_HOME/config.toml` is not loaded) rather than dependent on flag-precedence over a loaded permissive config; `--sandbox read-only` / `--ask-for-approval never` remain. `scripts/review-safety-negtest.ps1` automates the Batch A spec §2d negative test: under the environment's actual permissive global config it attempts source-tree create, tracked-file modify, and existing-file modify with the reviewer-safe flags, and confirms each is blocked by BOTH the model's report AND an independent filesystem check; a write that lands fails loudly (no silent pass). `docs/policies/REVIEWER_CONFIG_POLICY.md` documents the reviewer-safe invocation + the `--ignore-user-config` trade-off. **Reviewer-safe invocation is verified for the tested vectors only (create / modify-tracked / modify-existing), NOT a blanket guarantee** — untested vectors (arbitrary exec, network, alternative write APIs), other platforms, and other reviewer-tool versions remain limitations. The result artifact stays runner-controlled (`--output-last-message`). **Combined first hard gate:** both axes are now implemented and verified for their tested scope — U10 per-invocation effort override + run-fact capture (Batch B) and reviewer-safe invocation override (Batch C, tested-vectors). `U9 operational` and reviewer-safe invocation may be reported on that basis, carrying the explicit *tested-vectors-only, not-blanket* caveat for reviewer safety; they are not claimed beyond the verified scope. **Scope of `U9 operational` as established here (corrected):** Batch B wired only the *scalar* effort resolution (explicit `-Effort` > `config/reviewer.json` `reasoningEffort` > built-in `xhigh`) — the U10 per-invocation override mechanism. The decision record's named U9 vehicle, the config-backed `category → {model, effort}` mapping lookup, was **not** part of the Batch B as-built; so "U9 operational" here means the per-invocation effort override + reviewer-safety axes are live for the scalar safe-default / explicit-override path, **not** that the category-policy vehicle existed. The category-policy vehicle is implemented separately — see the "U9 config-backed category effort policy implemented" bullet below.
- **Model fallback removal (corrective):** the reviewer **model** now has no built-in default/fallback — `review-run.ps1` resolves explicit `-Model` > `config/reviewer.json` `model` > **fail-fast**, and `review-safety-negtest.ps1` resolves config `model` > fail-fast (no hardcoded version). A concrete model version is tied to an external lifecycle, so it is never hardcoded as a default that would mask a missing source-of-truth. `config/reviewer.schema.json` marks `model` required; `fallbackModel` is dead/legacy (never auto-used). The effort safe-default (`xhigh`) is unchanged.
- **Batch A spec (Gate 1 / Gate 2 investigation + impl spec):** `docs/systems/review/REVIEW_POLISHING_BATCH_A_SPEC.md` — investigation result + implementation spec for the two first-hard-gate axes, with no source/script/test/config change. Findings are separated into verified / not verified / unknown. Net boundary result: Codex CLI per-invocation effort override (`-c model_reasoning_effort`, allowed `{none,minimal,low,medium,high,xhigh}`) is a *verified CLI capability* but is *not wired into `review-run.ps1`*, so U9 operational stays `not verified / not operational`; reviewer-safe invocation precedence over the permissive global config is *verified for one tested write vector* (read-only flag blocked a model write under `danger-full-access`) but is *not a blanket guarantee*. It is a spec, not an implementation and not an operational claim; Batch B/C implementation each require a separate scoped goal + review gate.
- **Batch D spec (report schema / run-fact wiring investigation + impl spec):** `docs/systems/review/REVIEW_POLISHING_BATCH_D_SPEC.md` — investigation + implementation spec for the final-report 10-field schema and run-fact output wiring, with no source/script/test/config change. Findings (inspection-verified): the Batch B effort run-facts (`requested-effort` / `effort-source` / `applied-effort`) are already emitted by `review-run.ps1`, but the resolved `model` value (and a model-source — inferable from the resolution branch but not retained as a distinct value today), the reviewer-safe posture, and the review-engine ToolRoot/ProjectRoot identity are not emitted; the decision record's 10-field final-report schema lived only in `REVIEW_POLISHING_DECISION_RECORD.md` at spec time and was not yet mirrored into the contract or the `snippets/claude-skills/ai-harness-review/SKILL.md` step-7 report (which then conflated the invocation-count / artifact-pass-count / corrective-loop-count axes that the schema deliberately separates) — D1 has since codified the operative home (see the Batch D1 bullet below). The spec proposes a three-home classification (runner stdout / final human report / docs-only) and a recommended D1 (schema single-home + SKILL mirror) → D2 (runner run-fact expansion) → D3 (B/C effort/safety run-fact surfaced in the final report, carrying the tested-vectors-only caveat) split. Parser enforcement stays narrow (no new semantic gate, user-decision 1); no concrete model version is added to durable docs (it stays the `config/reviewer.json` source-of-truth). It is a spec, not an implementation and not an operational claim; each Batch D sub-step requires a separate scoped goal + review gate, and (per §5a.7) its closeout review runs on the global stable ToolRoot engine.
- **Batch D1 implemented (final-report schema operative home + SKILL mirror, docs-only):** the decision record's 10-field final-report schema now has an operative home in `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §6b (Operator final report schema); `REVIEW_POLISHING_DECISION_RECORD.md` §"Final report schema decision" stays the policy origin (authority-home move, not a decision reopening). §6b codifies the 10 fields with the invocation-count / artifact-pass-count / corrective-loop-count axes kept distinct, the `N/A — no artifact pass-NN` convention, the re-review status enum, and an optional reviewer-guard-status field (reviewer execution guard / effort / model + model-source / reviewer-safe posture / review-engine identity) that separates the observability source of each item (at D1 time: effort run-facts from Batch B; engine identity from operator invocation knowledge; model / model-source and reviewer-safe posture were then pending runner run-fact wiring — since delivered by Batch D2, below, so §6b.2 now reports all of them from runner run-facts). `snippets/claude-skills/ai-harness-review/SKILL.md` step 7 is realigned as the operator mirror (the three axes separated; the prior step-7 conflation removed). §6b is explicitly a human closeout-report schema — **not** the on-disk `result.md` parser contract (§3) and **not** a `scripts/review-verify.ps1` gate; the parser surface is unchanged (no new semantic gate, user-decision 1) and no concrete model version is added to durable docs (it stays the `config/reviewer.json` source-of-truth). Reviewer-safe stays tested-vectors-only (not a blanket guarantee). Docs-only: no source/script/test/config change. Batch D2 (runner run-fact expansion) and D3 (final-report surfacing) followed and are implemented below.
- **Batch D2 implemented (runner run-fact expansion):** `scripts/review-run.ps1` success output now also emits, alongside the Batch B effort run-facts, the resolved reviewer `model` + `model-source` (the actual resolver branch — at Batch D2: `explicit` for `-Model`, `config` for `config/reviewer.json`; U9 later added a `category` branch for a matched `categoryPolicy` entry's `model` — see the U9 category-policy ledger bullet below; `Get-ReviewerModel` returns both value and source, and a missing/empty model still fails fast with no built-in fallback), a `reviewer-safe-posture` run-fact derived from the structural flags actually passed (`--ask-for-approval never --sandbox read-only --ignore-user-config web_search=disabled` — posture flags only, not a blanket guarantee; the tested-vectors-only caveat stays in `docs/policies/REVIEWER_CONFIG_POLICY.md` and the operator final report), and the engine identity `tool-root` / `project-root` / `tool-root-source`. These are single-invocation H1 run-facts for operator debugging; the runner still does not emit H2 final-report fields (perspective coverage, invocation / artifact-pass / corrective-loop counts, commit/push recommendation). No concrete model version is added to durable docs/scripts — the runtime-resolved model value is printed to stdout only. `tests/review-run.Tests.ps1` adds AC-RR21–24 (config model + `model-source: config`; explicit `-Model` + `model-source: explicit`; reviewer-safe-posture flags; engine identity) and keeps the Batch B effort tests and the missing-model fail-fast (AC-RR19); 24/24 pass. Parser surface unchanged (`review-verify.ps1` / `review-input-verify.ps1` untouched). Batch D3 (surfacing those run-facts in the operator final report) is implemented below.
- **Batch D3 implemented (H1 run-fact → H2 final-report surfacing, docs-only):** the final-report schema now states how the operator surfaces the Batch D2 run-facts (effort 3-tuple, model / model-source, reviewer-safe posture, engine identity) into the **reviewer guard status** field of the human final report. `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §6b.2 adds a "Surfacing (Batch D3 — H1 run-fact → H2 final report)" note and §6b.1 reframes the reviewer-guard-status field: surfacing is **recommended whenever the runner emitted the run-facts** (the normal post-D2 case), optional for a trivial non-self-modification review, and **expected for a review-subsystem self-modification closeout** (engine identity + reviewer-safe posture + applied-effort), pairing with the §5a.7 engine-disclosure duty. `snippets/claude-skills/ai-harness-review/SKILL.md` step 7 mirrors this. This is operator reporting discipline, **not** a parser/gate change — `review-verify.ps1` / `review-input-verify.ps1` are unchanged and enforce nothing here (the §6b H1/H2 boundary holds). Caveats preserved: reviewer-safe stays tested-vectors-only / not-blanket; no concrete model version enters durable docs (the runtime-resolved value is cited); engine paths are operator-debugging facts, not source-of-truth claims; the verdict is not commit/push approval. Docs-only: no `scripts/review-run.ps1`, tests, or parser/gate change (D2 already wired the emission). SKILL.md stays a self-contained deployed surface (no repo-doc path / `§N` pointer reintroduced).
- **Batch D mainline complete (closeout):** the Batch D spec's recommended split — D1 (final-report schema operative home + SKILL mirror) → D2 (runner run-fact emission) → D3 (H1 run-fact → H2 final-report surfacing) — is fully implemented, dual-perspective reviewed on the global stable ToolRoot engine (per §5a.7), committed (spec `843b913`; D1 + deployed-surface hygiene `441886d`; D2 `065cbea`; D3 `b3a9d86`), pushed, and globally activated. The spec (`REVIEW_POLISHING_BATCH_D_SPEC.md` §4) defines no further mainline sub-step — there is no D4. Remaining items are non-mainline polishing candidates tracked outside this ledger as deferred/optional (not Batch D blockers): optional mirror propagation to `OPERATOR_GUIDE_KR.md` / `README.md` / `templates/`; and (originally) the repo-wide `.md` EOL convention — **this deferred item's premise no longer holds: a read-only audit at HEAD `a26f9d4` found every tracked `.md` already LF/no-BOM matching `.gitattributes` `*.md text eol=lf` (0 committed CRLF), so it is a normalization no-op (resolved); see the `.md` EOL audit closeout bullet below**. (The earlier "validation reproducibility inside the reviewer's read-only sandbox" deferred item is now **resolved** — codified as the reviewer-reproduction opt-in policy; see the reviewer-reproduction opt-in bullet below.) The review subsystem stays in maintenance mode — no new mainline review-system work without a separate scoped approval.
- **Reviewer runtime provenance in the result artifact (RV-B-06) — closed.** The P1–P4 reviewer runtime provenance track (P1 planning/design spec → P2 adapter kind/version runtime observation + H1 run-facts → P3 `result.md` `## Reviewer run provenance` persistence + contract dual-authorship → P4 operator surfacing + single-author mirror-label reconciliation) is implemented, reviewed, committed, and pushed. Authoritative compact record: **Completed ledger RV-B-06** above (BACKLOG tombstone: `docs/systems/review/BACKLOG.md`). Design + status detail: `docs/systems/review/REVIEW_RESULT_PROVENANCE_SPEC.md`; full P1–P4 implementation narrative in git history (commits `77691c2`/`fdde410`/`fbd295e`/`cfac1ed`). Adapter-neutral, no parser-gate change, no sidecar, no version pin. **Activation/global update is a separate pending action (not performed).** Batch D is not reopened (no D4).
- **Review-input `## Known concerns` hypothesis-form convention implemented (docs/template/skill wording).** The `## Known concerns` input section now distinguishes **confirmed disclosures** (operator-certain facts; §7 stale-by-omission applies in full) from **open concerns / hypotheses to verify** (unconfirmed suspicions, phrased neutrally as `verify whether…` / `check whether…`), with a guard that a genuine known compromise must stay a confirmed disclosure and must not be softened/disguised as a hypothesis — reducing confirmation-oriented input framing while preserving the disclosure duty. Wording/guidance only across the three active homes (`templates/review-input.md`, `snippets/claude-skills/ai-harness-review/SKILL.md`, `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §2); **no parser/lint/heading change** (§10 `## Known concerns` sub-shape-lint non-goal unchanged), RV-B-06 not reopened. `snippets/CLAUDE_SNIPPET.md` / `AGENTS_SNIPPET.md` deliberately left unchanged (managed-block mirror guards; contract precedence per §5a) — disclosed as a mirror asymmetry. Design/plan source-of-truth: `docs/systems/review/REVIEW_INPUT_KNOWN_CONCERNS_HYPOTHESIS_FORM_PLAN.md` (committed `929f534`). Under the RV-B-05 review-input-governance umbrella.
- **Runner stdout exact-line-anchor test authoring discipline codified (docs/test convention).** `tests/README.md` now documents that **new** stdout contract tests verify line-oriented `key: value` run-fact / status lines with exact-line anchors (`(?m)^key: value$`), not loose substrings — scoped to run-fact / status lines (not error / argv / non-run-fact strings), and noting variant run-facts such as `applied-effort` (`not-observed` / `WARNING` / plain). Existing loose effort assertions (AC-RR13/14/16 in `tests/review-run.Tests.ps1`) are intentionally left unchanged (no mass rewrite); the Batch D2 / RV-B-06 anchored assertions are the reference examples. **No parser/verifier/runtime/emission change.** Design/plan source-of-truth: `docs/systems/review/REVIEW_RUNNER_STDOUT_ANCHOR_TEST_PLAN.md` (committed `c0136a6`). A Batch D deferred/optional polishing item resolved here (not RV-B-06, not RV-B-05).
- **Reviewer reproduction opt-in policy codified (validation evidence / sandbox boundary; docs/contract/template/skill wording).** `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` gains **§3d (Reviewer reproduction opt-in)**: local operator owns execution, reviewer owns inspection, evidence bridges the two; reviewer **broad** validation / build / test reproduction is **opt-in** (only on explicit review-input authorization carrying exact command / working dir / read-write behavior / allowed temp-output path / dependency assumptions / timeout / interpretation boundary / how-to-report-sandbox-limitation), **not opportunistic** just because the read-only sandbox can execute commands; sandbox non-reproduction is a `## Review limitations` entry, **not automatically target risk** (escalation needs independent grounds — missing/stale local evidence, scope mismatch, static contradiction, explicit high-risk gap); target-project validation (VS/MSBuild/C++/CMake/Unity/Unreal/network restore/generated output/repo-external SDK) is not a default reproduction target. The existing **§3a evidence boundary** (reviewer-readable supporting material; not command re-execution / truth oracle / freshness binding / source-of-truth) and the **§3b narrow minimal-reproducible-check expectation** are **preserved and cross-referenced** (§3d.6 draws the narrow-probe vs broad-reproduction line; §3b/§3a/§6/§2 carry additive pointers, no wording weakened). Mirrors: `templates/review-input.md` `## Validation evidence` guidance + `snippets/claude-skills/ai-harness-review/SKILL.md` (self-contained, no `§N` pointer). **No parser/verifier/runtime change** (§10 records the no-build-runner / no-capability-probe / no-SDK-detection non-goal); `scripts/review-run.ps1` H1 reviewer-mode preamble intentionally **unchanged** (runtime — deferred to a later scoped batch if alignment proves needed); RV-B-06 not reopened; no EOL change. **Resolves the Batch-D "validation reproducibility inside the reviewer's read-only sandbox" deferred item (deferred→done).** Design/plan source-of-truth: `docs/systems/review/REVIEW_VALIDATION_EVIDENCE_REPRODUCTION_POLICY_PLAN.md` (committed `9d77986`). Under the RV-B-04 reviewer no-exec/no-write umbrella (RV-B-05 input-governance adjacency); BACKLOG rows unchanged (implemented-state recorded here in the ledger, per recent convention).
- **Local validation closeout policy implemented (validation scope by change class; docs/contract/template/skill wording).** `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` gains **§6c (Local validation closeout scope)**: local operator owns validation execution, the reviewer inspects validation evidence + scope rationale (the reviewer is **not** the full-suite runner), and "full suite green" is required **by change class, not universally**. §6c codifies the change-class matrix (planning/design doc · docs-only wording · contract/template/SKILL wording · script/runtime · parser/verifier gate · test code · install/update/activation · EOL/formatting), the full-suite-expected vs targeted split, the full-suite-not-run reporting rule (change class / validation run / not run / reason / residual risk → §6b.1 field 8), and the `git diff --check` untracked-file gotcha (`git add -N` before the check). Suite terminology (`full suite` / `review-system suite` / `affected tests` / `smoke / verify` / `manual AC`) is single-homed in `tests/README.md` (new "Validation scope terms" section) and cited by §6c; `tests/README.md` also corrects its review-topology list to include `review-adapter` and the real-Codex `review-safety-negtest` (the latter excluded from the routine subset guard). Cross-refs: §6b.1 field 8 + §3d.1 (operator-side counterpart to the §3d reviewer-reproduction opt-in — **not a reopening**) + a §10 non-goal (no validation runner / CI / change-class detection / affected-test mapping; not full-suite-for-everything). Mirrors: `snippets/claude-skills/ai-harness-review/SKILL.md` (step 7 field 8 + `## Validation evidence` guidance) + `templates/review-input.md`. **No parser/verifier/runtime change**; no test-logic change (`tests/README.md` is docs-only); RV-B-06 not reopened; §3d not reopened; no EOL change. **Resolves the planning-doc-only temporary orphan** (this inbound pointer wires it). Design/plan source-of-truth: `docs/systems/review/REVIEW_LOCAL_VALIDATION_CLOSEOUT_POLICY_PLAN.md` (committed `028f8dc`; docplan review `local-validation-closeout-policy-docplan-2026-06-04` pass-03 yes). Under the RV-B-05 operator-side validation-governance umbrella (operator-side pair to §3d / RV-B-04); BACKLOG rows unchanged (implemented-state recorded here in the ledger, per recent convention).
- **U9 config-backed category effort policy implemented (recovery batch — closes the U9 traceability gap; config/schema/runtime/tests/docs).** The decision record's named U9 vehicle — the config-backed `category → {model, reasoningEffort}` mapping lookup (`docs/systems/review/REVIEW_POLISHING_DECISION_RECORD.md` "Script / config candidate decisions"; `REVIEW_POLISHING_IMPLEMENTATION_PLAN.md` §6 Batch B) — is now built. Prior as-built Batch B implemented only the scalar effort resolution; the category-map skeleton was missing (a U9 traceability gap surfaced by audit). This batch adds: `config/reviewer.schema.json` `categoryPolicy` (strict `$defs/categoryEntry`: `reasoningEffort` required + enum, optional `model`; portable — per-project key set / values, `additionalProperties` a strict entry); `config/reviewer.json` `categoryPolicy` with **12 generic** categories (`default`, `simple-local`, `medium-scope`, `complex-broad`, `system-coherence-heavy`, `contract-sensitive`, `boundary-sensitive`, `script-runtime`, `test-code`, `mechanical-audit`, `docs-planning`, `docs-wording`) **all at the safe floor (the configured safe-default model + `xhigh`)** — the concrete model value lives only in `config/reviewer.json` (source-of-truth), not embedded in this durable doc; `scripts/review-run.ps1` `-EffortCategory <key>` resolver (precedence per axis: explicit `-Effort`/`-Model` > matched category entry > scalar config > effort `xhigh` / model fail-fast), soft fallback on a genuine category miss (key absent), **unconditional** fail-fast on a matched-but-malformed entry (`effort-policy-match` is by **key presence**, so a present-but-null entry or an out-of-enum / missing `reasoningEffort` fails fast — even under an explicit `-Effort`/`-Model` override — rather than silently downgrading to the scalar default or being bypassed), and two additive run-facts `effort-category:` / `effort-policy-match:` (stdout + the `## Reviewer run provenance` block); `effort-source` / `model-source` gain a `category` branch. `tests/review-run.Tests.ps1` adds AC-RR29–39 (no-category preserves scalar; matched applies `{model,effort}` end-to-end via a distinct-value fixture; missed soft-fallback; explicit `-Effort`/`-Model` win per axis; out-of-enum category fails fast; shipped config safety-floor; provenance persistence; matched-but-missing-`reasoningEffort` fails fast; present-but-null entry fails fast; explicit `-Effort` does not bypass a matched-malformed entry) — 39/39 review-run pass (full suite 488/488). **Project-specific categories are deliberately not shipped** (an `install-update` category was dropped, and `parser-verifier` was folded into `contract-sensitive` per decision §169) so the global-install config stays portable across projects (per operator direction). **Safety:** review effort is NOT lowered — every shipped category equals the current safe default; per-category VALUE tuning below the floor stays deferred until first-cycle operational data (decision record U9: 세부 category 는 운용 데이터 후), so the decision record's "category after operational data" remains accurate. **No parser/verifier gate change** (`review-verify.ps1` / `review-input-verify.ps1` untouched; category policy adds no new semantic gate — user-decision 1 preserved); no concrete model version enters durable docs (config source-of-truth); reviewer-safe stays tested-vectors-only; RV-B-06 not reopened; no EOL change. Docs reconciled: `REVIEWER_CONFIG_POLICY.md` (precedence + "Category policy" section + run-facts), `REVIEW_EFFORT_GUIDE.md` (config-backed category reconciliation + auto-inference still a non-goal), `REVIEW_RESULT_CONTRACT.md` §6b.2 (category run-facts in reviewer guard status), `SKILL.md` (step 5 `-EffortCategory` option + step 6/7 run-facts), `templates/review-input.md` (category is a review-run choice, not an input section), `tests/README.md`, `REVIEW_RESULT_PROVENANCE_SPEC.md` (U9 addendum: block gained `effort-category`/`effort-policy-match`; F4/F6 source sets gained `category`). BACKLOG: RV-B-07 opened + closed in this same batch (no tracking-only orphan). **Global activation after commit is a separate pending action (not performed in this batch).** Under the U9/U10 effort-policy track; supersedes the U9 category-policy weak-tracking gap.
- **Repo-wide `.md` EOL normalization deferred item — audited no-op / closed (docs-only closeout; no EOL change).** Read-only audit at HEAD `a26f9d49bc09c8abd5b706796c2a958192791867` (`== origin/main`, clean tree): of **58 tracked `.md`** (`git ls-files '*.md'`), **58/58 are `i/lf  w/lf  attr/text eol=lf`** (`git ls-files --eol`) with **0 CRLF, 0 mixed-EOL, 0 UTF-8 BOM** — already matching the `.gitattributes` `*.md text eol=lf` policy, so the deferred "committed CRLF `.md`" premise is false and normalization is a **no-op**. The explicit `eol=lf` attribute makes git store/checkout `.md` as LF regardless of `core.autocrlf` (system `core.autocrlf=true` was observed but does not affect `.md`). **`.md` LF enforcement is via `.gitattributes`, not `verify-ps1.ps1`** — `verify-ps1` scans only `scripts/**/*.ps1`, never inspects `.md`, and its sole EOL behavior is a WARN-only "no CRLF" check on `.ps1`. **D8 / `core.autocrlf` is unrelated to `.md` EOL**: `verify-ps1`'s D8 is the *self-target enforcement* guard (no git-tracked file under `log/`); the only autocrlf tie is the pre-existing **accepted residual test-harness flakiness** of `verify-ps1.Tests.ps1` AC-VPS1-D8-* under system `core.autocrlf` + native-exe stderr promotion (ledgered in `docs/systems/install-update/STATUS.md` "Accepted residual risks"), which is not a `.md` EOL issue and is unchanged here. Docs-only: no EOL normalization, no `scripts`/`tests`/`config` change; the obsolete premise is also corrected in the two planning-doc non-goal references (`REVIEW_VALIDATION_EVIDENCE_REPRODUCTION_POLICY_PLAN.md`, `REVIEW_INPUT_KNOWN_CONCERNS_HYPOTHESIS_FORM_PLAN.md`). **Resolves the Batch-D-deferred "repo-wide `.md` EOL convention" item (deferred→no-op/done).** Commit/push not performed (separate explicit approval).
- **S6 C1 review artifact perspective layout implemented (complete-candidate, pre-commit; scripts/tests + contract/mirror wording).** Optional `-Perspective <viewpoint>` adds a three-level review-artifact layout `log/review/<review-task-id>/<perspective>/pass-NN/` (work-id / perspective / corrective-attempt as separate path segments); the two-level `<review-task-id>/pass-NN/` stays the default (backward compatible; no migration). `scripts/lib/path.ps1` gains `Test-ValidPerspective` / `Assert-ValidPerspective` (single safe segment — `..` / separators / `pass-NN` shape rejected; charset/length per `Test-ValidReviewTaskId`; **empty = omitted, not invalid**), `Get-ReviewPassParent`, and `Assert-InTaskRoot` (task-root containment beyond review-root containment, blocking cross-task traversal); `Get-ReviewPassDir` is perspective-aware. `scripts/review-prepare.ps1` / `review-run.ps1` / `review-verify.ps1` take an optional `-Perspective` (unspecified → old two-level; specified → three-level; fail-fast `invalid Perspective` before any dir creation / Codex call; `perspective:` run-fact; per-perspective `pass-NN`; verify resolves old/new by the explicit flag — **no automatic inference**). Operator-visible activation landed with all source-of-truth/mirror wording in the same batch (coupling-invariant form (a)): `REVIEW_RESULT_CONTRACT.md` §1/§3/§5/§7/§4 item 1/§4a/§6b.1 field 4 (per-perspective artifact pass count)/§8; `templates/review-input.md` (+ optional `## Review perspective` informational section, no gate) / `templates/review-result.md`; `SKILL.md`; `README.md`; `OPERATOR_GUIDE_KR.md`; `tests/README.md`. Tests (`tests/{path,review-prepare,review-run,review-verify}.Tests.ps1`) cover old/new coexistence, per-perspective increment, invalid-perspective rejection (empty-as-omitted, `..`, `/`, `\`, `pass-NN`, over-length, bad chars), and cross-task / task-name-prefix-sibling containment; **full suite 521/521**. **No parser/verifier gate change** (`review-verify.ps1` / `review-input-verify.ps1` result-shape gates unchanged); verdict vocabulary unchanged; no sidecar; no automatic perspective inference; no S8 docs cleanup. **Codex corrected-state review (global stable ToolRoot, `system-coherence-heavy`): task `s6-c1-perspective-impl-2026-06-05` — pass-01 no (contract / operator-guide two-level-only mirror lines) → pass-02 no (three Mermaid invocation signatures) → pass-03 yes (blocking 0); this source-closeout cleanup re-reviewed at pass-04.** **Pre-commit complete-candidate:** commit / push **not performed**; **global (channel 3) update pending (not performed)**; **managed-block snippet alignment (`snippets/CLAUDE_SNIPPET.md` / `AGENTS_SNIPPET.md`) is a separate boundary — Q8 = option B disclosed two-level-only asymmetry (plan §8 / §9 Batch 3; a Tier-D class outside the contract's enumerated mirror set; editing the repo snippet would create a repo↔deployed-managed-block drift reconcilable only by a separate user-approved re-adoption).** Design / source-of-truth home: `docs/systems/review/REVIEW_ARTIFACT_PERSPECTIVE_LAYOUT_PLAN.md` (Status: implemented — complete-candidate). **BACKLOG intentionally untouched** (the track was never an open BACKLOG row and a `[CLOSED]` tombstone is premature pre-commit; a closed RV-B-08 tombstone is the natural commit-time addition).

## Accepted residual risks (LTS maintenance)

The following residual risks were surfaced during the LTS closeout and are explicitly accepted as part of LTS maintenance posture. Reopen requires concrete new evidence beyond the current pattern.

- **H1 preamble / Counter-argument surface alignment** (closed by Stage 4-R1) — the prior dual-surface tension (H1 preamble strongest-case-against pointing to `## Notes` vs `REVIEW_RESULT_CONTRACT.md §3c` making `## Counter-argument` the dedicated surface) was disclosed and user-accepted at Batch 1A entry. Stage 4-R1 (this batch) aligned the H1 reviewer-mode preamble in `scripts/review-run.ps1` to direct strongest-case-against substance to `## Counter-argument`; `## Counter-argument` remains optional / strongly-recommended / non-parser (no Option A parser-required escalation). Option A (parser-required `## Counter-argument`) is not introduced; `REVIEW_RESULT_CONTRACT.md` §3c.5 records it as a §3c non-goal, and no active source-managed escalation threshold exists.
- **Operator narrow-scope over-confidence pattern** (recurrence; dual nature — operator failure mode + review-system effectiveness evidence) — Batch 1A pass-02 within-file enumeration, IDEAS governance citation provenance, and v2/v3 polishing-inventory count drift are documented recurrences. In each case Codex independent review mechanically caught the operator scope-narrowing before it could land as final accepted state (via `Get-ChildItem` for filesystem count, `rg -n` for citation provenance, and grep for within-file enumeration); the recurrence is operator-side failure mode while the catch is review-system effectiveness evidence — both framings hold simultaneously in LTS maintenance. Residual: operator can still create wrong inputs before Codex review runs. Mitigation: Codex independent review (primary) + operator-side discipline codified in `snippets/claude-skills/ai-harness-review/SKILL.md` step 4 (citation provenance verify before quoting + mechanical count check + grep-based exhaustiveness) + `REVIEW_RESULT_CONTRACT.md §5a.4` retraction protocol when discovered ex-post. No script-side enforcement (governance plan D12 not-implementable-yet).
- **Bash chain final-exit-code false-fail** (operator reporting convention) — when a `;`-chained shell line ends with a command whose non-zero exit is informational (e.g., absence-check `ls` of a directory expected to be missing), the chain's final exit code can mask the success of the preceding `review-verify` call. Harness-level mitigation is out of toolset scope (the toolset does not own the shell-chain reporter). Operator convention: when reporting a `review-verify` result, do not chain it before an informational command whose non-zero exit would override the success exit.
