# review — system status

Current status for the Codex review subsystem. The **canonical artifact contract** is `docs/contracts/review/REVIEW_RESULT_CONTRACT.md`; reviewer **config** is `docs/policies/REVIEWER_CONFIG_POLICY.md`; effort/cost operating guidance is `docs/policies/REVIEW_EFFORT_GUIDE.md`. Routing: `docs/current/SOURCE_OF_TRUTH.md` (Q2, Q3).

## Current state

- **Maintenance mode.** No new feature / reviewer adapter / multi-reviewer orchestration / review-history DB / cross-run aggregation / automatic retention without a separate scoped approval. Bug fix and contract clarification are in-scope.
- **Review System LTS — closed.** LTS Phase 1 (3 batches; `e6027ca`), Phase 2 Batch 1A (Counter-argument H2 Option B; `76033f4`), and IDEAS.md governance (devil's advocate + multi-reviewer idea-only durability mitigation; `37a895c`) all closed. Subsequent work proceeds under maintenance mode only — no broad redesign without separate scoped approval.
- **Canonical task/pass topology** is the current shape: one Codex attempt per `pass-NN`, grouped under one `<review-task-id>` directory; record = `<ProjectRoot>/log/review/<review-task-id>/pass-NN/input.md` (AI-authored) + `result.md` (Codex-authored). No sidecar JSON, no hash-binding files, no external staging, no flat run-id layout.
- **Operator entrypoint:** `scripts/review-prepare.ps1` → `scripts/review-run.ps1` (one Codex execution per call), with `scripts/review-verify.ps1` as the post-hoc canonical-artifact check and `scripts/review-input-verify.ps1` as the input gate. Natural-language UX: `docs/user_guide/OPERATOR_GUIDE_KR.md` §7 + `snippets/claude-skills/ai-harness-review/SKILL.md`.
- Verdict (`yes` / `no` / `yes with risk`) never auto-approves commit / push / publish / merge / release.

## Completed ledger

| ID | Item | Closed at | Current meaning | Detail |
|---|---|---|---|---|
| RV-01 | Codex review subsystem operational; entered maintenance mode | — | review flow stable; maintenance scope only | `docs/decisions/POST_MVP_PLAN.md` §2 |
| RV-02 | Canonical review task/pass topology — contract alignment | `a5d94a5` | source-of-truth wording aligned to task/pass shape | `docs/decisions/POST_MVP_PLAN.md` §10 |
| RV-03 | Canonical review task/pass topology — implementation | `c81fe45` | script/template/I-O shape aligned to RV-02; removed-legacy sidecar artifacts dropped from normal path | `docs/decisions/POST_MVP_PLAN.md` §10 |
| RV-04 | Review effort/cost operating guide added | — | advisory only; introduces no automatic gate | `docs/policies/REVIEW_EFFORT_GUIDE.md` |
| RV-05 | Review System LTS Phase 1 (Batch 0/I/II) closed | `e6027ca` | V2 mirror cleanup + verdict→next-action mapping (`§6a`) + §5a operator stance + light P3 wording in H1 preamble; all source mutation + propagation 4 surfaces (channel 3 / CLAUDE.md / Claude skill / AGENTS.md) resolved | `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §5a / §6a + `scripts/review-run.ps1` H1 preamble; commit `e6027ca` |
| RV-06 | Phase 2 Batch 1A — Counter-argument H2 Option B codification | `76033f4` | strongly-recommended (non-parser) `## Counter-argument` H2 added to result.md convention (`REVIEW_RESULT_CONTRACT.md §3c`); 4 source surfaces (contract + result template + input template + SKILL); parser surface unchanged; H1 preamble runtime instruction is aligned with §3c by Stage 4-R1 (strongest-case-against directs to `## Counter-argument`); Batch 1B mirror surfaces deliberately separate (see RV-B-05) | `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3c; commit `76033f4` |
| RV-07 | IDEAS.md governance — idea-only durability mitigation | `37a895c` | `docs/systems/review/IDEAS.md` codifies devil's advocate pre-pass and multi-reviewer/calibration loop as idea-only items (not implementation backlog, not deferred implementation scope); reopen criteria documented in source | IDEAS.md (`docs/systems/review/IDEAS.md`) |

## Open / historical

- Open review-subsystem backlog candidates are consolidated in `docs/systems/review/BACKLOG.md` (RV-B-01..RV-B-06); their full historical text and the removed-legacy items are preserved in git history (classification index: `docs/backlog/INDEX.md`). The explicit range above mirrors the current row set in BACKLOG.md — that file is the source-of-truth for open candidates and supersedes this range when they diverge. Removed-legacy review-cycle / `meta.json` / `result.json` / `target-files.list` / `<run-id>` identifiers are historical reason only, never operator paths.
- Idea-only items (devil's advocate pre-pass orchestration, multi-reviewer consensus / post-merge calibration loop) are codified in `docs/systems/review/IDEAS.md`. They are NOT backlog rows and NOT deferred implementation scope — reopen requires the criteria documented in that file plus a separate scoped goal. Vertical-vs-horizontal review mode is a separate "later candidate" class — not in IDEAS.md, not in BACKLOG, not in the implementation queue.
- **Adopted decision record (review-system polishing cycle):** `docs/systems/review/REVIEW_POLISHING_DECISION_RECORD.md` — agent-blind / role-neutral decision record covering `dual-perspective coverage` (the canonical successor to the legacy vertical/horizontal review-mode framing noted above — coverage policy, not reviewer invocation count), the U1–U10 decisions, packet enforcement, effort/model strategy, reviewer read-only / no-silent-fix and reviewer-invocation-safety invariants, the U10 hard gate for U9 operationalization, and the final-report 10-field schema. Layered readiness: final decision adoption `yes`; implementation `no — plan/spec required`; U9 operational and reviewer-safe invocation claims blocked until the combined first hard gate (U10 per-invocation effort override + reviewer-safety override) is verified. It is a policy-level adopted decision, not an implementation — no source/script/test change accompanies it, and it stays outside the implementation queue until a separate scoped plan/spec.
- **Implementation planning document (plan/spec entry):** `docs/systems/review/REVIEW_POLISHING_IMPLEMENTATION_PLAN.md` — plan/spec entry document derived from the adopted decision record. Defines the first hard gate (U10 per-invocation effort override verification + reviewer-safe invocation override verification), the evidence plan, the batch split (A investigation/spec → B effort override → C reviewer-safety → D report wiring), the approval boundary, and the caller-side 2-pass review closeout method usable at the current HEAD. It is a plan entry document, not an implementation directive and not an implementation approval; each batch requires a separate scoped goal + review gate. Layered readiness is unchanged from the decision record (adoption `yes`; implementation `no — plan/spec required`; U9 operational and reviewer-safe invocation claims blocked until the combined first hard gate (U10 per-invocation effort override + reviewer-safety override) is verified).
- **Batch B implemented (per-invocation effort override wiring):** `scripts/review-run.ps1` now resolves a reasoning effort (explicit `-Effort` > `config/reviewer.json` `reasoningEffort` > built-in safe default `xhigh`), validates it against `{none,minimal,low,medium,high,xhigh}` (invalid fails fast, no silent fallback), passes it to the Codex CLI as `-c model_reasoning_effort=<value>`, and captures the applied effort as a run-fact from the Codex stderr header (`applied-effort:` line; `not-observed` when absent). Config schema (`config/reviewer.schema.json`) and the effort/config policies (`docs/policies/REVIEWER_CONFIG_POLICY.md`, `docs/policies/REVIEW_EFFORT_GUIDE.md`) were reconciled to the adopted default-`xhigh`-with-downgrade policy. **Batch B alone does not establish `U9 operational`** — that still requires the Batch C reviewer-safety verification (the combined first hard gate). Reviewer-safe invocation precedence remains verified for one tested write vector only (Batch A), not a blanket guarantee.
- **Batch C implemented (reviewer-safe invocation hardening + negative test):** `scripts/review-run.ps1` now also passes `--ignore-user-config` so the reviewer-safe posture is structural (the reviewer tool's permissive `$CODEX_HOME/config.toml` is not loaded) rather than dependent on flag-precedence over a loaded permissive config; `--sandbox read-only` / `--ask-for-approval never` remain. `scripts/review-safety-negtest.ps1` automates the Batch A spec §2d negative test: under the environment's actual permissive global config it attempts source-tree create, tracked-file modify, and existing-file modify with the reviewer-safe flags, and confirms each is blocked by BOTH the model's report AND an independent filesystem check; a write that lands fails loudly (no silent pass). `docs/policies/REVIEWER_CONFIG_POLICY.md` documents the reviewer-safe invocation + the `--ignore-user-config` trade-off. **Reviewer-safe invocation is verified for the tested vectors only (create / modify-tracked / modify-existing), NOT a blanket guarantee** — untested vectors (arbitrary exec, network, alternative write APIs), other platforms, and other reviewer-tool versions remain limitations. The result artifact stays runner-controlled (`--output-last-message`). **Combined first hard gate:** both axes are now implemented and verified for their tested scope — U10 per-invocation effort override + run-fact capture (Batch B) and reviewer-safe invocation override (Batch C, tested-vectors). `U9 operational` and reviewer-safe invocation may be reported on that basis, carrying the explicit *tested-vectors-only, not-blanket* caveat for reviewer safety; they are not claimed beyond the verified scope.
- **Model fallback removal (corrective):** the reviewer **model** now has no built-in default/fallback — `review-run.ps1` resolves explicit `-Model` > `config/reviewer.json` `model` > **fail-fast**, and `review-safety-negtest.ps1` resolves config `model` > fail-fast (no hardcoded version). A concrete model version is tied to an external lifecycle, so it is never hardcoded as a default that would mask a missing source-of-truth. `config/reviewer.schema.json` marks `model` required; `fallbackModel` is dead/legacy (never auto-used). The effort safe-default (`xhigh`) is unchanged.
- **Batch A spec (Gate 1 / Gate 2 investigation + impl spec):** `docs/systems/review/REVIEW_POLISHING_BATCH_A_SPEC.md` — investigation result + implementation spec for the two first-hard-gate axes, with no source/script/test/config change. Findings are separated into verified / not verified / unknown. Net boundary result: Codex CLI per-invocation effort override (`-c model_reasoning_effort`, allowed `{none,minimal,low,medium,high,xhigh}`) is a *verified CLI capability* but is *not wired into `review-run.ps1`*, so U9 operational stays `not verified / not operational`; reviewer-safe invocation precedence over the permissive global config is *verified for one tested write vector* (read-only flag blocked a model write under `danger-full-access`) but is *not a blanket guarantee*. It is a spec, not an implementation and not an operational claim; Batch B/C implementation each require a separate scoped goal + review gate.
- **Batch D spec (report schema / run-fact wiring investigation + impl spec):** `docs/systems/review/REVIEW_POLISHING_BATCH_D_SPEC.md` — investigation + implementation spec for the final-report 10-field schema and run-fact output wiring, with no source/script/test/config change. Findings (inspection-verified): the Batch B effort run-facts (`requested-effort` / `effort-source` / `applied-effort`) are already emitted by `review-run.ps1`, but the resolved `model` value (and a model-source — inferable from the resolution branch but not retained as a distinct value today), the reviewer-safe posture, and the review-engine ToolRoot/ProjectRoot identity are not emitted; the decision record's 10-field final-report schema lived only in `REVIEW_POLISHING_DECISION_RECORD.md` at spec time and was not yet mirrored into the contract or the `snippets/claude-skills/ai-harness-review/SKILL.md` step-7 report (which then conflated the invocation-count / artifact-pass-count / corrective-loop-count axes that the schema deliberately separates) — D1 has since codified the operative home (see the Batch D1 bullet below). The spec proposes a three-home classification (runner stdout / final human report / docs-only) and a recommended D1 (schema single-home + SKILL mirror) → D2 (runner run-fact expansion) → D3 (B/C effort/safety run-fact surfaced in the final report, carrying the tested-vectors-only caveat) split. Parser enforcement stays narrow (no new semantic gate, user-decision 1); no concrete model version is added to durable docs (it stays the `config/reviewer.json` source-of-truth). It is a spec, not an implementation and not an operational claim; each Batch D sub-step requires a separate scoped goal + review gate, and (per §5a.7) its closeout review runs on the global stable ToolRoot engine.
- **Batch D1 implemented (final-report schema operative home + SKILL mirror, docs-only):** the decision record's 10-field final-report schema now has an operative home in `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §6b (Operator final report schema); `REVIEW_POLISHING_DECISION_RECORD.md` §"Final report schema decision" stays the policy origin (authority-home move, not a decision reopening). §6b codifies the 10 fields with the invocation-count / artifact-pass-count / corrective-loop-count axes kept distinct, the `N/A — no artifact pass-NN` convention, the re-review status enum, and an optional reviewer-guard-status field (reviewer execution guard / effort / model + model-source / reviewer-safe posture / review-engine identity) that separates the observability source of each item (at D1 time: effort run-facts from Batch B; engine identity from operator invocation knowledge; model / model-source and reviewer-safe posture were then pending runner run-fact wiring — since delivered by Batch D2, below, so §6b.2 now reports all of them from runner run-facts). `snippets/claude-skills/ai-harness-review/SKILL.md` step 7 is realigned as the operator mirror (the three axes separated; the prior step-7 conflation removed). §6b is explicitly a human closeout-report schema — **not** the on-disk `result.md` parser contract (§3) and **not** a `scripts/review-verify.ps1` gate; the parser surface is unchanged (no new semantic gate, user-decision 1) and no concrete model version is added to durable docs (it stays the `config/reviewer.json` source-of-truth). Reviewer-safe stays tested-vectors-only (not a blanket guarantee). Docs-only: no source/script/test/config change. Batch D2 (runner run-fact expansion) and D3 (final-report surfacing) followed and are implemented below.
- **Batch D2 implemented (runner run-fact expansion):** `scripts/review-run.ps1` success output now also emits, alongside the Batch B effort run-facts, the resolved reviewer `model` + `model-source` (the actual resolver branch — `explicit` for `-Model`, `config` for `config/reviewer.json`; `Get-ReviewerModel` now returns both value and source, and a missing/empty model still fails fast with no built-in fallback), a `reviewer-safe-posture` run-fact derived from the structural flags actually passed (`--ask-for-approval never --sandbox read-only --ignore-user-config web_search=disabled` — posture flags only, not a blanket guarantee; the tested-vectors-only caveat stays in `docs/policies/REVIEWER_CONFIG_POLICY.md` and the operator final report), and the engine identity `tool-root` / `project-root` / `tool-root-source`. These are single-invocation H1 run-facts for operator debugging; the runner still does not emit H2 final-report fields (perspective coverage, invocation / artifact-pass / corrective-loop counts, commit/push recommendation). No concrete model version is added to durable docs/scripts — the runtime-resolved model value is printed to stdout only. `tests/review-run.Tests.ps1` adds AC-RR21–24 (config model + `model-source: config`; explicit `-Model` + `model-source: explicit`; reviewer-safe-posture flags; engine identity) and keeps the Batch B effort tests and the missing-model fail-fast (AC-RR19); 24/24 pass. Parser surface unchanged (`review-verify.ps1` / `review-input-verify.ps1` untouched). Batch D3 (surfacing those run-facts in the operator final report) is implemented below.
- **Batch D3 implemented (H1 run-fact → H2 final-report surfacing, docs-only):** the final-report schema now states how the operator surfaces the Batch D2 run-facts (effort 3-tuple, model / model-source, reviewer-safe posture, engine identity) into the **reviewer guard status** field of the human final report. `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §6b.2 adds a "Surfacing (Batch D3 — H1 run-fact → H2 final report)" note and §6b.1 reframes the reviewer-guard-status field: surfacing is **recommended whenever the runner emitted the run-facts** (the normal post-D2 case), optional for a trivial non-self-modification review, and **expected for a review-subsystem self-modification closeout** (engine identity + reviewer-safe posture + applied-effort), pairing with the §5a.7 engine-disclosure duty. `snippets/claude-skills/ai-harness-review/SKILL.md` step 7 mirrors this. This is operator reporting discipline, **not** a parser/gate change — `review-verify.ps1` / `review-input-verify.ps1` are unchanged and enforce nothing here (the §6b H1/H2 boundary holds). Caveats preserved: reviewer-safe stays tested-vectors-only / not-blanket; no concrete model version enters durable docs (the runtime-resolved value is cited); engine paths are operator-debugging facts, not source-of-truth claims; the verdict is not commit/push approval. Docs-only: no `scripts/review-run.ps1`, tests, or parser/gate change (D2 already wired the emission). SKILL.md stays a self-contained deployed surface (no repo-doc path / `§N` pointer reintroduced).
- **Batch D mainline complete (closeout):** the Batch D spec's recommended split — D1 (final-report schema operative home + SKILL mirror) → D2 (runner run-fact emission) → D3 (H1 run-fact → H2 final-report surfacing) — is fully implemented, dual-perspective reviewed on the global stable ToolRoot engine (per §5a.7), committed (spec `843b913`; D1 + deployed-surface hygiene `441886d`; D2 `065cbea`; D3 `b3a9d86`), pushed, and globally activated. The spec (`REVIEW_POLISHING_BATCH_D_SPEC.md` §4) defines no further mainline sub-step — there is no D4. Remaining items are non-mainline polishing candidates tracked outside this ledger as deferred/optional (not Batch D blockers): review-input framing-tilt / hypothesis-form `## Known concerns` convention; validation reproducibility inside the reviewer's read-only sandbox (Pester / `verify-ps1` / `git check-attr` not re-runnable there); runner-stdout test exact-line-anchor discipline; optional mirror propagation to `OPERATOR_GUIDE_KR.md` / `README.md` / `templates/`; and the repo-wide `.md` EOL convention (committed `.md` blobs are CRLF while `.gitattributes` says `eol=lf`). The review subsystem stays in maintenance mode — no new mainline review-system work without a separate scoped approval.
- **Reviewer runtime provenance in the result artifact (planning/design spec, not implemented):** `docs/systems/review/REVIEW_RESULT_PROVENANCE_SPEC.md` — design spec for recording runtime-observed reviewer provenance (reviewer adapter kind / version / model / requested+applied effort / reviewer-safe posture) so `result.md` alone is self-describing. The design is adapter-neutral: provenance values come from config / the active reviewer adapter / runtime observation / reviewer self-report (never caller declaration), and no concrete vendor/tool/version is baked into a durable rule. Inspection findings: Batch D2 run-facts are stdout-only (not persisted in the canonical record) and `result.md` is authored by the active reviewer adapter (the current MVP adapter, codex, via its result-output mechanism), while the reviewer adapter's **version** is not observed at all today. The spec proposes input.md-vs-result.md responsibility separation (provenance is runtime observation, not caller declaration → result side, not input), a runner-appended provenance block inside `result.md` (no sidecar; parser surface unchanged), and a P2 (adapter kind+version observation) → P3 (result.md provenance persist + contract amend) → P4 (operator surfacing) split — each a separate scoped goal + review gate (§5a.7 global stable engine) + commit/push approval. Concrete tool/version names appear only as current-adapter inspection facts or the historical motivating incident, not as durable schema. Open candidate row: `docs/systems/review/BACKLOG.md` RV-B-06. It is a spec, not an implementation or operational claim; Batch D is not reopened (no D4).

## Accepted residual risks (LTS maintenance)

The following residual risks were surfaced during the LTS closeout and are explicitly accepted as part of LTS maintenance posture. Reopen requires concrete new evidence beyond the current pattern.

- **H1 preamble / Counter-argument surface alignment** (closed by Stage 4-R1) — the prior dual-surface tension (H1 preamble strongest-case-against pointing to `## Notes` vs `REVIEW_RESULT_CONTRACT.md §3c` making `## Counter-argument` the dedicated surface) was disclosed and user-accepted at Batch 1A entry. Stage 4-R1 (this batch) aligned the H1 reviewer-mode preamble in `scripts/review-run.ps1` to direct strongest-case-against substance to `## Counter-argument`; `## Counter-argument` remains optional / strongly-recommended / non-parser (no Option A parser-required escalation). Option A (parser-required `## Counter-argument`) is not introduced; `REVIEW_RESULT_CONTRACT.md` §3c.5 records it as a §3c non-goal, and no active source-managed escalation threshold exists.
- **Operator narrow-scope over-confidence pattern** (recurrence; dual nature — operator failure mode + review-system effectiveness evidence) — Batch 1A pass-02 within-file enumeration, IDEAS governance citation provenance, and v2/v3 polishing-inventory count drift are documented recurrences. In each case Codex independent review mechanically caught the operator scope-narrowing before it could land as final accepted state (via `Get-ChildItem` for filesystem count, `rg -n` for citation provenance, and grep for within-file enumeration); the recurrence is operator-side failure mode while the catch is review-system effectiveness evidence — both framings hold simultaneously in LTS maintenance. Residual: operator can still create wrong inputs before Codex review runs. Mitigation: Codex independent review (primary) + operator-side discipline codified in `snippets/claude-skills/ai-harness-review/SKILL.md` step 4 (citation provenance verify before quoting + mechanical count check + grep-based exhaustiveness) + `REVIEW_RESULT_CONTRACT.md §5a.4` retraction protocol when discovered ex-post. No script-side enforcement (governance plan D12 not-implementable-yet).
- **Bash chain final-exit-code false-fail** (operator reporting convention) — when a `;`-chained shell line ends with a command whose non-zero exit is informational (e.g., absence-check `ls` of a directory expected to be missing), the chain's final exit code can mask the success of the preceding `review-verify` call. Harness-level mitigation is out of toolset scope (the toolset does not own the shell-chain reporter). Operator convention: when reporting a `review-verify` result, do not chain it before an informational command whose non-zero exit would override the success exit.
