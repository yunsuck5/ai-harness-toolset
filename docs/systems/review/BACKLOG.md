# review — backlog (open candidates)

Open, not-yet-started review-subsystem candidates. **This file is the open-work entrypoint for the review subsystem** — each row (ID + candidate + direction note) is the triage-level entry, sufficient to scope a future goal. The original full analysis of each item is preserved as historical provenance in `docs/archive/backlog/` (not a current dependency). None of these is approved for implementation; each needs a separate scoped goal + review.

| ID | Open candidate | Direction (triage) | Historical provenance |
|---|---|---|---|
| RV-B-01 | Review 2-pass / profile for user-facing instruction text | trial/profile concept; not adopted — decide whether a second profiling pass adds value | `docs/archive/backlog/review.md` |
| RV-B-02 | `timeoutSeconds` enforcement decision debt | decide: enforce / demote-to-metadata-only / remove (`docs/policies/REVIEWER_CONFIG_POLICY.md` currently defers it; it is metadata-only/unenforced) | `docs/archive/backlog/operations.md` |
| RV-B-03 | Review result wrapper / fence artifact hygiene | artifact-level (`result.md` on-disk shape). When Codex `--output-last-message` dumps the reviewer's last message, a fenced/wrapper rendering can appear (e.g. if the Codex sandbox rejects a write tool). Decide whether to normalize that fence/wrapper rendering. NOT a change to the `## Verdict` shape contract. | `docs/archive/backlog/operations.md` |
| RV-B-04 | Review subsystem no-exec / no-write reviewer contract | contract-level reviewer-role boundary: explicitly frame what the reviewer does **not** do (no exec, no write). Distinct from RV-B-03 (artifact-level); this is the role contract. NOT a change to the `## Verdict` shape. **R1 first batch closed (commit `2997bb3`)**: Markdown evidence convention adopted — `## Validation evidence` informational input section + reviewer-readable runtime supporting material boundary (not command re-execution, not deterministic truth oracle, not freshness binding, not source-of-truth) — in contracts / templates / reviewer skill / snippets. **Deferred (R1-specific)**: evidence file freshness / hash / mtime binding, deterministic validation runner, automatic validation execution, JSON schema for evidence, evidence-section presence / sub-shape script lint. | `docs/archive/backlog/operations.md` |
| RV-B-05 | Review input governance | input.md / result.md wording, informational dimensions, and verdict semantics governance. Adjacent to RV-B-04 (R1) but separate: this row tracks reviewer-side disclosure shape and operator-side honest-framing conventions. **First batch closed (commit `04176ad`)**: verdict narrowing (`yes` = no blocking finding, `no` = blocking finding exists, `yes with risk` = blocking-free but disclosed risks need explicit supervisor/user acceptance — not the automatic equivalent of `yes`); 4 result disclosure sections introduced as recommended at the time of this batch (`## Blocking findings`, `## Non-blocking concerns`, `## Review limitations`, `## Assumptions relied on`) — later promoted to parser-required by the fourth batch below; `## Known concerns` informational input section + stale-by-omission rule; neutral question and framing-tilt self-audit guidance; placeholder token literal citation operator conventions (brace-less / wildcard / backslash-escape, three mechanical-safe forms); deployable payload contamination cleanup (templates / snippets / reviewer skill carry self-contained wording, no repo-local docs path or source-of-truth refs). **Second batch closed (commit `bae32fe`)**: placeholder-citation convention (c) backslash-escape mechanical guarantee regression-locked via Pester (`AC-IV7` / `AC-IV8` / `AC-IV9` in `tests/review-input-verify.Tests.ps1`); template + snippet mechanism wording corrected to the "no two adjacent `{` characters in raw file bytes" explanation, aligned across `templates/review-input.md` and `snippets/claude-skills/ai-harness-review/SKILL.md`; no script change. **Third batch closed (commit `8eb310a`)**: P4 (reviewer-side) + O1 (operator-side) mechanical-behavior-claim verification guidance — when the verdict depends on a claim about how a specific regex / parser / verifier / script actually behaves on specific input, the reviewer performs a minimal reproducible check where feasible in its read-only sandbox (recording results under `## Notes` / `## Assumptions relied on`, or sandbox infeasibility under `## Review limitations`), and the operator verifies such claims with a similarly narrow check before stating them in input.md / templates / snippets / contracts; wording in contract §3b + §5 item 7, deployable skill step 4 + step 6, and `templates/review-result.md` `## Review limitations` + `## Assumptions relied on` example lists. **Fourth batch closed (commit `107eadc`)**: V2 / A.1 — parser-required H2 enforcement of the 4 result disclosure sections in `scripts/review-verify.ps1 -RequireResult`. New `Test-DisclosureSections` function requires each of `## Blocking findings`, `## Non-blocking concerns`, `## Review limitations`, `## Assumptions relied on` to appear exactly once via case-sensitive line equality (-ceq); fails on count 0 (missing) or count > 1 (duplicate). `Test-VerdictShape` heading-position check also switched to -ceq for file-wide consistency. 8 new Pester cases (AC-VF-RR11..AC-VF-RR18) lock baseline, each-one-missing, duplicate, and wrong-case regressions for both the disclosure H2s and the `## Verdict` heading. Mechanical presence/count/case check only — no Markdown parser, no semantic check, no sub-shape lint. Contract §3 / §4 item 6 / §4a updated to mark the 4 H2s as required and to add the new gate (count 6 → 7); templates/review-result.md and snippets/claude-skills/ai-harness-review/SKILL.md (step 4 operator instruction + step 6 verify description) updated to surface the new requirement to the operator. **Deferred (governance)**: `## Known concerns` sub-shape lint, automated leading-question lint, framing-tilt detection lint, devil's advocate pre-pass orchestration, script-side placeholder token escape mechanism beyond convention (c) — specifically the bare two-brace `{{...}}` honest-citation false-fail (when the operator wants to cite the bare form in prose for documentation purposes); convention (c) backslash-escape mechanical guarantee is regression-locked by `AC-IV7`, so this deferred item is narrowed to the bare-form false-fail and any broader script-side escape recognition, `log/review_polishing/` collected items processing (some items absorbed by the V1 and P4/O1 batches; queue stays open as a long-running channel for future polishing signals), verdict vocabulary migration beyond the current narrowing, reviewer-mode preamble in `scripts/review-run.ps1` carrying the V2 4-H2 requirement directly (currently each operator-authored `input.md` must state it) and the consistency gap that `scripts/review-input-verify.ps1` heading checks still use case-insensitive `-eq` (out of scope of the V2 batch). | `docs/archive/backlog/operations.md` |

Closed review-subsystem items are recorded in `docs/systems/review/STATUS.md` (completed-ledger). Removed-legacy review artifacts (review-cycle / `meta.json` / `result.json` / `target-files.list` / `<run-id>` flat layout) are historical-reason only in `docs/archive/backlog/review.md` / `operations.md`, never operator paths.
