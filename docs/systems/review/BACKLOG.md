# review — backlog (open candidates)

Open, not-yet-started review-subsystem candidates. **This file is the open-work entrypoint for the review subsystem** — each row (ID + candidate + direction note) is the triage-level entry, sufficient to scope a future goal. The original full analysis of each item is preserved as historical provenance in `docs/archive/backlog/` (not a current dependency). None of these is approved for implementation; each needs a separate scoped goal + review.

| ID | Open candidate | Direction (triage) | Historical provenance |
|---|---|---|---|
| RV-B-01 | Review 2-pass / profile for user-facing instruction text | trial/profile concept; not adopted — decide whether a second profiling pass adds value | `docs/archive/backlog/review.md` |
| RV-B-02 | `timeoutSeconds` enforcement decision debt | decide: enforce / demote-to-metadata-only / remove (`docs/policies/REVIEWER_CONFIG_POLICY.md` currently defers it; it is metadata-only/unenforced) | `docs/archive/backlog/operations.md` |
| RV-B-03 | Review result wrapper / fence artifact hygiene | artifact-level (`result.md` on-disk shape). When Codex `--output-last-message` dumps the reviewer's last message, a fenced/wrapper rendering can appear (e.g. if the Codex sandbox rejects a write tool). Decide whether to normalize that fence/wrapper rendering. NOT a change to the `## Verdict` shape contract. | `docs/archive/backlog/operations.md` |
| RV-B-04 | Review subsystem no-exec / no-write reviewer contract | contract-level reviewer-role boundary: explicitly frame what the reviewer does **not** do (no exec, no write). Distinct from RV-B-03 (artifact-level); this is the role contract. NOT a change to the `## Verdict` shape. **R1 first batch closed (commit `2997bb3`)**: Markdown evidence convention adopted — `## Validation evidence` informational input section + reviewer-readable runtime supporting material boundary (not command re-execution, not deterministic truth oracle, not freshness binding, not source-of-truth) — in contracts / templates / reviewer skill / snippets. **Deferred (R1-specific)**: evidence file freshness / hash / mtime binding, deterministic validation runner, automatic validation execution, JSON schema for evidence, evidence-section presence / sub-shape script lint. | `docs/archive/backlog/operations.md` |
| RV-B-05 | Review input governance | input.md / result.md wording, informational dimensions, and verdict semantics governance. Adjacent to RV-B-04 (R1) but separate: this row tracks reviewer-side disclosure shape and operator-side honest-framing conventions. **First batch closed (commit `04176ad`)**: verdict narrowing (`yes` = no blocking finding, `no` = blocking finding exists, `yes with risk` = blocking-free but disclosed risks need explicit supervisor/user acceptance — not the automatic equivalent of `yes`); 4 recommended result disclosure sections (`## Blocking findings`, `## Non-blocking concerns`, `## Review limitations`, `## Assumptions relied on`); `## Known concerns` informational input section + stale-by-omission rule; neutral question and framing-tilt self-audit guidance; placeholder token literal citation operator conventions (brace-less / wildcard / backslash-escape, three mechanical-safe forms); deployable payload contamination cleanup (templates / snippets / reviewer skill carry self-contained wording, no repo-local docs path or source-of-truth refs). **Deferred (governance)**: parser-required H2 enforcement of the 4 disclosure sections, `## Known concerns` sub-shape lint, automated leading-question lint, framing-tilt detection lint, devil's advocate pre-pass orchestration, script-side placeholder token escape mechanism, `log/review_polishing/` collected items processing, verdict vocabulary migration beyond the current narrowing. | `docs/archive/backlog/operations.md` |

Closed review-subsystem items are recorded in `docs/systems/review/STATUS.md` (completed-ledger). Removed-legacy review artifacts (review-cycle / `meta.json` / `result.json` / `target-files.list` / `<run-id>` flat layout) are historical-reason only in `docs/archive/backlog/review.md` / `operations.md`, never operator paths.
