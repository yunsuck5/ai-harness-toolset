# review — backlog (open candidates)

Open, not-yet-started review-subsystem candidates. **This file is the open-work entrypoint for the review subsystem** — each row (ID + candidate + direction note) is the triage-level entry, sufficient to scope a future goal. The original full analysis of each item is preserved as historical provenance in `docs/archive/backlog/` (not a current dependency). None of these is approved for implementation; each needs a separate scoped goal + review.

| ID | Open candidate | Direction (triage) | Historical provenance |
|---|---|---|---|
| RV-B-01 | Review 2-pass / profile for user-facing instruction text | trial/profile concept; not adopted — decide whether a second profiling pass adds value | `docs/archive/backlog/review.md` |
| RV-B-02 | `timeoutSeconds` enforcement decision debt | decide: enforce / demote-to-metadata-only / remove (`docs/policies/REVIEWER_CONFIG_POLICY.md` currently defers it; it is metadata-only/unenforced) | `docs/archive/backlog/operations.md` |
| RV-B-03 | Review result wrapper / fence artifact hygiene | artifact-level (`result.md` on-disk shape). When Codex `--output-last-message` dumps the reviewer's last message, a fenced/wrapper rendering can appear (e.g. if the Codex sandbox rejects a write tool). Decide whether to normalize that fence/wrapper rendering. NOT a change to the `## Verdict` shape contract. | `docs/archive/backlog/operations.md` |
| RV-B-04 | Review subsystem no-exec / no-write reviewer contract | contract-level reviewer-role boundary: explicitly frame what the reviewer does **not** do (no exec, no write). Distinct from RV-B-03 (artifact-level); this is the role contract. NOT a change to the `## Verdict` shape. **R1 first batch closed (commit `2997bb3`)**: Markdown evidence convention adopted — `## Validation evidence` informational input section + reviewer-readable runtime supporting material boundary (not command re-execution, not deterministic truth oracle, not freshness binding, not source-of-truth) — in contracts / templates / reviewer skill / snippets. **Deferred (R1-specific)**: evidence file freshness / hash / mtime binding, deterministic validation runner, automatic validation execution, JSON schema for evidence, evidence-section presence / sub-shape script lint. | `docs/archive/backlog/operations.md` |
| RV-B-05 | Review input governance | input.md / result.md wording, informational dimensions, and verdict semantics governance. Adjacent to RV-B-04 (R1) but separate: this row tracks reviewer-side disclosure shape and operator-side honest-framing conventions. **First batch closed (commit `04176ad`)**: verdict narrowing (`yes` = no blocking finding, `no` = blocking finding exists, `yes with risk` = blocking-free but disclosed risks need explicit supervisor/user acceptance — not the automatic equivalent of `yes`); 4 result disclosure sections introduced as recommended at the time of this batch (`## Blocking findings`, `## Non-blocking concerns`, `## Review limitations`, `## Assumptions relied on`) — later promoted to parser-required by the fourth batch below; `## Known concerns` informational input section + stale-by-omission rule; neutral question and framing-tilt self-audit guidance; placeholder token literal citation operator conventions (brace-less / wildcard / backslash-escape, three mechanical-safe forms); deployable payload contamination cleanup (templates / snippets / reviewer skill carry self-contained wording, no repo-local docs path or source-of-truth refs). **Second batch closed (commit `bae32fe`)**: placeholder-citation convention (c) backslash-escape mechanical guarantee regression-locked via Pester (`AC-IV7` / `AC-IV8` / `AC-IV9` in `tests/review-input-verify.Tests.ps1`); template + snippet mechanism wording corrected to the "no two adjacent `{` characters in raw file bytes" explanation, aligned across `templates/review-input.md` and `snippets/claude-skills/ai-harness-review/SKILL.md`; no script change. **Third batch closed (commit `8eb310a`)**: P4 (reviewer-side) + O1 (operator-side) mechanical-behavior-claim verification guidance — when the verdict depends on a claim about how a specific regex / parser / verifier / script actually behaves on specific input, the reviewer performs a minimal reproducible check where feasible in its read-only sandbox (recording results under `## Notes` / `## Assumptions relied on`, or sandbox infeasibility under `## Review limitations`), and the operator verifies such claims with a similarly narrow check before stating them in input.md / templates / snippets / contracts; wording in contract §3b + §5 item 7, deployable skill step 4 + step 6, and `templates/review-result.md` `## Review limitations` + `## Assumptions relied on` example lists. **Fourth batch closed (commit `107eadc`)**: V2 / A.1 — parser-required H2 enforcement of the 4 result disclosure sections in `scripts/review-verify.ps1 -RequireResult`. New `Test-DisclosureSections` function requires each of `## Blocking findings`, `## Non-blocking concerns`, `## Review limitations`, `## Assumptions relied on` to appear exactly once via case-sensitive line equality (-ceq); fails on count 0 (missing) or count > 1 (duplicate). `Test-VerdictShape` heading-position check also switched to -ceq for file-wide consistency. 8 new Pester cases (AC-VF-RR11..AC-VF-RR18) lock baseline, each-one-missing, duplicate, and wrong-case regressions for both the disclosure H2s and the `## Verdict` heading. Mechanical presence/count/case check only — no Markdown parser, no semantic check, no sub-shape lint. Contract §3 / §4 item 6 / §4a updated to mark the 4 H2s as required and to add the new gate (count 6 → 7); templates/review-result.md and snippets/claude-skills/ai-harness-review/SKILL.md (step 4 operator instruction + step 6 verify description) updated to surface the new requirement to the operator. **Fifth batch closed (commit `dc1cad0`)**: H1 — V2 reviewer-instruction propagation. The reviewer-mode preamble here-string in `scripts/review-run.ps1` now names the 4 V2 H2s as required-each-exactly-once with `none`-body convention so Codex receives the requirement on every invocation regardless of operator-authored input.md content. `templates/review-input.md` `## Final verdict` section seed aligned with the same wording in Korean. `tests/review-run.Tests.ps1` AC-RR11 (preamble-injection probe) extended with presence + preamble-position assertions for the 4 H2 literals using the `===== BEGIN REVIEW INPUT` anchor. **Sixth batch closed (commit `a163c77`)**: H2 — heading case-sensitivity completion. `scripts/review-input-verify.ps1` required-H2 heading match and `scripts/review-run.ps1` `Get-VerdictFromResultMd` `## Verdict` heading detection both switched from case-insensitive `-eq` to case-sensitive `-ceq`, mirroring the V2 corrective in `scripts/review-verify.ps1`. AC-IV10 locks the input-verify case-sensitive heading match; new stub mode `verdict-lowercase-heading` + AC-RR12 lock the review-run verdict-heading case-sensitive detection. After this batch all three source-controlled heading match sites in the review subsystem use `-ceq`. **Seventh batch closed (commit `55b0913`)**: V1 / I2 — active-placeholder grammar narrowing. `scripts/review-input-verify.ps1` placeholder regex narrows from generic `\{\{[A-Za-z_][A-Za-z0-9_]*\}\}` to active-namespace `\{\{AI_TO_FILL_[A-Za-z0-9_]+\}\}`, with the error message updated to "FAIL unreplaced active placeholder". The 9 active placeholders in `templates/review-input.md` (all `AI_TO_FILL_*` prefixed) remain caught when unfilled; generic `{{TOKEN}}` / `{{example}}` literals now pass freely as documentation literals. `tests/review-input-verify.Tests.ps1` rewrites AC-IV4 / AC-IV5 / AC-IV8 / AC-IV9 to use `AI_TO_FILL_*` fixtures with the updated assertion text; drops AC-IV7 (the legacy convention (c) backslash-escape regression-lock) because that convention is no longer a maintained operator contract; adds AC-IV11 (generic-in-prose passes), AC-IV12 (generic-in-backticks passes — proves NOT a Markdown exemption), AC-IV13 (active-in-backticks still fails — proves the verifier does not exempt backticks), AC-IV14 (mixed-form fails on the active placeholder). `docs/contracts/review/REVIEW_RESULT_CONTRACT.md`, `snippets/claude-skills/ai-harness-review/SKILL.md`, `templates/review-input.md`, and `docs/user_guide/OPERATOR_GUIDE_KR.md` updated to describe the new grammar; the three legacy operator conventions (brace-less / wildcard / backslash-escape) are obsoleted as a maintained operator contract (brace-less identifier retained only as the citation mechanism when referring to a specific active placeholder by name). The `AI_TO_FILL_` prefix is the new active-placeholder safety invariant — future active review-input placeholders MUST use this prefix. No Markdown parser, no inline-code-span exemption, no fenced-code-block exemption, no HTML-comment exemption was introduced. The prior inline-code-span exemption proposal (planning artifact `polishing/review/complete/rv-b-05-v1-script-side-escape-retriage-parallel-analysis-20260526.md`) was superseded by the grammar-narrowing decision (parallel-blind merged in `polishing/review/complete/rv-b-05-v1-placeholder-grammar-design-addendum-parallel-analysis-20260526.md`). BRIEF subsystem (`scripts/brief-check.ps1`, `docs/contracts/brief/BRIEF_CONTRACT.md`, `tests/brief-check.Tests.ps1`) is orthogonal — separate gate, separate file scope — and was not changed. Recurrence framing preserved as historical record: pre-addendum count was 3 source-repo dogfooding bare-form pre-Codex verifier rejections (R1, V1, H2 pass-01 polishing logs); the design addendum's own pass-01 produced a 4th instance during meta-analysis, recorded as `log/review_polishing/rv-b-05-v1-placeholder-grammar-addendum-20260526/pass-01.md`; the carved-out RV-B-05 first-batch impl pass-01 polishing log remains a different friction class (operator wrong-reasoning self-correction caught by Codex at verdict-determination, not a pre-Codex bare-form verifier rejection) and is still not counted. Note on deployment: the channel 3 global stable install at `%USERPROFILE%\.claude\ai-harness-toolset\current\scripts\review-input-verify.ps1` may still carry the pre-I2 generic regex until a separate global refresh / apply step is taken; that refresh is a post-source-mutation boundary outside this batch's scope and is not implied by this commit. **Eighth batch (Review System LTS Phase 1 Batch II — light P3 wording + §5a operator stance)**: contract new `§5a Operator stance and discipline` codifies five operator-stance rules — 5a.1 target file accuracy verification, 5a.2 off-repo / reference material handling (inline body verbatim when sandbox cannot read repo-outside paths), 5a.3 stop/report vs self-correct boundary (source / runtime / sibling / user-global / global-install / commit-push boundaries), 5a.4 retraction / correction reporting protocol (pair of §7 stale-by-omission, in-progress / post-discovery side), 5a.5 source / runtime / sibling-report / user-global scope discipline. `snippets/claude-skills/ai-harness-review/SKILL.md` targeted gap-filling: step 2 (target-file accuracy verify), step 4 (off-repo material + reviewer P3 framing-pressure mirror), step 5 (stop/report-vs-self-correct boundary at run time), step 7 (retraction / correction reporting). Light P3 wording added to `scripts/review-run.ps1` H1 reviewer-mode preamble — one bullet between the V2 4-H2 disclosure requirement and the insufficient-input handling line, instructing the reviewer to articulate the strongest case AGAINST its own conclusion in `## Notes` as a pressure-test before issuing the final verdict (no new required H2; no parser gate; light wording-level guidance only). `tests/review-run.Tests.ps1` AC-RR11 preamble-injection probe extended with `strongest case AGAINST` + `pressure-test` substring assertions positioned before the `===== BEGIN REVIEW INPUT` marker, locking the P3 wording into the preamble against future drift. Tier D managed-block snippets (`snippets/CLAUDE_SNIPPET.md`, `snippets/AGENTS_SNIPPET.md`) carry C-lite minimal `## Operator stance` guard only — scope discipline + retraction one-line plus cross-reference to SKILL / contract — and do not duplicate the full §5a 5-rule table (user-decided long-term migration direction = mapping source-of-truth lives in skill / contract / scripts / future hooks, not in Tier D snippets). No new verdict token; no V1/I2 placeholder grammar change; no V2 4-H2 parser enforcement change; no V3 `## Known concerns` sub-shape lint introduction; no scripts/review-run.ps1 behavior change beyond the preamble prose addition. **Deferred (Phase 2 candidates, this batch deliberately does not introduce them)**: `## Counter-argument` H2 (parser-required new section), devil's advocate pre-pass orchestration (runtime cost 2x + contract complexity), multi-reviewer consensus, vertical-vs-horizontal review mode distinction — these heavy P3 mechanisms remain deferred as Phase 2 candidates per anchor `polishing/review/review_lts_phase1_execution_anchor_20260527.md` §10. **Deferred (governance)**: `log/review_polishing/` collected items processing (some items absorbed by the V1 and P4/O1 batches; queue stays open as a long-running channel for future polishing signals), verdict vocabulary migration beyond the current narrowing. The "script-side placeholder token escape mechanism" deferred item is removed from this list because it is closed by the seventh batch. **Rejected as deterministic-lint scope (P1/P2/V3)**: automated leading-question lint (P1), framing-tilt detection lint (P2), and `## Known concerns` sub-shape lint (V3) are not planned as script gates. (P1/P2) Parallel-blind analysis on 2026-05-27 (recorded in `polishing/review/complete/rv-b-05-p1-p2-input-framing-scope-parallel-analysis-20260527.md`) found no source-repo dogfooding case where a leading question or framing tilt caused an unsound verdict, and current protections already cover the intent: neutral review-question phrasing guidance, framing-tilt self-audit recommendation, `## Known concerns`, stale-by-omission, V2 required disclosure H2s, H1 preamble propagation, P4/O1 mechanical-behavior-claim verification, and I2 grammar narrowing reducing unrelated operator friction. Regex/string lint for Korean/English mixed operator prose would be brittle, high false-positive, and not script-tractable without semantic judgment, so it is rejected under the deterministic-gate-only contract posture and the false-positive maintenance cost would be disproportionate. (V3) `## Known concerns` sub-shape lint — deterministic check of the recommended sub-categories (convention deviation / skipped alternatives / validation limitations / baseline failures / direct verification not performed / operator assumptions) inside the informational `## Known concerns` section body — is rejected on the same P1/P2 posture: operator honesty invariant + §7 stale-by-omission rule + supervisor judgment is the currently effective handling path, and regex/string sub-category lint without semantic judgment would carry the same brittleness and false-positive maintenance cost. Anchor `polishing/review/review_lts_phase1_execution_anchor_20260527.md` §8 V3 (merged with F1) records this rejection. The contract reflects it in `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §10 non-goals, parallel to the existing `## Validation evidence` sub-shape lint rejection. This three-way rejection does not claim that input-bias / framing risk / disclosure-shape risk is permanently solved — it is the current handling path. Reopen only on concrete evidence that a leading question, framing tilt, or sub-category disclosure omission / misformatting caused an unsound verdict that existing self-audit / disclosure mechanisms did not catch. **Ninth batch closed (commit `76033f4`, Phase 2 Batch 1A — Counter-argument H2 Option B codification)**: `## Counter-argument` H2 added to result.md convention as **optional, strongly-recommended, non-parser** (`docs/contracts/review/REVIEW_RESULT_CONTRACT.md §3c`). 4 source surfaces touched: contract §3c + `templates/review-result.md` + `templates/review-input.md` (`## Final verdict` body adds reviewer instruction to articulate `## Counter-argument` when verdict is `yes` / `yes with risk`) + `snippets/claude-skills/ai-harness-review/SKILL.md` step 4 + step 6. Parser surface unchanged — `scripts/review-verify.ps1 -RequireResult` still enforces only the four V2 H2s. H1 reviewer-mode preamble unchanged in this Ninth batch — the dual-surface tension between H1 preamble (`## Notes` strongest-case-against) and §3c (`## Counter-argument` dedicated surface) was **user-accepted residual risk** for LTS maintenance at the time of this batch (subsequently closed by the Eleventh batch's Stage 4-R1 part — H1 preamble runtime instruction realigned to direct strongest-case-against to `## Counter-argument` per §3c); Option A (parser-required) escalation criteria remain codified in `REVIEW_RESULT_CONTRACT.md §3c.5`. Batch 1B mirror surfaces (`docs/user_guide/OPERATOR_GUIDE_KR.md` + `README.md` + Tier D managed-block snippets) are handled in the Stage 4 LTS closeout batch separately — they are not parser-required and do not block reviewer reception. The "Phase 2 candidates remain deferred" framing in this row's Eighth-batch text is a Phase 1 Batch II historical snapshot and was accurate at that time; the current per-candidate state is: Counter-argument H2 Option B = closed (this row's Ninth batch), Counter-argument H2 Option A = deferred-until-evidence (`REVIEW_RESULT_CONTRACT.md §3c.5`), devil's advocate pre-pass + multi-reviewer consensus = idea-only (`docs/systems/review/IDEAS.md`), vertical-vs-horizontal review mode = later candidate (not idea-only, not deferred-until-evidence; Phase 2 Implementation Plan §10.4 advisory). **Tenth batch closed (commit `37a895c`, IDEAS.md governance — idea-only durability mitigation)**: new source-managed governance doc `docs/systems/review/IDEAS.md` codifies devil's advocate pre-pass orchestration and multi-reviewer consensus / post-merge calibration loop as **idea-only items** (not planned work, not implementation backlog, not deferred implementation scope, no planning/design/implementation without separate explicit user decision). Each idea-only item is documented with what-it-is / why-might-useful / why-not-planned / reopen-criteria. Counter-argument H2 Option A and vertical-vs-horizontal mode are explicitly separated from idea-only class in `IDEAS.md` "What is NOT in this document" section. **Eleventh batch closed (Stage 4 + Stage 4-R1 + Stage 4-R2 — LTS closeout source-side completion)**: Stage 4 (docs-only) — this BACKLOG row update + STATUS.md completed-ledger rows RV-05/06/07 + accepted residual risks section + Batch 1B mirror surfaces (`OPERATOR_GUIDE_KR.md` / `README.md` / Tier D snippets) C-lite mirror of `§3c` convention + small `SKILL.md` operator-discipline additions (off-repo read-first guidance, citation provenance verify, mechanical count check) addressing the three remaining `log/review_polishing/` root queue signals. Stage 4-R1 (runtime instruction alignment) — `scripts/review-run.ps1` H1 reviewer-mode preamble redirected strongest-case-against directive from `## Notes` to `## Counter-argument` (§3c dedicated pressure-test surface, with short-literal fallback to avoid ceremonial boilerplate) + `## Counter-argument` added to the optional-sections enumeration line; `tests/review-run.Tests.ps1` AC-RR11 extended with `## Counter-argument` substring + position-before-marker assertions locking the new preamble against future drift; `STATUS.md` Accepted residual risks reworded — Risk 1 (H1 preamble / Counter-argument surface alignment) marked closed by Stage 4-R1, Risk 2 (operator narrow-scope over-confidence) reclassified as dual EFFECTIVENESS_EVIDENCE + ACCEPTED_RESIDUAL framing (Claude Code operator failure mode + Codex independent review effectiveness evidence — both framings hold simultaneously in LTS maintenance), plus three `polishing/review/...` references annotated as `(repo-sibling)` for path-resolution clarity. Stage 4-R2 (source-side consistency closure on Codex `no` blocking finding) — `snippets/claude-skills/ai-harness-review/SKILL.md` H1 preamble description aligned to the Stage 4-R1 runtime (strongest-case-against → `## Counter-argument`, no more "H1 preamble's wording is unchanged" stale claim); `docs/user_guide/OPERATOR_GUIDE_KR.md` §11 verdict-handling table `yes` row optional sections list now includes `## Counter-argument`; `docs/contracts/review/REVIEW_RESULT_CONTRACT.md` §3c.5 H1 preamble non-goal bullet reframed as Batch 1A historical batch-scope statement with forward annotation to the Stage 4-R1 closure. Parser surface unchanged (`scripts/review-verify.ps1 -RequireResult` still enforces only the 4 V2 H2s — `## Counter-argument` remains optional / strongly-recommended / non-parser); verdict vocabulary unchanged (`yes` / `no` / `yes with risk`); Option A (parser-required `## Counter-argument`) NOT introduced (criteria in `REVIEW_RESULT_CONTRACT.md §3c.5` remain unmet); global propagation (channel 3 install / managed blocks / Claude skill mirror) deliberately not refreshed in this batch — separate explicit user approval boundary. Review System enters LTS maintenance state with source-side closeout complete. The current state of RV-B-05 is no longer "Phase 2 candidates remain deferred" wholesale — `STATUS.md` Current state + Completed ledger + Accepted residual risks are the discoverable current-state surface. | `docs/archive/backlog/operations.md` |

Closed review-subsystem items are recorded in `docs/systems/review/STATUS.md` (completed-ledger). Removed-legacy review artifacts (review-cycle / `meta.json` / `result.json` / `target-files.list` / `<run-id>` flat layout) are historical-reason only in `docs/archive/backlog/review.md` / `operations.md`, never operator paths.
