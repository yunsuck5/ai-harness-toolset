# POST-MVP Completed — detailed closeout narrative (archived)

> Historical / superseded-location material. This is the full commit-bound closeout narrative that was previously inline in `docs/decisions/POST_MVP_PLAN.md` §10 "Completed (source repo side)". It is preserved here verbatim for historical context. **Do not use it as current status** — current status lives in `docs/systems/install-update/STATUS.md`, `docs/systems/review/STATUS.md`, `docs/systems/brief/STATUS.md` (compact completed-ledgers), and the current summary in `docs/current/PROJECT_STATE.md`. See `docs/archive/README.md` for the archive authority restriction.

## Completed (source repo side) — verbatim narrative

- CLI-only MVP closed (§1).
- Codex review subsystem operational, maintenance mode 진입 (§2).
- Brief 의 narrow source-side primitive (`scripts/brief-init.ps1`, `scripts/brief-check.ps1`, `templates/brief/BRIEF.md`) implementation 완료 (§3, §5). 이는 BF Level 3 capability 의 full implementation 이 아니다 — `docs/contracts/brief/BRIEF_CONTRACT.md` §"BF Level — save/restore capability maturity" 참조.
- `snippets/CLAUDE_SNIPPET.md` / `snippets/AGENTS_SNIPPET.md` 본문이 현행 (3차 reconciliation) framing 으로 정합화 완료 (§3, §5) — canonical Brief = `<ProjectRoot>/log/brief/BRIEF.md`, root `<ProjectRoot>/brief/` rejected, `log/chatlog/current/resume.md` / `summary.md` 가 failed intermediate / legacy migration source / deprecation candidate. BF Level 1/2 manual save 절차의 "operator 는 trigger / approve / reject / discard 주체이고 BRIEF 본문 hand-edit 을 하지 않으며 agent (또는 deterministic tooling) 가 본문을 작성한다" wording 도 함께 정합화되었다. 이미 destination `CLAUDE.md` / `AGENTS.md` 에 적용된 managed block 본문은 그 시점의 snippet 을 가지고 있으며 자동으로 refresh 되지 않는다 — destination refresh 는 사용자 명시 승인이 필요한 별도 managed-block replacement step (`docs/decisions/GLOBAL_ADOPTION_DECISION.md` §6) 이다. source snippet / managed block / docs contract 의 framing 이 충돌하면 docs contract 가 우선이다.
- target payload 측 source-side primitive smoke test 완료 (§3, §5).
- post-MVP review effort / cost 운영 권고 문서 `docs/policies/REVIEW_EFFORT_GUIDE.md` 추가 완료 (§2).
- global adoption operating layer 방향 결정 `docs/decisions/GLOBAL_ADOPTION_DECISION.md` 기록 완료 (§6, §11).
- `snippets/CLAUDE_SNIPPET.md` / `snippets/AGENTS_SNIPPET.md` 에 `AI_HARNESS_TOOLSET_GLOBAL` managed block marker 적용 완료 (`docs/decisions/GLOBAL_ADOPTION_DECISION.md` §6).
- Claude skill 의 global adoption / update / removal 절차 문서화 완료 (`docs/user_guide/GLOBAL_ADOPTION_PROCEDURE.md`, `docs/decisions/GLOBAL_ADOPTION_DECISION.md` §5).
- ToolRoot / ProjectRoot path handling audit 문서화 완료 (`docs/archive/audits/TOOLROOT_PROJECTROOT_AUDIT.md`, `docs/decisions/GLOBAL_ADOPTION_DECISION.md` §8). self-target / dogfooding path collision sub-scope 포함.
- shared / global mode invocation contract design 문서화 완료 (`docs/contracts/global-invocation/SHARED_GLOBAL_INVOCATION_CONTRACT.md`). audit §8 의 D1–D9 결정 및 implementation split 포함.
- shared / global mode implementation 완료. `docs/contracts/global-invocation/SHARED_GLOBAL_INVOCATION_CONTRACT.md` §6 의 8 개 split unit 이 모두 독립 commit 으로 반영됨 — `Get-ToolRoot` channel chain (`bd0ac83`), component script fallback policy (`9130c68`), snippet mode-neutral body (`8234bf1`), SKILL.md script root 분기 (`df09bf5`), review-verify toolRoot binding (`dadff4d`), review-cycle untracked exclusion (`67430c4`), self-target enforcement check (`14ce6c9`), `Get-ProjectRoot` CWD advisory (`bebe7ab`, `043b0e0`). **단, 이 implementation 완료는 source-side 의 path / invocation 동작이 갖춰졌다는 의미이며, actual global activation / install / update / self-adoption 의 수행을 의미하지 않는다 — 후자는 §11 의 remaining order 에 별도 step 으로 남는다.**
- clean target smoke test criteria 정의 완료 (`docs/archive/audits/CLEAN_TARGET_SMOKE_CRITERIA.md`). SC1–SC7 의 pre-condition / action / observable pass / fail / required evidence 정의 포함 (`85433e5` 및 후속 정합 commit `9af5f62`, `24d2010`).
- clean target smoke test full 실행 완료. `docs/archive/audits/CLEAN_TARGET_SMOKE_CRITERIA.md` 의 SC1–SC7 suite 를 source repo 외부 clean target 에서 실행하여 전 케이스 PASS — SC5 는 full Codex CLI dependent path 로 수행되어 SC5' partial substitute 가 아닌 full coverage 다. `<SourceRepoRoot>` read-only invariant (SC7) 는 모든 case 에서 hold 했고 source repo working tree 는 무변경이다. evidence 는 `docs/contracts/evidence/EVIDENCE_CONTRACT.md` / `CLEAN_TARGET_SMOKE_CRITERIA.md` §4 의 source-repo 외부 저장 경계에 따라 **user-managed repo-external backup** 으로 보존되며 source repo 로 import 하지 않는다 (`docs/archive/backlog/operations.md` "Smoke evidence preservation" 의 raw evidence → repo-external 경계와 정합).
- global install / update / validation / self-adoption operating model 문서화 완료 (`docs/systems/install-update/GLOBAL_INSTALL_UPDATE_MODEL.md`). 설치 source 획득 방식 2 종 (install-from-git-url, install-from-local-clone), metadata-dispatched update, global install metadata schema, validation-before-implementation (manual global activation / controlled global materialization), target footprint contract, self-adoption model 을 포함한다. 본 문서는 global install / update / self-adoption 판단의 **current source-of-truth** 다. 단, 문서화 완료가 actual global install / update 의 수행을 의미하지는 않는다.
- canonical review task/pass topology contract alignment 완료 (`a5d94a5`). review artifact topology 의 source-of-truth wording 을 task/pass shape 로 정렬했다 — review record 는 `<ProjectRoot>/log/review/<review-task-id>/pass-NN/input.md` + `result.md` 두 파일이고, 그 외 sidecar JSON / staging directory / hash-binding file 은 normal contract 밖이다. 본 commit 은 contract docs 정렬이며 implementation 행동의 변경을 의미하지 않는다.
- canonical review task/pass topology implementation 완료 (`c81fe45`). `a5d94a5` 의 contract 에 정합하도록 normal operator path 의 script / template / I/O shape 가 갱신되었다. 변경 핵심:
  - normal operator entrypoint 가 `scripts/review-prepare.ps1` → `scripts/review-run.ps1` 두 스텝으로 정착했고, `scripts/review-verify.ps1` 는 post-hoc canonical-artifact check (Codex 미호출) 로 동작한다.
  - canonical review record 는 `<ProjectRoot>/log/review/<review-task-id>/pass-NN/input.md` + `result.md` 두 파일.
  - `scripts/review-cycle.ps1` 가 normal operator path 에서 제거되었다.
  - normal contract 밖의 legacy artifact (`meta.json`, `result.json`, `target-files.list`, `log/review-targets/`, `log/review-requests/`, `-TargetFilesPath`, `-ReviewRequestPath`, `templates/review-meta.json`, 별도 `templates/review-result.json`) 가 제거되었다.
  - 본 변경은 §2 의 maintenance mode 안의 contract clarification + 그에 정합한 implementation 이며, 새 feature / 새 reviewer adapter / multi-reviewer orchestration 의 도입이 아니다.
- 위 canonical review task/pass topology 채택 이후 global Claude install layer 의 payload 가 `c81fe45` 기준으로 refresh 되었다. 구체적으로 (a) global stable install (`%USERPROFILE%\.claude\ai-harness-toolset\current`) 의 payload, (b) global Claude skill 자산, (c) global `%USERPROFILE%\.claude\CLAUDE.md` 의 `AI_HARNESS_TOOLSET_GLOBAL` managed block, (d) global Codex `AGENTS.md` (default `%USERPROFILE%\.codex\AGENTS.md`, 또는 `CODEX_HOME` 환경에서 `%CODEX_HOME%\AGENTS.md`) 의 동일 managed block 이 `c81fe45` 의 source snippet / skill 기준으로 정합화되었다. 본 refresh 는 사용자 명시 승인 하의 managed-block replace + payload refresh 였으며, `docs/decisions/GLOBAL_ADOPTION_DECISION.md` §6 / `docs/user_guide/GLOBAL_ADOPTION_PROCEDURE.md` 의 explicit user-approved global / user config mutation scope 안에서 수행되었다. global apply 이후 source repo 의 working tree 는 변경 없음을 유지했다.
- 본 항목들이 추가하는 사실은 (1) canonical review task/pass topology 가 contract / implementation / global payload refresh 까지 한 라운드 완료되었다는 점, (2) source repo 가 `c81fe45` clean 상태로 유지된다는 점, (3) §11 의 remaining order (global behavior validation / install / update implementation / self-adoption / closeout / GJMNet) 는 본 라운드로 자동 충족되지 않는다는 점이다. 본 라운드의 global payload / managed-block refresh 는 §11 step 2 의 manual global activation 검증을 의미하지 않으며, install / update automation (§11 step 3–4) 의 implementation 도 의미하지 않는다.
- deployable payload hygiene 완료 (`f8e698e`). 본 라운드는 deployable roots (`config/`, `scripts/`, `snippets/`, `templates/`) 에 대한 Batch F docs-dependency zero audit 을 PASS 했고, 그 결과 위에 global current (`%USERPROFILE%\.claude\ai-harness-toolset\current`) payload, Claude global skill 자산, global `%USERPROFILE%\.claude\CLAUDE.md` 의 `AI_HARNESS_TOOLSET_GLOBAL` managed block (Codex 동등 destination 은 `%USERPROFILE%\.codex\AGENTS.md`, `CODEX_HOME` 환경에서 `%CODEX_HOME%\AGENTS.md`) 이 모두 `f8e698e` 의 source 기준으로 refresh 되었다. clean post-refresh global invocation smoke 는 PASS — accepted smoke evidence 는 fresh `_rerun01` 실행이며, 같은 라운드의 이전 out-of-contract smoke 실행은 completion evidence 가 아니다. reopen 조건은 deployable roots 안에 (a) source repo docs path dependency, (b) 본 라운드에서 제거된 topology marker, (c) target-unsafe deployable payload wording 중 하나라도 재출현하는 경우다.
- POST_MVP_PLAN §11 step 2 closeout — `docs/systems/install-update/GLOBAL_INSTALL_UPDATE_MODEL.md` §7.2 의 four-axis (global entrypoint / ToolRoot·ProjectRoot 분리 / target footprint / runtime artifact 위치) 가 d557580 baseline 시점의 기존 evidence 로 충족되었음을 기록한다. 본 항목은 새 validation 실행이 아니라 이미 §10 Completed 에 기록된 evidence 의 step 2 binding 이며, 새 evidence 체계 / convention / helper / archive / manifest / schema / lifecycle / mandatory report field / operations backlog 항목을 도입하지 않는다. evidence binding:
  - (1) global entrypoint — global current refresh, Claude global skill refresh, 글로벌 `CLAUDE.md` / Codex `AGENTS.md` managed block refresh 후의 invocation 이 `%USERPROFILE%\.claude` layer 에서 시작됨을 같은 라운드의 clean post-refresh global invocation smoke (accepted fresh `_rerun01`) 가 확인 (직전 bullet 의 f8e698e closeout 참조).
  - (2) ToolRoot / ProjectRoot 분리 — channel 3 (global stable install) 로 ToolRoot 가 resolve 되면서 runtime artifact 가 ProjectRoot 안에만 생성됨을 `docs/archive/audits/CLEAN_TARGET_SMOKE_CRITERIA.md` §2A 의 stable global channel 3 positive smoke 시리즈 (CH3-A: brief-init 의 channel 3 ToolRoot resolution + template SHA-256 byte-identity; CH3-B: review-cycle 의 `meta.json.toolRoot` / `projectRoot` / `projectLogRoot` binding + `review-verify` default + `-RequireResult` PASS; 및 후속 reaffirming round) 가 직접 보장. SC1–SC7 (channel 1 / channel 2 positive + channel exhaustion) 은 channel 3 외 채널 분리 동작에 대한 supporting evidence 다.
  - (3) target footprint — target 안에 `<ProjectRoot>/log/` 외의 ai-harness payload 가 생성되지 않음을 §2A CH3-C (fixture 의 forbidden subtree `.ai-harness/`, `scripts/`, `config/`, `templates/`, `snippets/` absence + source repo / global `current/` payload / global `.claude` layer 모두 unchanged) 와 SC1–SC7 의 `<SourceRepoRoot>` read-only invariant (SC7) 가 함께 보장하고, source-side payload hygiene 은 직전 bullet 의 deployable payload hygiene closeout (`f8e698e` round, Batch F docs-dependency zero audit) 이 보장. 본 step 2 closeout 은 그 hygiene closeout 의 기존 reopen 조건을 변경하지 않으며 본 marker 를 reopen 하지도 않는다.
  - (4) runtime artifact 위치 — Brief / Chatlog / Evidence / Review 네 트리가 `<ProjectRoot>/log/` 아래로만 생성됨을 3차 reconciliation 의 canonical Brief 자리 (`<ProjectRoot>/log/brief/BRIEF.md`) framing 과 §2A CH3-A (channel 3 으로 seed 된 BRIEF 가 fixture `log/brief/` 에 생성) / CH3-C (fixture `log/` only runtime artifact) 의 관찰이 확인. SC1–SC7 의 case 단위 관찰 (channel 1 / 2 경로) 은 supporting evidence 다.
  본 step 2 closeout 은 step 3 (install / update automation implementation), step 4 (install / update validation), step 5 (`ai-harness-toolset` self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet repo clean adoption) 어느 것도 자동 승인하지 않는다. `docs/archive/backlog/operations.md` 의 3 개 항목 status 도 본 closeout 으로 변경되지 않는다.
- POST_MVP_PLAN §11 step 3 partial-progress closeout — `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §6 canonical decomposition 의 anchored decisions (3-0 layer layout, 3-1 install metadata contract, 3-2~3-5 runtime pipeline contract grouping) + temp-only install-pipeline skeleton implementation (local-clone mode 의 `install` / `update-source` / `update-current` / `restore` 4 action 을 `$TestDrive` fixture 기준으로 end-to-end 동작) + 3-7 dry-run coverage extension (install→update-current→restore 연속 flow, `lastUpdatedAt` lifecycle, source-cut 거부 후 `current/` byte-identity 보존, dogfooding `update-source` 거부 후 source HEAD 무변경, forbidden InstallArea descendant reject) 까지의 누적 진행 상태가 STEP3 guide §13 (3-8 minimal docs closeout) 에 한 자리에 정리되었다. commit 시리즈: `c055ea5` (3-0 layer layout anchor), `bb9b832` (3-1 metadata contract anchor), `f11ed27` (3-2~3-5 runtime pipeline grouping anchor), `84d1126` (temp-only install-pipeline skeleton implementation), `3bff209` (3-7 dry-run coverage extension). 본 항목은 §11 step 3 의 **부분 진행 closeout** 이다 — step 3 의 deferred 잔여 (git-url mode 의 actual source acquisition / network fetch, payload integrity manifest / completeness marker, 3-6 managed-block / skill replace boundary, actual global / user filesystem apply, source-cut path 의 실제 처리, dogfooding enforcement mechanism 의 final shape, `schemaVersion` migration writer 등) 는 STEP3 guide §13.2 에 분리 기록되어 별도 scoped goal 의 대상이다. 본 closeout 은 step 4 (install / update validation), step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않으며, `docs/archive/backlog/operations.md` 항목 status 도 변경하지 않는다.
- POST_MVP_PLAN §11 step 3 3-6 boundary anchor — `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §14 에 **3-6 managed-block / skill replace boundary** 가 § anchor 로 정착되었다. 본 anchor 는 install / update automation core (§12 의 4 action) 와 managed-block / skill replace apply (`GLOBAL_ADOPTION_DECISION.md` §6 / `GLOBAL_ADOPTION_PROCEDURE.md`) 의 scope 분리를 한 줄 boundary 로 정의하고, in-scope (boundary 정의 / governing source-of-truth 표기 / automation 본체 scope 한정 재진술 / managed-block destination path enumeration boundary 보존 / Claude skill source / destination shape boundary 보존) 와 out-of-scope (actual managed-block insert / replace, actual Claude skill install / update / removal, automation core 의 묶음 apply 호출, `%USERPROFILE%\.claude\AGENTS.md` 생성, whole-file overwrite, marker / detection algorithm 의 재정의 / 복제, 부모 root-level docs 본문 mutation, Step 4 시작, commit / push / publish / merge / release / adoption) 를 enumerate 한다. STEP3 guide §13.1 Completed 에 3-6 anchor entry 가 추가되었고, §13.2 Deferred 의 "3-6 managed-block / skill replace boundary" 항목은 §14.4 의 후속 implementation 항목 (boundary 진단 helper / check-only helper, managed-block apply 의 actual writer surface, Claude skill install / update / removal 의 actual writer surface, boundary violation detection mechanism, 3-6 의 implementation-level closeout) 으로 좁아졌다. commit `9cf2000` (`Anchor Step 3 3-6 managed-block boundary`). 본 entry 는 STEP3 guide §6 canonical decomposition 9 단계 중 남은 § anchor (3-6) 가 작성되었음을 기록하는 docs-only 갱신이며, 신규 implementation / 새 contract field 도입 / source code 변경 / tests 변경을 포함하지 않는다. 본 entry 는 step 4 (install / update validation), step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않으며, `docs/archive/backlog/operations.md` 항목 status 도 변경하지 않는다. actual managed-block insert / replace, Claude skill install / update / removal, global / user filesystem mutation, commit / push / publish / merge / release 어느 것도 본 entry 로 자동 승인되지 않는다.
- POST_MVP_PLAN §11 step 3 payload integrity manifest + payload completeness marker minimum contract — `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §15 에 payload integrity manifest 와 payload completeness marker 의 minimum contract 가 § anchor 로 정착되고, `scripts/lib/install-pipeline-core.ps1` 에 temp-only local-clone implementation 이 (manifest write / read / verify + marker write / read + dispatcher write hook + verify hook 의 manifest per-file size+SHA-256 / extra-on-disk / missing-on-disk / marker presence / head cross-binding 검증) 추가되고, `tests/install-pipeline.Tests.ps1` 에 9 신규 Pester tests (AC-IP-MANIFEST-1..9) 가 추가되었다. backlog "Aggregate digest reproducibility — install/update verification scope debt" 의 candidate (b) per-file manifest 채택; manifest = `payload-manifest.json` (sibling-of-`current/`, JSON UTF-8 no-BOM, per-file `{path, size, sha256}` sorted ascending), marker = `payload-marker.json` (sibling-of-`current/`, presence flag + head/manifestPath/payloadRoots constant). write hook 은 materialization 직후 / install.json write 이전이며, verify hook 은 `Invoke-InstallPipelineVerify` 안에서 manifest / marker / metadata 의 일치성을 모두 fail-fast 검사한다. STEP3 guide §13.1 Completed 에 manifest + marker entry 가 추가되었고, §13.2 Deferred 의 "payload integrity manifest / payload completeness marker" 항목은 §15.6 의 6 항목 (entrypoint set finalize, aggregate digest algorithm (candidate (a)) 의 별도 도입, schema bump migration writer, 외부 검증 tool / linter, channel 3 활성 hook 의 actual implementation, target adoption / external target 에 대한 manifest / marker) 으로 좁아졌다. commit `1273afe`. Pester 전체 suite 144/144 PASS (manifest+marker 9 tests 포함), `scripts/verify-ps1.ps1` PASS (17 files, UTF-8 BOM + CRLF), `git diff --check` clean. 본 entry 는 actual global / user filesystem mutation, git-url mode actual network fetch, source-cut path 의 실제 처리, dogfooding enforcement final UX, managed-block apply, Claude skill install / update / removal, Step 4 (install / update validation) 시작, step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않으며, `docs/archive/backlog/operations.md` "Aggregate digest reproducibility" 항목의 status 도 자동 변경하지 않는다 (본 entry 는 candidate (b) 채택을 STEP3 guide §15 안에 record 하며, backlog 항목 자체의 status 변경은 별도 scoped goal). actual managed-block insert / replace, Claude skill install / update / removal, global / user filesystem mutation, commit / push / publish / merge / release 어느 것도 본 entry 로 자동 승인되지 않는다.
- POST_MVP_PLAN §11 step 3 git-url mode minimum source acquisition contract — `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §16 에 git-url mode 의 deterministic local-git-operations 기반 source acquisition contract 가 § anchor 로 정착되었다. §10.5 의 deferred source-cache layer 결정을 reassessment 결과로 §16 에서 채택 — canonical `<InstallArea>/source-cache/` (sibling-of-`current/`), normal-clone format, single-cache-per-InstallArea, per-action lifecycle (install → `git clone -q`; update-source → `git fetch -q origin <branch>` + resolve `origin/<branch>`; update-current → cache-local `git rev-parse HEAD` (no network); restore → cache-local `git rev-parse --verify <user-ref>^{commit}` (no network)). `scripts/lib/install-pipeline-core.ps1` 에 cache helper / clone / fetch / remote-head resolve 함수 4 신설 + `Invoke-InstallPipelineNativeGit` helper (PS 5.1 NativeCommandError 회피용 $ErrorActionPreference scope pin) 추가; materialization source 가 tuple.toolRoot (cache path) 로 통일되어 local-clone / git-url 두 mode 가 동일 pipeline 으로 흐른다. `scripts/install-pipeline.ps1` entry 가 mode-branched tuplePath resolution + git-url 의 clone/fetch hook 을 dispatcher 진입 이전에 수행. `tests/install-pipeline.Tests.ps1` 에 9 신규 Pester tests (AC-IP-GITURL-INSTALL-1, UPDATE-SOURCE-1, UPDATE-CURRENT-1, RESTORE-1, CACHE-MISSING-1, RESTORE-MISSING-REF-1, FETCH-FAILURE-1, FORBID-1, REINSTALL-1) + `New-FixtureSourceRepo` 의 init-branch 를 `main` 으로 deterministic 화 + `New-FixtureBareRepo` / `Add-FixtureBareCommit` test helper 신설 + `Invoke-InstallPipeline` helper 가 $ErrorActionPreference scope pin 으로 native stderr capture safety 확보. test fixture 는 `git clone --bare -q` 로 만든 local bare repo 만 사용하며 외부 GitHub / 외부 network reachability 의존 없음 (§16.7 boundary 와 정합). STEP3 guide §13.1 Completed 에 git-url anchor entry 가 추가되었고, §13.2 Deferred 의 "git-url mode 의 실제 source acquisition / network fetch" 항목은 §16.6 의 후속 deferred 항목 (credential / auth handling, clone recovery, multi-cache / per-ref subdirectory, bare clone 채택, cache identity verification, fetch retry / backoff / shallow clone, HTTP/HTTPS/SSH/git protocol specific 처리, submodule, source-cut path 실제 처리 / actual global apply 등) 으로 좁아졌다. commit hash 는 본 closeout 라운드의 후속 commit (사용자 명시 결정 후) 으로 carry. Pester 전체 suite 154/154 PASS (이전 145 + 신규 9 git-url tests), `scripts/verify-ps1.ps1` PASS (17 files, UTF-8 BOM + CRLF), `git diff --check` clean. 본 entry 는 actual `%USERPROFILE%\.claude` / `%USERPROFILE%\.codex` mutation, actual external network reachability test, credential / auth implementation, clone recovery, source-cut path 의 실제 처리, dogfooding enforcement final UX, managed-block apply, Claude skill install / update / removal, Step 4 시작, step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않는다. actual global / user filesystem mutation, commit / push / publish / merge / release 어느 것도 본 entry 로 자동 승인되지 않는다.
- POST_MVP_PLAN §11 step 3 source-cut path actual handling decision anchor — `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §17 에 source-cut path 의 actual handling 결정이 **`deferred with exact boundary`** 로 § anchor 되었다. `explicit unsupported fail-fast now` 도 `minimum support now` 도 채택하지 않으며, future implementation 가능성은 보존하되 본 라운드의 자동 승인은 거부한다. 본 anchor 는 (a) 3 concept (local-clone mode / git-url mode / source-cut path) 의 boundary separation 을 한 자리에서 종합 — local-clone / git-url 은 mutually exclusive 한 두 mode 이며 source-cut 은 세 번째 mode 가 아니라 metadata mutation class. (b) §12.7 의 6 trigger field (`installMode` / `repoUrl` / `sourcePath` / `toolRoot` / `branch` / `remote`) 를 §17 에 복제 anchor (재정의 아님). (c) §17.4 in-scope behavior 보존 — resolver detection-only / dispatcher non-process / STOP / destination artifact byte-identity preservation (§12.7 / §12.4 / §15.4 / §16.4 와 정합) 그대로. (d) §17.5 deferred boundary 8 항목 enumeration — 재install handler / metadata mutation writer / new install path 및 cutover 절차 / approve UX / source-cache lifecycle on mode change / `schemaVersion` source-cut-related field 추가 / dogfooding mode interaction enforcement / source-cut 처리 후 manifest+marker 재작성 절차. (e) §17.6 forbidden enumeration. STEP3 guide §13.1 Completed 에 본 anchor entry 가 추가되었고, §13.2 Deferred 의 "source-cut path 의 실제 처리" 항목은 §17.5 의 8 항목으로 좁아졌다. commit `9308f3d` (`Anchor Step 3 source-cut path actual handling decision`). 본 entry 는 docs-only 갱신이며 `scripts/install-pipeline.ps1` / `scripts/lib/install-pipeline-core.ps1` / `tests/install-pipeline.Tests.ps1` 어느 것도 변경하지 않았다. 본 entry 는 source-cut handler 의 신규 implementation, install-pipeline 의 source-cut 자동 처리 함수 추가, 신규 Pester test 추가, metadata in-place mutation, actual global / user filesystem mutation, git-url mode actual network fetch 의 추가 확장, managed-block apply, Claude skill install / update / removal, Step 4 (install / update validation) 시작, step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않았으며, `docs/archive/backlog/operations.md` 항목 status 도 자동 변경하지 않았다. actual managed-block insert / replace, Claude skill install / update / removal, global / user filesystem mutation, commit / push / publish / merge / release 어느 것도 본 entry 로 자동 승인되지 않았다.
- POST_MVP_PLAN §11 step 3 dogfooding enforcement final shape decision anchor — `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §18 에 dogfooding enforcement 의 final shape 가 § anchor 되었다. §12.8 의 마지막 문장 ("본 boundary 의 정확한 enforcement mechanism (warning prompt / `--allow-dogfood-mutation` flag 등) 은 implementation 시점에 결정 — 본 anchor 가 fix 하지 않는다") 의 closeout. 5 명제 final shape — (1) explicit-flag bypass 모델 (`-AllowDogfoodSource` switch); warning-only / interactive prompt / env var / config file bypass 거부, (2) strict mode confinement — local-clone mode 에만 적용, git-url 은 구조적으로 dogfooding-irrelevant (§16.2 source-cache 가 InstallArea 안), (3) action × mode × dogfooding boundary matrix (§18.4 — install ALLOWED, update-source BLOCKED-default / explicit-confirm-required via flag, update-current ALLOWED, restore ALLOWED git-archive 한정), (4) 5 tree separation invariant (source repo working tree / global current / InstallArea / source-cache / ProjectRoot.log 의 비-overlap; pipeline 의 어느 action 도 source repo working tree 를 직접 mutate 하지 않음), (5) docs-only 결정 — 현행 skeleton 의 as-built 동작 (`Test-InstallPipelineDogfoodingSource` helper, `-AllowDogfoodSource` switch, 3-7 dry-run "dogfooding `update-source` 거부 후 source repo HEAD 무변경" Pester case) 을 final shape 로 anchor. dogfooding detection 의 두 detection point 분리 — invocation-time ToolRoot resolution (`SHARED_GLOBAL_INVOCATION_CONTRACT.md` D1 channel 4) 와 action-time source-side dogfooding (`Test-InstallPipelineDogfoodingSource` 의 `SourcePath == ProjectRoot` + `Test-IsSourceRepoRoot` 조건) 의 별개 detection 의미. STEP3 guide §13.1 Completed 에 본 anchor entry 가 추가되었고, §13.2 Deferred 의 "dogfooding enforcement mechanism 의 final shape" 항목은 §18.6 의 8 항목 (dirty source working tree guard, restore implementation strategy 변경 시의 dogfooding 보호 재anchor, install 의 dogfooding source 에 대한 explicit-confirm 도입 여부, source-cut path 와 dogfooding mode 의 interaction enforcement, dogfooding enforcement 의 actual implementation 확장, dogfooding enforcement bypass 의 audit log, Claude Code agent-driven invocation 의 dogfooding boundary, InstallArea placement 의 source-repo subtree 거부 enforcement) 으로 좁아졌다. commit hash 는 본 closeout 라운드의 후속 commit (사용자 명시 결정 후) 으로 carry. 본 entry 는 docs-only 갱신이며 `scripts/install-pipeline.ps1` / `scripts/lib/install-pipeline-core.ps1` / `tests/install-pipeline.Tests.ps1` 어느 것도 변경하지 않는다. 본 entry 는 dogfooding enforcement 의 신규 implementation, install-pipeline 의 새 함수 / 새 flag 추가, 신규 Pester test 추가, metadata schema 변경, actual global / user filesystem mutation, managed-block apply, Claude skill install / update / removal, Step 4 (install / update validation) 시작, step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않으며, `docs/archive/backlog/operations.md` 항목 status 도 자동 변경하지 않는다. actual managed-block insert / replace, Claude skill install / update / removal, global / user filesystem mutation, commit / push / publish / merge / release 어느 것도 본 entry 로 자동 승인되지 않는다.
- POST_MVP_PLAN §11 step 3 D (materialization atomicity) reinstall-first closeout — D materialization atomicity / transaction design 이 transaction log / rollback framework / tamper detection / partial-state reconciliation 구현 항목이 아니라 **reinstall-first recovery policy** 로 닫혔음이 docs-only 로 정리되었다. generated payload (`current/` + `install.json` + `payload-manifest.json` + `payload-marker.json`) 의 partial / unknown / 손상 상태의 회복 source-of-truth 는 기존 installed payload 가 아니라 trusted source identity (resolved commit SHA) 이며, 회복 = trusted source 재준비 후 deterministic overwrite reinstall 한 줄이다 (별도 broken-payload mode 없음; 본 toolset 은 detection 만 보장). activation surface 는 generated payload 와 별도 영역으로, managed-block instruction file (`CLAUDE.md` / `AGENTS.md`) 은 dry-run / marker validation / pre-write backup / 손상 즉시 rollback / 적용 후 verification 의 managed-block tooling 대상이고 (A-1 / A-2 tooling 제공), Claude skill `SKILL.md` 는 managed-block file 이 아니라 whole-file copy / update + hash verification + 사용자 수정 overwrite 사전 고지 모델 (`INSTALL.md` §10) 로 별도 backup / rollback / dry-run tooling 을 두지 않는다. 반영 위치: `INSTALL.md` §9 / §9.1 / §11, `docs/systems/install-update/GLOBAL_INSTALL_UPDATE_MODEL.md` §3.1 / §12, `docs/systems/install-update/STEP3_INSTALL_UPDATE_DECISION_GUIDE.md` §19.5 (§7 #5 / §12.11 open question closeout), `docs/archive/backlog/operations.md` "Activation managed-block apply tooling hardening" §Closeout. 본 entry 는 docs-only 갱신이며 `scripts/` / `tests/` / `snippets/` / global / user filesystem 어느 것도 변경하지 않고, transaction / rollback / tamper detection / partial-state reconciliation 의 신규 implementation, step 4 (install / update validation), step 5 (self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않는다. 본 closeout 은 Step 3 deferred 잔여 중 D atomicity 항목만 reinstall-first policy 로 닫으며, step 3 의 다른 deferred 잔여 (actual global / user filesystem apply, git-url actual network fetch, source-cut path 실제 처리 등 — STEP3 guide §13.2) 와 §11 numbered order 어느 것도 변경하지 않는다. install / update validation (step 4) 및 그 이후 step 들은 그대로 별도 scoped 승인 대상이다.
- POST_MVP_PLAN §11 step 4 install / update validation closeout — §11.1 anchor 의 Tier A / Tier B scope 가 실제 실행으로 닫혔다. **Tier A (fixture-local / project-local determinism):** repo HEAD 기준 install-pipeline 4 action + `Invoke-InstallPipelineVerify` + managed-block / activate-global suite 를 fixture / temp area 에서 실행해 `100/100 PASS` (0 failed / 0 skipped), 실제 `%USERPROFILE%` 무접촉. **Tier B (real installed-state validation):** 두 실제 host 에서 pure-UX git-url update + post-update verification 을 수행 — (a) **mainpc**: install metadata 가 부재한 구버전 installed state 를 trusted repo source 기준 deterministic overwrite 로 정합화, update + verification `PASS`; (b) **vanilla pc**: installed `lastUpdatedHead` 729f96a → 0a07d90 update + verification `PASS` (installedHead / installedAt history 보존). 두 host 모두 resolved HEAD = `0a07d90cf023d4ba722902f3a289edfcaf8c0fd7` 로 수렴했고, `Invoke-InstallPipelineVerify` ok=True + cross-binding (`payload-manifest.json.head` == `payload-marker.json.head` == `install.json.lastUpdatedHead`) 일치 + source fidelity 28/28 byte 일치를 확인했다. activation surface (Claude `%USERPROFILE%\.claude\CLAUDE.md` managed block / Codex `%USERPROFILE%\.codex\AGENTS.md` managed block / `ai-harness-review` skill) 는 두 host 모두 이미 source 와 동기 상태라 no-op (write target 아님). 본 Tier B 결과는 **host-global validation evidence** 이며, raw evidence 는 `docs/contracts/evidence/EVIDENCE_CONTRACT.md` / `docs/archive/audits/CLEAN_TARGET_SMOKE_CRITERIA.md` §4 의 source-repo 외부 보존 경계에 따라 repo-external (ChatGPT Web review hand-off packet) 로 보존되고 source repo 로 import 하지 않는다. **Polish triage (non-repo-owned observation, not a blocker).** host validation 중 관찰된 4 개 reporting / verification 표시 이슈 — PV-1 (`VERIFY ok=True` 이후 `$LASTEXITCODE` unset 으로 인한 harness exit-code probe wrapper artifact), PV-2 (raw `git clone` / `fetch` / `log` stderr progress 의 red-like 표시), PV-3 (inline / prose marker 문자열에 걸린 naive substring marker 비교의 false negative), PV-4 (한글 console 출력 중복 / 깨짐 rendering artifact) — 는 triage 결과 **install / update correctness blocker 가 아니며 repo-owned reproducible defect 도 아닌 non-repo-owned observation** (harness wrapper / ad-hoc command / terminal rendering 영역) 으로 분리되었다: repo 의 canonical helper (`scripts/lib/git.ps1` 의 stderr 격리, `scripts/lib/managed-block.ps1` 의 whole-line marker 추출, `scripts/install-pipeline.ps1` 의 explicit `exit` code) 가 이미 올바르게 동작하며, `tests/install-pipeline.Tests.ps1` (AC-IP-GIT-ROUTE-1..4 등) + `tests/apply-managed-block.Tests.ps1` (AC-AMB-INLINE-1 / AC-AMB-FENCE-1..2 등) `90/90 PASS` 가 이를 보장한다. **Follow-up (verification discipline hardening).** PV 관찰을 계기로 operator-side verification discipline 을 `INSTALL.md` §5.1 ("Operator verification discipline — canonical helper 우선") 로 보강해 commit `63ca6ec` 으로 반영했다 (canonical CLI / repo helper 우선, standalone-line marker boundary, raw git stderr 단독 실패 판정 금지, `$LASTEXITCODE` unset wrapper artifact 기록 + cross-binding 우선, 충돌 시 canonical 재검증). 본 §5.1 은 install / update PASS 판정을 뒤집지 않는 verification discipline 보강이다. 본 closeout 은 step 5 (`ai-harness-toolset` self-adoption), step 6 (post-MVP closeout 결정), step 7 (new GJMNet clean adoption) 어느 것도 자동 승인하지 않으며, §11.1.3 의 deferred 항목 (literal `?` / `0x3F` non-increase gate, source-cut actual handling, external git-url / network validation, BF Level 3 deterministic Brief 자동 저장 / 자동 복구) 과 GJMNet clean adoption (§7) 은 deferred 상태로 유지된다. `docs/archive/backlog/operations.md` 항목 status 도 본 closeout 으로 변경하지 않는다. 추가 actual global / user filesystem mutation, managed-block apply, Claude skill install / update / removal, commit / push / publish / merge / release 어느 것도 본 entry 로 자동 승인되지 않는다.
